# --- Core LLM / RAG stack ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
openai>=1.40.0

# --- Configuration ---
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# --- Database (conversations, messages, feedback, RLHF preference data) ---
SQLAlchemy>=2.0
# For Postgres in production, also install a driver, e.g. psycopg[binary]>=3.1

# --- Document loaders (lightweight, no `unstructured`) ---
pypdf>=4.2
docx2txt>=0.8
python-pptx>=0.6.23

# --- Production API (consumed by the React frontend) ---
fastapi>=0.111
uvicorn[standard]>=0.30

# --- Dev / test ---
pytest>=8.0
