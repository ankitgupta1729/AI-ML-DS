1.  
 
Hi, my name is Nicola, and I will be taking you through this convolutional neural networks course. I am a natural sciences graduate from the University of Cambridge in the UK with a passion for mathematics, physics and programming. In fact, I enjoy the subject matter so much. I've taken part in multiple national and international competitions where I've won numerous awards. I also hold a silver medal from the International Physics Olympiad and while I have this background, I am not doing much physics nowadays. I am a firm believer that you don't need high school or university to become proficient in a particular area. All you need is passion and a little guidance. That's why we devised this course for you to help you master convolutional neural networks. And I say we because I didn't work on this project alone. It's a collaborative effort with Eskin is Grant is a very capable software developer and computer scientist with a bachelor's degree in computer science and physics from the University of Edinburgh and a master's degree in computer science from the University of Oxford. This grant has been engaged in deep learning programming for more than five years, with a focus on recurrent neural networks. His help and input were invaluable to the creation of this course, and it was a complete pleasure to work together on this project. So what are we going to learn in the lessons to come? We'll start by taking a look at Col's in the context of image processing as they are an essential tool for working with and understanding convolutional neural networks. We'll explore how to achieve different image transformations and help you grasp the role of a mathematical operation of convolution. In this process, this will be the basis for our next topic, Convolutional Layer's. Armed with all that knowledge, we will introduce the main subject of the course. Convolutional neural networks here will discuss intriguing concepts such as feature maps and puling. What's more, we'll inspect how such a network transforms the dimensions of the Tensas. Then what follows is a short and optional neural networks revision. CNN's are simply a subtype of deep neural networks, so a general understanding of Anan's is required. That's why we'll explain the basics. Activation functions early stopping and optimizers. Once we've covered all that, you will have the minimum required knowledge to start putting all this theory to practice by building your first convolutional neural network. Working on the Éminence data set will help you grasp the general workflow of creating a CNN architecture and build one from scratch. You are going to train it to recognize handwritten digits, a very useful tool in the real world. At this point, you will get the hands on opportunity to tinker and change the network and see the results for yourself. And we won't stop at creating the scenes. We will also spend a good amount of time exploring them through the proper visualizations, through Tenzer board, the go to visualization and logging tool when working with tensor flow. This will make your journey and experimentation in the field more straightforward and definitely more memorable. Neural networks are notorious for their difficult interpretation, so we will examine the confusion matrix as a tool to help you understand and interpret the results of your networks. Finally, we'll show you how to easily tune the hyper parameters of your networks. But there's more. We will also show you how to master three common techniques to improve the performance of your models. In fact, you will have the opportunity to apply those techniques to the networks we create for the next practical section. You heard that right. The idea of this course is to give you the real CNN experience. We will have an enormous practical exercise so you can work on a real world project to do that. We've created our very own custom data set that comes from the fashion industry. It consists of more than 16000 images of trousers, jeans, shoes, glasses and sunglasses. And we will be using these for numerous practical examples and problems. We've devised a task to classify the different items with a corresponding label. Not only that, but we will also determine other characteristics such as the item, subtype and gender. Given the nature of these, we will be able to try out different techniques to achieve our goal and compare how these approaches fare against each other. You'll get a taste of the real world challenges of solving such a task and gain experience with a real project that you can later add to your portfolio. Finally, to cap it all off, we end this course with a review of the timeline of convolutional neural networks professional research. Given my background, this section is a complete must. We will dive into the workings of some popular CNN architectures and all stars like Alex Net, Google Net, as well as Resnet all make an appearance. By the end of this course, you will be completely equipped with all the tools you need to confidently work on CNN projects. Sounds great, doesn't it? This will be an awesome adventure to get the most out of this course. Please don't skip any lectures as we will gradually build on your knowledge. Now, the lessons in this course contain plenty of downloadable resources that will help you reinforce what you have learnt. Course notes, exercise files, PDF materials, handy notebook files, everything is included and can be downloaded easily. I strongly encourage you to complete all the exercises. They are designed not only for practice but also as an additional source of information that deepens your understanding of convolutional neural networks. And please don't hesitate to post in Q&A if you experience some difficulties, have a question to ask or simply want to share something with the community. Are you excited? Awesome. Let's dive straight in and begin this journey together.


2.   
  
Ever since the dawn of computer technology and electronics, many scientists and engineers have been experimenting with ways to make the computers see. This refers to the ability of a computer to process visual information in ways comparable to humans and is called, unsurprisingly, computer vision. Computer vision can have a huge impact on numerous areas because there are many tasks that require awareness of the surroundings or just the ability to understand what is illustrated in the photo to advance the field, researchers took inspiration from the source itself, the human visual system. They looked at how individual neurons operate and found some of them only search for specific patterns, such as circles. Inspired by this, they devised a special type of neural network, the convolutional neural network. This reinvigorated computer vision and this field started rapidly expanding with the beginning of the 21st century. So in the following lectures will dive precisely into the world of convolutional neural networks. But what does this term mean? Well, convolutional neural networks or CNN, in short, are a subtype of deep neural networks that have at least one convolutional layer. The details of that will be discussed in the following sections. Now, all you need to know is that they are extensively used in the field of computer vision. Computer vision is an A.I. feel that explores how computers can gain a high level understanding from digital images or videos that can be as simple the task as classifying an image to be a dog or a cat. But it can also explode in complexity, as is the case with self-driving cars, for example. Speaking of the recent innovations in self-driving cars made computer vision a really hot topic. After all, a crucial aspect of self-driving cars is for the machines to see their surroundings in real time with the help of cameras and make decisions based on that. This, however, is no easy task because a live video can contain an enormous amount of detail. Besides this input, video data needs to be filtered in real time. It wouldn't do you any good to know there is a fence ahead of you if you learn about it 10 seconds after you've crashed into it. Right. And this is where CNN has come into play. They are specialized in inferring information from data that has some spatial structure in correlation since images are structured in a 2D plane. This is the perfect avenue for this type of neural networks other than self-driving cars. There are many more avenues in which convolutional neural networks are extensively employed. For instance, you may have noticed that when you upload an image to Facebook or similar social networks, the website automatically detects the faces of all people on the photo and sometimes even recognizes the person in question. This is all done through a machine learning algorithm with a CNN at the forefront. If you think that this is simply a gadget, you should know it's far from where the contributions of CNN's. And they can also help detect illegal items at airport security's visualize radio signals and astronomy, provide faster passport control through face recognition, help understand and predict the climate and weather, and even localize wildfires and illegal fishing through satellite imaging. In short, seasons are essential for modern technologies and our continued advancement. Moreover, they are finding new applications in various fields every day. This means that you can be the person to come up with the next breakthrough in this area. Once you complete this course, you will have a deeper understanding of their workings and given enough passion and hard work, I'm confident you'll have great success. Without further ado, let's get down to business. Thanks for watching.
       

3.    

![1.png](./images/1.png)
![2.png](./images/2.png)
![3.png](./images/3.png)
![4.png](./images/4.png)
![5.png](./images/5.png)
![6.png](./images/6.png)

I know you must be eager to dive straight into the advanced stuff, but before proceeding to the convolutional neural network SPETZ, there was one subject matter we have to discuss first, Col's. And that is precisely the topic of this section. So what are image Col's? Well, an image, colonel, has been a well-known tool in image processing since at least the 70s. It is the basic building block of filters such as the ones employed by Instagram and is also the linking bridge between simple neural networks and convolutional neural networks. However, since this is a very visual topic, it is best to explain it through visuals. Let's take a look at this image. It is a simple photo of a house in Greyscale. Nothing special about it whatsoever. Now, here's a question. Can you think of some popular transformations we can apply to this image to change it up a bit? I assume that some of you thought of blurring. Indeed, that is a very prominent image transformation. So let's see how this image looks after blurring it. Not very exciting, right? Well, the reason is that blurring is a pretty natural image transformation. For instance, when a person is tired, sick, intoxicated or simply has forgotten their glasses. They see the world in a blurry way. However, when talking about digital images, blurring is nothing but a mathematical transformation. In fact, what blurring does is to smear out the differences in the neighboring pixel values. But what if we want to do the opposite? Highlight the differences further. This operation is called sharpening. Here is how it looks in our original image. Notice the more pronounced lines between the bricks. It almost looks like we increase the resolution and somehow we have more information than before. However, we should not be fooled by this visual illusion. As whatever transformations we apply, we cannot create new information, but only trick our brains into thinking there is more information. Zoom and Enhance is only possible in TV shows such as CSI Miami. All right, let's show one final transformation. What if we want to detect only the edges in our original image? Here's an example of such an operation. You may think we are talking about abstract concepts, but in fact, only five minutes from now you will be able to do it on your own. Great. Now, all of these images were generated using a kernel sometimes referred to as filter. In the next lecture, we will take a look at each of the three cases individually in order to better understand what's happening. See you there. And thanks for watching.


4.   

![7.png](./images/7.png)
![8.png](./images/8.png)
![9.png](./images/9.png)
![10.png](./images/10.png)
![11.png](./images/11.png)
![12.png](./images/12.png)
![13.png](./images/13.png)
![14.png](./images/14.png)
![15.png](./images/15.png)
![16.png](./images/16.png)
![17.png](./images/17.png)
![18.png](./images/18.png)
![19.png](./images/19.png)
![20.png](./images/20.png)
![21.png](./images/21.png)
![22.png](./images/22.png)
![23.png](./images/23.png)
![24.png](./images/24.png)
![25.png](./images/25.png)
![26.png](./images/26.png)
![27.png](./images/27.png)
![28.png](./images/28.png)
![29.png](./images/29.png)
![30.png](./images/30.png)
![31.png](./images/31.png)
![32.png](./images/32.png)
![33.png](./images/33.png)
![34.png](./images/34.png)
![35.png](./images/35.png)
![36.png](./images/36.png)
![37.png](./images/37.png)
![38.png](./images/38.png)
![39.png](./images/39.png)
![40.png](./images/40.png)
![41.png](./images/41.png)
![42.png](./images/42.png)
![43.png](./images/43.png)
![44.png](./images/44.png)
![45.png](./images/45.png)
![46.png](./images/46.png)
![47.png](./images/47.png)
![48.png](./images/48.png)
![49.png](./images/49.png)

Hey, welcome back. Last time we showed you three different transformations of a single image using kernels in this video, we will dive deeper into the mechanics of how they were generated. This may sound a bit disconnected from CNNs, but it is an important step in this direction. We promise. OK, let's start with blurring. Blurring or smoothing is one of the simplest ways we can change our original image. And you probably intuitively know how it works. Basically, its purpose is to smear out the differences, in other words, to average them out. And that is exactly how this result is achieved by averaging the values of a pixel with its neighbors. For example, suppose this is a zoomed part of an image with the squares representing individual pixels. Since we are working with grayscale images, the pixels are represented by just one number between zero and two hundred and fifty five. The principles we are going to show can be applied to color images as well, in which every pixel has three values a red or a green and a blue one. For simplicity, we will focus on grayscale images as each pixel could be denoted by a single number. All right, let's take this three by three portion of the image, it has a total of nine pixels, therefore nine values. In that case, to blur this image, we must make the pixel in the center less pronounced. Therefore, we need to calculate a new value for it. So it's time to see how many neighbors it as well. There are different definitions of neighbors, depending on whether you count only direct neighbors, diagonals and so on. For this example, we will count the diagonals as well, which means that the direct neighbors of the center pixel are these. There are eight in total. All we have left to do is to average their values right? But before we do that, it is crucial to remember that we must take the center pixel into the average as well. After all, it is the fundament of the blurring. Therefore, we take all nine pixels. Since we are taking their average, we can just multiply them by one over nine like that and then add them together. And that is how we calculate the new value of the pixel. Do that for all pixels of the original photo and we have blurred the image. All right. For homework, you can try to achieve that for the whole image. Now, this was fairly easy. How about sharpening? Well, this one is a little bit trickier, first, we've got to ask ourselves, what is it we are trying to accomplish? In contrast to the previous example, we are trying to highlight the difference in pixel values. One way to think about it is as the opposite of blurring. Instead of combining the pixel with its neighbors, we want to take only the part of that pixel that is different from the rest. That would be the change from the previous value. In other words, to sharpen out a pixel, we find the differences from the neighbors and then double it, which makes them in a way twice as different. So let's take a look at the pixilated image once again. How would we apply this algorithm when there is more than one neighbor? First, we start by identifying which neighbors to use for my experiments. I have noticed that I get better results if I don't use the diagonals. So we are going to use only those four neighbors. Now, as we discussed, we need to subtract those values from the center one. So what we are going to do is multiply them by minus one to signify this. Since we have four neighbors, then we have four differences. So we need four copies of the original pixel. So far we've accounted for the differences. Apart from that, we also need to add the original pixel itself. This results in multiplying the center value by a total of five. And now, just as before, we simply add all of those values and get the new updated value for this pixel. I know this may seem a bit daunting, but don't worry about it. Our goal is not to teach you how these specific filters work, but to give you an insight into how filters in general are constructed. It will all become a bit clearer in a couple of lectures. All right. Finally, let's talk about edge detection. Now, this may give the impression of magic, but actually it is not that difficult to understand this time. It will be easier to work backwards, though. I'm going to show you the solution and then discuss why it works. So let's go back to the pixels image one more time in order to obtain the new value for the center pixel. We multiply these neighbors by minus one, then multiply the center pixel by four and lastly, add them together. This looks familiar, doesn't it? That's because it is almost identical to the sharpened filter, with the only difference being that we multiply the center by four instead of five. So why is that? Well, at the end of the day, the purpose of edge detection is to highlight the edges. That is to highlight the portions of the image that have high contrast. In other words, here we want to see only the difference, hence the factor of four. By the way, this is exactly what we were trying to do with sharpening. What distinguishes the two is that with sharpening, we were adding these edges on top of the original image. Let's take a look at two cases of edge detection to get a better picture. Suppose all pixels have the same value in this case, the same shade of gray with value. A let's apply the algorithm we discussed. We take the center value and multiply it by four. That's four times a then we subtract the values of the neighbors. In this case, that is again four times. So in the end we are left with zero. Thus, if there is a place in the image where the surrounding region has the same value or close to it, that is there is no edge. The result of the transformation would be black or very close to black. Now let's look at another situation. We have two regions with two values, A and B, we can say that there is an edge between them. How does the algorithm perform in this case? Well, we have four times A minus A, minus A, minus B, minus B, which is two times A minus B. Thus, in this case, the value of the new pixel would be as high or as white as possible when the difference between A and B is bigger. In other words, our algorithm detects high contrast zones. All right, that's enough about those filters we used in the next lecture. We will connect those to the concept of kernels. Stay tuned. And thanks for watching.
   

5.   

![50.png](./images/50.png)
![51.png](./images/51.png)
![52.png](./images/52.png)
![53.png](./images/53.png)
![54.png](./images/54.png)
![55.png](./images/55.png)
![56.png](./images/56.png)

Hello there and welcome to this lecture. Last time we discussed in detail how the three different transformation algorithms work, now we will see how that connects to kernels. So far we've seen how filters work by substituting each pixel value with an appropriately weighted average of its value and its neighbors values. We visualize that by having the factors go on top of each pixel. This can be seamlessly expressed in a matrix form. For instance, recall that the blur consisted of factors of only Ninth's. We can express that with this matrix. Notice that in the Matrix itself, we included only ones while we have the one 9th at the front. This more clearly shows that we take all neighboring pixels with equal importance. OK, what about the sharpening filter? Well, it is straightforward. Here is the matrix for it. Just as we discussed, there are four factors of minus one and one factor of five at the center. The rest of the pixels that we don't use are zeros. Finally, here is the matrix for edge detection. Notice how, without words, these matrices neatly and conveniently summarize the logic of the algorithms. And that is what we call Col's kernels are the matrices that describe how a certain transformation is to be applied to an image. Kernels are central to image processing because of how well they represent the information it is provided both in a visual way for humans to understand at a glance, as well as in a form optimized for the computer to operate with. Doing computations with matrices is one of the most efficient ways to do them with a computer. Great, but what is the relation of kernels to convolutional neural networks? Well, CNN's are usually employed when the data has some structure, such as images. In that case, we need a way to transform the data without losing the underlying structure. As you probably guessed, that is done through the use of kernels. That is why it is important to understand how they work. All right. Tune in the next video where we explain how kernels are applied and why those types of networks are called convolutional. Thanks for watching.


6.   

![57.png](./images/57.png)
![58.png](./images/58.png)
![59.png](./images/59.png)
![60.png](./images/60.png)
![61.png](./images/61.png)
![62.png](./images/62.png)
![63.png](./images/63.png)
![64.png](./images/64.png)
![65.png](./images/65.png)
![66.png](./images/66.png)
![67.png](./images/67.png)
![68.png](./images/68.png)
![69.png](./images/69.png)

Now we know how to construct the new value of a pixel by combining the old image and placing the kernel over the desired pixel. But how do we create the new image? Well, the answer comes quite naturally. We simply slide the kernel over every individual pixel of the image. This operation of sliding the colonel over an image and calculating the value for every position is called convolution in computer science. That is why the neural networks that use Col's are called convolutional neural networks. Mathematically, convolution can be described by this equation here S of I and J is the resulting pixel at position I. GE Capital II is the original image and Capital K is the kernel or filter convolution is described with the star symbol. So that's why we have AI Star K here. Finally, the convolution itself is this double some here. Now the mathematical details of this are not important. However, some of you probably noticed that the kernel is actually inverted in this equation. That is to say that if we follow this equation closely, it is equivalent to rotating the kernel and only then multiplying and adding the result. And that is true. Convolution flips the kernel around. However, in machine learning, whether we use a flipped filter or not does not matter as the neural network learns the different parameters anyway. So if we flip the kernel, the network will just learn the parameters in flipped order. That is why in machine learning we actually use a non flipped version of the kernel. Finally, for those of you who are curious, we'd like to take a quick sidestep. Please note that the term convolution in the case of CNN is a bit of a misnomer in mathematical terms. Using a non flipped kernel is called cross correlation. However, in the ML community, literally everyone calls it convolution and so will we. Great. While the convolution formula looks straightforward now, there are some edge cases and in fact it literally breaks around the edges when the kernel sticks out of the image, so to speak. So how do we proceed in this situation? Well, that is what we are going to discuss in the next lecture. Thanks for watching.

7.   

![70.png](./images/70.png)
![71.png](./images/71.png)
![72.png](./images/72.png)
![73.png](./images/73.png)
![74.png](./images/74.png)
![75.png](./images/75.png)
![76.png](./images/76.png)
![77.png](./images/77.png)
![78.png](./images/78.png)
![79.png](./images/79.png)
![80.png](./images/80.png)
![81.png](./images/81.png)

In this video, we will explore some edge cases, we will literally deal with the edges of an image. Here's the problem. If we are trying to apply a kernel on an edge pixel as part of the kernel will obviously stick out of the image. Since we are supposed to multiply the pixel values with the kernel factors, that's a problem we need to resolve. Many different solutions are employed in practice. One of the easiest ones is to extend the image outwards with pixel values set to zero. That is equivalent to just not using the part of the kernel sticking out. Since it is easier to simply overwrite the sticking parts with zero, we prefer to use that when coding. What about the performance of this remedy? Well, those simplistic that works well for a lot of situations and is one of the most popular methods to deal with the issue. In most places, this is referred to as zero padding. OK, another common solution is to expand the image, however, instead of setting all the values to zero, they may be taken from the actual image. This is like we are placing a mirror at the edge that reflects all values. Sometimes these values are taken with a lower weight. All right. But if you want to really out of the box solution, here's one. Sometimes the best way to resolve the problem may be to just not deal with that problem at all in practice. That would mean to simply omit all pixels for which the kernel sticks out of the image. Essentially, that would trim the border of the image out, reducing its size. For instance, suppose we have a 256 by 256 pixels image and our kernel has a dimensionality of five by five. If we don't deal with the edges, we will obtain a 252 by 252 image. This is not a huge loss of information, especially for images bigger than 256 pixels. So there are situations in which that is an OK practice. A fact that helps a lot is that the objects of interest are rarely found at the border of an image, as you will see later on in CNN's. Oftentimes we don't care that much about a few pixels at the border. Moreover, CNN's involve operations such as puling, where we basically throw away half of the information in an image anyway. Therefore, removing a few pixels at the borders is no biggie in the context of CNN. Great. Now we know what kernels are and how they are applied. In the next section, we will explore the role in convolutional neural networks and how everything is constructed. Stay tuned and thanks for watching.


8.  

![82.png](./images/82.png)
![83.png](./images/83.png)
![84.png](./images/84.png)
![85.png](./images/85.png)
![86.png](./images/86.png)
![87.png](./images/87.png)
![88.png](./images/88.png)
![89.png](./images/89.png)
![90.png](./images/90.png)
![91.png](./images/91.png)
![92.png](./images/92.png)
![93.png](./images/93.png)
![94.png](./images/94.png)
![95.png](./images/95.png)
![96.png](./images/96.png)
![97.png](./images/97.png)
![98.png](./images/98.png)
![99.png](./images/99.png)
![100.png](./images/100.png)
![101.png](./images/101.png)
![102.png](./images/102.png)
![103.png](./images/103.png)
![104.png](./images/104.png)
![105.png](./images/105.png)
![106.png](./images/106.png)
![107.png](./images/107.png)
![108.png](./images/108.png)
![109.png](./images/109.png)
![110.png](./images/110.png)
![111.png](./images/111.png)
![112.png](./images/112.png)
![113.png](./images/113.png)
![114.png](./images/114.png)
![115.png](./images/115.png)
![116.png](./images/116.png)
![117.png](./images/117.png)
![118.png](./images/118.png)
![119.png](./images/119.png)
![120.png](./images/120.png)
![121.png](./images/121.png)
![122.png](./images/122.png)
![123.png](./images/123.png)
![124.png](./images/124.png)
![125.png](./images/125.png)
![126.png](./images/126.png)



Hello and welcome back. In the previous section, we talked about kernels and how to transform images in a certain way. We understood how blurring, sharpening and edge detection are achieved and what their connection to Col's is. In this section, we will leverage this knowledge to learn more about convolutional neural networks. We will discuss why we need them and how they work. Then we will construct our first CNN on the Éminence data set. All right, let's get into it. As you probably realize from the last section, Col's can transform an image in various ways, some more useful than others. Turns out that these transformations precisely are crucial to our ability to create image recognition. How would that work? Theoretically, let's say we want to classify an image as either a dog or a cat similar to our edge detection kernel. From the previous section, we can construct a kernel that would detect I like objects or shapes and so on note that detect in this context means to emphasize the differences between various features in an image. In essence, we will be ignoring the similarities and focusing on the differences between the different photos. Based on these differences, we could classify the image as a cat or a dog. With this conceptual idea in mind, we may try to actually construct such a kernel. Unfortunately, achieving that manually would be a burdensome task. Also, it will probably fail when being faced with different dog and cat breeds. What if we could find a way in which to create such a kernel automatically and reliably? What if we didn't even know which features of the image would be worth detecting? Well, based on the topic of this course, this question has only one possible answer. We can do that and we will. We will let the computer learn what the best possible kernel to use is. On top of that, instead of having just one kernel, we can have hundreds of them since the computer does all the work. And that's it. That is the crucial revelation that leads to the concept of convolutional neural networks. Those are neural networks that use Col's to transform the input in a more useful form and discover the best kernels to use on their own. There are many more technical aspects and subtleties, but this is the general idea. As a side note, in the context of neural networks, kernels are often referred to as filters. That is because of their use to detect certain features in an image, one might say, to filter the interesting features. In this course, we will use the two terms interchangeably. OK, but why are these types of networks called convolutional? Well, the name is related to how kernels are applied. If you remember from our last section, in order to produce the transformed image, we convolve the original one and the kernel that is, we use the mathematical operation convolution subsequently in a neural network. The layers that use kernels are called convolutional layers. Great at first sight, though, and it's not obvious why transforming in an image would be beneficial at all. At the end of the day, when we transform it, we lose some information and even worse, we twist the rest of the information through convolution. So why don't we just use the whole image with the maximum information? Well, to answer this, let's take a step back and look at the next example we solved with deep networks in one of our case studies. For those that don't know or don't remember amnesties, a data set of handwritten digits, zero to nine, consisting of seventy thousand images of size twenty eight by twenty eight pixels. In that exercise, the goal was to train a feed for neural network to recognize those digits. We achieved that by flattening the images into vectors and used a deep network with three fully connected layers to hidden layers of with fifty and one final layer of with ten for the classification for that particular model, we managed to achieve ninety seven point three percent accuracy. That was pretty amazing. So if we can achieve such great results with a deep model, why don't we just stick with it? Well, first, that is not that great of an accuracy. I don't get me wrong, it's good, but we can do much better. After all, the minus data set is one of the easiest. Also, we've achieved this accuracy on a particularly simple grayscale data set when we get to real color images of cats, dogs and so on, a simple feed forward neural network would perform poorly. But that is not the main problem here. The main issue comes from how the model treats the image, as you remember, to feed the twenty eight by twenty eight Tenzer of pixels, we first unwrap it to a vector. That is to say that we take all pixel values and order them in one big line of numbers. Because of this, we have lost the initial structure of the image and this is just awful. We have lost the spatial information. For instance, these two pixels are neighbors in the original image, but in the row of vectors they are pretty far away. Conversely, these two pixels at the two edges of the image have nothing in common but appear next to each other in the flattened vector. This is not good, not good at all. Our network doesn't actually see the image. It just sees a string of numbers. And that means that if we take an image that our model learn to classify and just move it a bit to the edge, the algorithm may be confused and no longer recognize it. You see, this method is just not good for inputs with an underlying structure like images enter scenes in scenes. We transform the image while preserving its structure. Features that appeared in the top left of the original image are still found in the top left corner of the transformed image. We don't unfold the image to a vector until the very last layer as an input. We just pass the image as is a 2D matrix, and that is the brilliance of this technique. This, in fact, has some extra benefits, such as drastically reducing the number of parameters our network needs to optimize. Imagine you want to build a self-driving car for this purpose. You set up a one to two megapixel camera, which gives us inputs of dimensions, say sixteen hundred by twelve hundred. Pretty standard. Now consider a single fully connected feed forward layer acting on this input that reduces its dimensionality by half. That would require us to introduce the staggering amount of nine hundred and twenty one billion weights to our model just for a single layer. Meanwhile, a convolutional layer with a kernel of size five by five would require only twenty six weights. Twenty five for the kernel matrix and one additional weight for the bias. There is no need to elaborate further on the computational advantages of scenes. You may argue that a single convolutional layer has less predictive power than the fully connected feed forward. And however, if we were to match the number of parameters, we could in principle stack thirty five billion convolutional layers with twenty five parameters each to reach the nine hundred and twenty one billion weights of the simple and in practice a less extreme version of that set up with just a couple of convolutional layers. Does the trick right now you understand what the motivation behind the concept of convolutional neural networks is. So it is time to get a little bit more practical. Join us in the next lecture where we are going to talk about local receptive field and feature maps. Thanks for watching.

9.   

![127.png](./images/127.png)
![128.png](./images/128.png)
![129.png](./images/129.png)
![130.png](./images/130.png)
![131.png](./images/131.png)
![132.png](./images/132.png)
![133.png](./images/133.png)
![134.png](./images/134.png)
![135.png](./images/135.png)
![136.png](./images/136.png)
![137.png](./images/137.png)
![138.png](./images/138.png)
![139.png](./images/139.png)
![140.png](./images/140.png)
![141.png](./images/141.png)
![142.png](./images/142.png)
![143.png](./images/143.png)
![144.png](./images/144.png)
![145.png](./images/145.png)
![146.png](./images/146.png)
![147.png](./images/147.png)
![148.png](./images/148.png)
![149.png](./images/149.png)
![150.png](./images/150.png)
![151.png](./images/151.png)
![152.png](./images/152.png)
![153.png](./images/153.png)
![154.png](./images/154.png)
![155.png](./images/155.png)
![156.png](./images/156.png)
![157.png](./images/157.png)
![158.png](./images/158.png)
![159.png](./images/159.png)
![160.png](./images/160.png)
![161.png](./images/161.png)
![162.png](./images/162.png)
![163.png](./images/163.png)
![164.png](./images/164.png)
![165.png](./images/165.png)
![166.png](./images/166.png)
![167.png](./images/167.png)
![168.png](./images/168.png)
![169.png](./images/169.png)
![170.png](./images/170.png)
![171.png](./images/171.png)
![172.png](./images/172.png)
![173.png](./images/173.png)
![174.png](./images/174.png)
![175.png](./images/175.png)
![176.png](./images/176.png)
![177.png](./images/177.png)
![178.png](./images/178.png)
![179.png](./images/179.png)
![180.png](./images/180.png)
![181.png](./images/181.png)
![182.png](./images/182.png)
![183.png](./images/183.png)
![184.png](./images/184.png)
![185.png](./images/185.png)
![186.png](./images/186.png)
![187.png](./images/187.png)
![188.png](./images/188.png)
![189.png](./images/189.png)
![190.png](./images/190.png)
![191.png](./images/191.png)
![192.png](./images/192.png)
![193.png](./images/193.png)



So far, we discussed why we need CNN when dealing with visual data. Now we are going to talk about how they are implemented in practice. As already stated, the crucial part of a CNN are its convolutional layers in that layer. We have our image as an input, the kernel matrix with weights sometimes referred to as a filter and the result of their convolution. Let's visualize how that works one more time to make it clearer. First, we get the image of interest. Second, we place the kernel on top of the image at a particular location. Then all pixel values are multiplied by the parameters of the kernel and some together to produce a single number. That number is the new pixel value at the position we place the kernel. We don't overwrite the value, but rather note it down in a new matrix. Then we slide the kernel around the image and perform these actions at each position. Notice how since we placed the kernel at every position possible, there is a lot of overlap. For instance, the value we calculate from this convolution would be similar to the value calculated at the next position of the kernel. This level of specificity is not always necessary. That's why sometimes instead of sliding the kernel everywhere, we skip some positions. Thus we may apply the kernel once every two pixels or so. Another thing to consider is what to do with the edges in the last section, we outline a lot of different strategies to deal with that. As mentioned, though, in CNN's, the most employed solution is to just ignore the pixels, as usually the features we are interested in do not sit at the very edge of a photo. Now, after exhausting all positions of the filter, we have compiled a new matrix of numbers. If we have skipped some positions or ignored the edges, its size may be smaller than the original image. Now, in certain situations, this new matrix is also an image, as was the case, with blurring and sharpening. Nevertheless, that is not generally true. The visualization and interpretation of the result depend on the kernel we use and is rarely a legible image. This is one of the most fascinating aspects of convolution. So let's investigate that further. We'll take the next example once again. One recurring pattern in handwritten digits is an arc, a part of a circle zero two three, five, six, eight and nine. All have at least one arc, while one four and seven don't. So if I give you this arc, could you say which digit it is from? Is it a piece from a zero or a nine? Maybe a six. You can't tell, can you? However, if I told you that some digit contains it, you at least know it's not one, four or seven. Thus, if the model detects arcs, this will increase the probability of associated numbers to be classified with the corresponding image. Similarly, detecting an acute angle would increase the probabilities of the image being a one, a four or a seven and decrease it for the other digits. So far, so good. Now the question is how can we detect arcs and acute angles? The answer pops immediately kernels. So in a sense, the kernels are not used for transforming the image to another image. They are used for detecting features in the image. Often the resulting outputs look nothing like a photo at all, but more like a collection of distinguishable features of the image. Because of this, the resulting matrix is called a feature map. You can think of it as a map showing how strong the presence of a certain feature in that part of the image is. We have already seen one feature map, the edge detection. However, this is not usually called a feature map outside of a machine learning context. The main advantage of convolutional neural networks is that they can learn by themselves what features to look for in the image and how to construct the kernel for them. As a side note, a feed forward networks can in theory learn to detect individual features like arcs and angles as well. However, it can only detect them at a fixed position in CNN's the filter slides over the whole image, so no matter where the arc is located, it will find it OK. So the convolutional layer is simply a layer in which we search for a certain pattern. The kernel may be searching for arcs or for acute angles, or it may be searching for faces. We generally don't really care what the algorithm optimizes for as long as there is a useful pattern to find it then outputs a map of where that pattern has been found. Great, but what do we do with this output? It doesn't seem useful to stack more layers on top of that because we have erased most of the information and all that is left is an is there an arc here or not? This doesn't fit nicely with the main concept of neural networks has layers that can be stacked on top of each other. So what we actually do in convolutional layers is just a bit different. Instead of having one kernel that we convolve, we may have hundreds of kernels. Ideally, they would all search for different features and thus produce hundreds of feature maps. So out of a single convolutional layer, we may get a map of ARC's acute angles, circles, eyes, faces and many others depending on the data set. Cool. Now we can stack more layers after this one as we have a lot more information to work with. Now, one might say that all we do with a convolutional layer is that we collapse the original image into its many components and use only those components that are useful for our particular exercise. That is pretty much all there is to the convolutional layer. With one exception. So far in the course, we have always assumed that the input image is in grayscale, meaning that each pixel can be described with one number. In reality, though, oftentimes we may need to deal with color images, although sometimes it is possible to get good results. If we just convert the color images to grayscale, we need to know how to deal with colors in color images. Each pixel is represented by three numbers red, green and blue intensity. Thus the original image, instead of being a matrix, can be represented by three matrices the matrix corresponding to red values, the one with blue values and one with green values. So the image transforms from a 2D tensor to a. Tenzer, along with the change of dimensionality of the image, we need to transform the colonel as well. Previously, a Matrix colonel worked well, but now we need to introduce depth to the colonel as well. Luckily, this isn't that hard to achieve. The rule is that the colonel always needs to have the same depth as the image. Since the image has a depth of three four, the three colors, the colonel also has a depth of three. Most importantly, the output doesn't change. It is still a 2D matrix. Of course, since we have hundreds of colonels, we have hundreds of feature maps. OK, so far in this section we saw what a convolutional layer is and how it is constructed. But that's only part of the story of a CNN tune in the next lecture where we will talk about the other ingredients pooling layers. Thanks for watching.


10.   

![194.png](./images/194.png)
![195.png](./images/195.png)
![196.png](./images/196.png)
![197.png](./images/197.png)
![198.png](./images/198.png)
![199.png](./images/199.png)
![200.png](./images/200.png)
![201.png](./images/201.png)
![202.png](./images/202.png)
![203.png](./images/203.png)
![204.png](./images/204.png)
![205.png](./images/205.png)
![206.png](./images/206.png)
![207.png](./images/207.png)
![208.png](./images/208.png)
![209.png](./images/209.png)
![210.png](./images/210.png)
![211.png](./images/211.png)
![212.png](./images/212.png)
![213.png](./images/213.png)
![214.png](./images/214.png)
![215.png](./images/215.png)
![216.png](./images/216.png)
![217.png](./images/217.png)
![218.png](./images/218.png)
![219.png](./images/219.png)
![220.png](./images/220.png)
![221.png](./images/221.png)
![222.png](./images/222.png)
![223.png](./images/223.png)
![224.png](./images/224.png)
![225.png](./images/225.png)
![226.png](./images/226.png)
![227.png](./images/227.png)
![228.png](./images/228.png)
![229.png](./images/229.png)
![230.png](./images/230.png)
![231.png](./images/231.png)



Welcome back to this section on CNN's in this lecture, we will discuss the second ingredient in a CNN pooling layers. We will first explain how they work and then motivate their usage. OK. If we have an image that is one thousand nine hundred and twenty pixels by one thousand and eighty pixels, we can safely assume that making it much smaller, say nine sixty by five forty, what an effect. The accuracy of our algorithm, much if at all. That's where pooling comes into play. Pooling essentially reduces the dimensionality of the problem we are facing. This helps to speed the algorithm tremendously. Why does pooling have such a special name if it is nothing but a way to shrink an image? Well, in layman's terms, pooling is basically the intelligent way to shrink an image. Moreover, as we will see in just a bit, pooling is also one of the main remedies for overfitting. Let's jump straight into an example. So there are a few types of pooling, but let's start with the most commonly used one, two by two max pooling layer with stride to the idea behind this layer is quite simple. We take a feature map from the previous layer, then partition it into two by two regions. This is possible because usually we work with an even number of pixels, finally take the maximum value in each region. That's it. The key thing to observe here is that if the input is NBN, then the output is NT over to by an over two, we replace a region of two by two four numbers with a single number, their maximum. Thus we are effectively reducing the dimensionality four times. All right. So why would that be useful at all? Well, there are two main reasons. The first reason is to reduce the number of parameters and the computational power needed to train the model while preserving the most important information. In general, efficient computation is a big topic in machine learning, as oftentimes models may be limited by the parameters of the machines on which we are training. Even if they aren't achieving the same result 100 times faster is always a plus as it helps us try more variations or even focus on other problems. Since even a single pooling layer outputs a size of only a quarter of the input, it can seriously reduce the computational strain on the system. Great training the network faster is amazing, but that is not the only reason we may wish to include pooling layers. The second reason, arguably even more important than the first, is to prevent Overfitting. You've probably heard it a hundred times by now, but it's worth repeating. Overfitting is one of the worst enemies we have to deal with in machine learning. So how can pooling help us overcome it? Well, imagine you're looking at a friend's photo from their vacation and see a cloud in the sky in the top left corner. It doesn't matter to you if the cloud is that coordinates one seventy four by one fifty one or at one seventy three by 152. It's just a simple cloud at the top left. Similarly, it shouldn't matter to the neural network exactly where the feature was. It just needs to know the rough area. However, when performing the convolution, we generally get a feature map with the same precision as the original image. Hence, pooling is usually used right after a convolutional layer to preserve only the most significant features and to make them less dependent on their precise positioning. A pooling layer effectively erases small differences in positioning. In layman's terms, this whole procedure smears out the noise. Therefore, it is harder for the network to learn this noise and in turn it is harder to overfit. So far, so good. Now let's talk about the types of pooling in stride. In general, a pooling layer can use any kernel and function that produces a single number. The kernel can be of varying size k biem and this kernel is sliding on the image as we are already familiar. However, usually. But it's not sliding in the same way as in the convolutional layer. There is a stride associated stride in this context refers to the extent to which a kernel is moved to the right or down at each step during convolution. We have only dealt with stride. One that is, the kernel moves one pixel away just to the next possible position. However, other values for the stride are possible. For instance, astride of two would mean the kernel skips one pixel, thus moving two pixels away straight. Three would skip two pixels and land on the third and so on. By changing the stride, you can make your puling kernels overlap, just touch or have gaps in between. The most common practice is to have the stride equal the side of the kernel. In that way, you get full coverage of the inputs without overlap. That is exactly what, two by two max pool with stride two means. First, the kernel size is two by two. Then we have the stride, which is again two. That means that the next kernel position will just touch the previous. This is certainly the most logical way to pull pixels together, but it's worth mentioning that it is not the only one. Finally, the word max in the name indicates the function we use on the kernel. Take the maximum of the numbers inside the kernel. Of course, we can change the function as well. L2 norm pooling is another possibility. Where you take the square root of the average square values in the region, still, Max pooling remains the most popular choice and probably the only one you would need to know. Great. That is all of the basics of convolutional neural networks, in the next lecture, we will spend some time on the dimensionality of all inputs and outputs. Thanks for watching.

11.   

![232.png](./images/232.png)
![233.png](./images/233.png)
![234.png](./images/234.png)
![235.png](./images/235.png)
![236.png](./images/236.png)
![237.png](./images/237.png)
![238.png](./images/238.png)
![239.png](./images/239.png)
![240.png](./images/240.png)
![241.png](./images/241.png)
![242.png](./images/242.png)
![243.png](./images/243.png)
![244.png](./images/244.png)
![245.png](./images/245.png)
![246.png](./images/246.png)
![247.png](./images/247.png)
![248.png](./images/248.png)
![249.png](./images/249.png)
![250.png](./images/250.png)
![251.png](./images/251.png)
![252.png](./images/252.png)
![253.png](./images/253.png)
![254.png](./images/254.png)


Welcome back. While discussing kernels convolution puling stride and colours, we cannot help but talk about dimensions, in fact, the dimensions of the various layers in a network have the biggest impact on training, time and memory. Those, in turn, can be the deciding factor between a well optimized network and a complete failure. That is why in this lecture, we will take a quick look at the dimensions of the various layers, inputs and outputs. Suppose we have an image that is 32 by 32 pixels. If it is a colored image, its actual dimensions are 32 by 32 by three. So we have this image as an input to a convolutional layer. The convolutional layer we will use as an example has 10 five by five Col's. Despite that, the dimensions of each kernel are not five by five. They are actually five by five by three because of the color of the image. In fact, Col's always extend the full depth of the input. OK, so we have an input of 32 by 32 by three involved with 10 kernels of five by five by three. What is the result of this? Well, since we ignore the edges when involving the original 32 by 32, image creates only a twenty eight by twenty eight feature map as two pixels on all four sides get truncated because we've got ten kernels. There are ten different convolutions in total. Therefore we've got ten feature maps. Thus the output becomes twenty eight by twenty, eight by ten. All right, now, let's say we have a max pool player after the first convolutional player in that case, just divide each spatial dimension by two. So we have 14 by 14 by 10 as an output. Notice have a number of the feature maps is not having only the spatial dimensions. Great. Finally, let's stack another convolutional layer after the pooling one, this time with six kernels of size, three by three. So we have 14 by 14 by 10 as an input. Thus the Colonel's actual size is three by three by ten. Since there are ten feature maps, remember the Colonel takes the depth of the input, then the output would be twelve by twelve by six. This time only one pixel has been truncated from each side because of the edge effects. Also, we only get six feature maps as a single kernel, takes all previous feature maps at once to create only a single feature map as an output. All right, when working with high level libraries, it may seem unimportant to know all the dimensions, but it is a good skill to have. So make sure you understand why all inputs and outputs are the way they are in this lecture. OK, that was all the theory about convolutional neural networks. In the next section, we will create our first CNN in practice and we'll train it on the Emmis data set. See you there. And thanks for watching.


12.  

![255.png](./images/255.png)
![256.png](./images/256.png)
![257.png](./images/257.png)
![258.png](./images/258.png)
![259.png](./images/259.png)
![260.png](./images/260.png)
![261.png](./images/261.png)
![262.png](./images/262.png)
![263.png](./images/263.png)
![264.png](./images/264.png)
![265.png](./images/265.png)
![266.png](./images/266.png)
![267.png](./images/267.png)


Since we are going to build our first convolutional neural network in the next section, I figured a quick revision session wouldn't hurt if you were feeling confident in your neural networks knowledge. Feel free to skip this section. If, on the contrary, you are not all too familiar with the terms used here, it might be a good idea to check out our materials on feed forward neural networks before jumping in to CNN. All right. Now, let's start with the revision. So the main concept of deep neural networks is to achieve complex, nonlinear behavior by stacking relatively simple layers on top of each other. A single isolated layer is well understood. However, when combined with others, much more complex behavior can emerge. In other words, the hole is much, much more than the sum of its parts. Now, apart from linear regressions and other linear models in a neural network, we always add a non-linearity to the linear combination of each layer. Reason is that if we only stack linear layers, the result would still behave linearly in machine learning context. These nonlinearities that we add are called activation functions. OK, here is a table with four of the most commonly used activation functions. Let's go through one row to see how the table is organized. First, we have the sigmoid, also known as the logistic function. Here is its formula. Next to it we have its derivative. As you may recall, the derivative is an essential part of the gradient descent. Naturally, when we work with tensor flow, we won't need to calculate the derivative as tensor flow. Does that automatically anyhow? The purpose of this lesson is to remind you of these functions, their graphs and ranges. So here is the functions graph and finally we have its range. Once we have applied the sigmoid as an activator, all the outputs will be contained in the range from zero to one. So the output is somewhat standardized. All right, here are the other three common activators. The technique also known as the hyperbolic tangent, the RELU, a.k.a. the rectified linear unit and softmax. You may notice that the graph of the softmax is missing, but don't worry about that. We will get to it in a minute. In practice, the most widely used activation function is the RELU and its many variations. In fact, we will use it extensively for our own networks. So what does it do? Well, as you can see, it is a relatively simple one, both from a computational and mathematical point of view. Its output is simply the input. If the input is greater than zero, otherwise it returns zero. It really is surprising how many things can be achieved with this simple function. Of course, there are also situations in which Relu has some drawbacks. That's why there are many different modifications for specific use cases. Great. What about the softmax? Well, softmax is used specifically when dealing with classification. If we take a careful look at its formula, we would see that the key difference between this function and the others is that it takes as an argument the whole vector A instead of individual elements. So the softmax function is equal to the exponential of the element and position I divided by the sum of the exponential of all elements of the vector. And while the other activation functions get an input value and transform it regardless of the other elements, the softmax considers the information about the whole set of numbers we have. A key aspect of the softmax transformation is that the values IT outputs are in the range from zero to one there. Some is exactly one. What else has such a property, hmm, probabilities, yes, probabilities indeed, the point of the softmax is to transform a bunch of arbitrarily large or small numbers that come out of previous layers and fit them into a valid probability distribution. This is extremely important and useful because when we deal with categorization problems, we want our output to be an easily interpretable number. That's why the softmax activation function is almost always used as the activation of the final output layer in classification problems. So no matter what happens before, the final output of the algorithm is a probability distribution. All right. That was a quick recap of the activation functions. In the next video, we will talk about early stopping. Thanks for watching.

13.   

![268.png](./images/268.png)
![269.png](./images/269.png)
![270.png](./images/270.png)
![271.png](./images/271.png)
![272.png](./images/272.png)
![273.png](./images/273.png)
![274.png](./images/274.png)
![275.png](./images/275.png)
![276.png](./images/276.png)
![277.png](./images/277.png)
![278.png](./images/278.png)
![279.png](./images/279.png)
![280.png](./images/280.png)
![281.png](./images/281.png)
![282.png](./images/282.png)
![283.png](./images/283.png)
![284.png](./images/284.png)


You may have heard this a lot, but it's worth repeating it overfitting is the worst enemy of a machine learning algorithm in this lecture. We will talk about the battle with Overfitting and the most common strategy to consider in that battle, or at least stopping, OK? Let's start with a quick recap on Overfitting in machine learning, what we fundamentally do is to construct a model with a certain number of parameters. We then try to tune those parameters based on already existing data. This is the training process. However, in doing so, we can accidentally tune our model to fit the training data perfectly. That is, to also capture the noise of the data. The problem with capturing the noise is that the model then misses the general pattern. That's what Overfitting is about. All right, so what can we do to prevent Overfitting from happening? Well, overfitting is not something that just switches on and off. It gradually builds up in the training process. Thus, one technique to counter it is to stop the training when we detect overfitting. Cool. Now, the logical follow up question is how can we detect overfitting? The answer is pretty simple. Split the data into a training set and a validation set. While we are training our model on the training set, we are testing its accuracy on the validation data set. Since the validation set is one our model has never seen, it can't possibly overfit on it. Thus, to check for overfitting, we can track when the loss on the validation set increases or the accuracy decreases. That is the point at which we have to take action, namely stop the training process. This is called early stopping to stop the training before the predetermined time of completion because we have detected overfitting great. We've covered activation functions, overfitting and early stopping. In the next video, we will quickly get over the optimizers. Thanks for watching.


14.  

![285.png](./images/285.png)
![286.png](./images/286.png)
![287.png](./images/287.png)
![288.png](./images/288.png)
![289.png](./images/289.png)
![290.png](./images/290.png)
![291.png](./images/291.png)
![292.png](./images/292.png)
![293.png](./images/293.png)
![294.png](./images/294.png)
![295.png](./images/295.png)
![296.png](./images/296.png)
![297.png](./images/297.png)
![298.png](./images/298.png)
![299.png](./images/299.png)
![300.png](./images/300.png)


In this video, we will talk a bit about the optimizers role in neural networks and their different variants as evident in the name machine learning is a process in which the computer gradually learns what parameters or weights fit the training data best. But how is this learning process actually implemented? Well, the answer is through optimizers. The optimizers job is to minimize the loss function, a metric representing how well or more precisely how bad the model is predicting. The lower the loss, the better our algorithm is behaving. Thus, most optimizers center around lowering this loss at every epoch. The simplest yet highly effective optimization algorithm of the kind is called gradient descent. Its function is to always change the weights a little bit in the direction in which the losses optimize the most. This direction is obtained by calculating the gradient, hence the name of this approach. The size of the step we take in that direction is called learning rate and is one of the central hyper parameters in a neural network. OK, this method, however, has obvious drawbacks, for example, if the network finds itself in a valley like this one, the gradient descent optimizer will fail to get it out of here. So we may get stuck in a local minimum. Consequently, we'll never find the true minimum of the lost function and will fail to optimize the parameters to correct. For this, we can use a modification of this optimizer called gradient descent with momentum. This algorithm basically keeps track of how fast we are going downhill and so we can just skip past small valleys just like a ball would do. Great, as you can imagine, this is not the only issue computer scientists have kept working to refine. Optimization algorithms skip a couple of years and the ADA grad and arms propre designed these algorithms basically change the step size or learning rate dynamically throughout the training, since a certain learning rate might be good in the beginning, but not optimal at the end of the training process. Cool. By combining the design ideas of at a grad and momentum, the adaptive moment optimizer known as Adam was born. This optimizer is the state of the art method for estimation and is probably the only one you would need to use in your work. It works well in almost all situations except for the most specific ones. Adam is also the optimizer in this course. All right, that's enough for the revision, it's time to get into the practical stuff in the next section, we will tackle the amnesty data set with the capabilities of convolutional neural networks. See you there. And thanks for watching.


15.   

![301.png](./images/301.png)
![302.png](./images/302.png)
![303.png](./images/303.png)

Hey, this is a short but very important lecture, deep learning can be performed in many programming languages. We have chosen Python for several reasons, all of which we will explore in the next lesson. Now, in order to continue with the course, you will need to install Anaconda, Python, three Jupiter Notebook and the relevant packages in this section. We will show you how to do that and we will help you to learn about the environment in which we will code. Even if you already have Anakonda, Python three and Jupiter installed. This is where we will show you how to install the relevant packages. So should you need just the last part? Please refer to the last lecture of the section. If you have any technical problems during this section, don't hesitate to ask for our assistance in the Q&A section. Good luck and thanks for watching.


16.   

Python and are probably the two most widely used languages in data science. This course uses Python, and now we will see why. There are actually very few arguments against its usage. It is an open source general-purpose, high level language. I would like to quickly go over these points. Open source means that it's free to use and people can actually contribute to it. For example, the Essakane learn package that we will use extensively started as a Google summer of code project, but developed in one of the most popular free machine learning packages. If Python wasn't an open source framework, then that wouldn't have happened. Moreover, it is available for use in all major operating systems Windows, Mac and Linux. General-Purpose shows that it is suitable for all types of needs, not only data science and machine learning, but also preprocessing Web programming and just about anything you can think of. We will focus on its analytical part. That's a bit more technical, but loosely explained it as an easy syntax, which is close to logical human language and which is later translated into lower level languages or even zeros and ones. For example, the numpad package actually goes to the programming language, see, and that's why it is so fast. That said, we have to know that Python isn't among the fastest programming languages, however, it is very easy to learn and use and there are countless packages developed and maintained for it. OK, now we have to install Python in Jupiter, but why isn't there just one software application called Python that you can install on your computer that is automatically updated and that runs everything smoothly? Well, Python is a programming language, it can allow you to communicate with the computer. To do that, you'll need the help of a specific software or an application, namely the Jupiter notebook app, which is more often called Jupiter, can help us do that. It is a server client application that allows you to edit your code through a Web browser. Consider the following graph. All units represent different software on one side, you have several language colonels, these are programs designed to read and execute code in a specific programming language like Python R or Julia. The Jupiter installation always comes with an installed python kernel and the other kernel is going to be installed additionally. On the other side, you have various types of interfaces where you can write code. They represent the clients, an example of such a client is the Web browser. The Jupiter server provides the environment where a client is matched with a corresponding languages Colonel. In our case, we will focus on Python and a Web browser as a client or as an interactive shell. Your work will be stored on a notebook document and since will be strictly using the Python language. It will be called Eye Python notebook file with a file format dot IPY and be. Having said all that, we can explain why Jupiter is used in so many large corporations like Google, Microsoft and IBM. For its design, it is well suited for demonstrations of programming concepts and training. First, in large corporations, solving a particular task could require coding in a few languages, say Python, Ah Julia or PHP. Instead of installing different interfaces for each language kernel you need, Jupiter allows you to use the same structure of the notebook type of file. Simply, each notebook you create will connect to the language kernel you request. Consider also this file can be easily stored locally or on a remote server. Therefore, Jupiter facilitates the communication between teams and a corporation tremendously. Second, Jupiter is not a text editor that opens a new window every time you execute a different part of your code, as is the case with some other software applications. In the same file, you can have pure text that can communicate a message to the reader computer code like Python, an output containing rich text like equations, figures, graphs, pictures and others. This simplifies the process of the workflow immensely, and Jupiter notebook is increasingly preferred over other software packages. That's why we'll use it to. The next step would be to install Anakonda, a software package that contains both the Python programming language and the Jupiter notebook app. Thank you for watching.

17.   

There are various ways to install Python on your computer, but especially for new users, it is highly recommended to opt for Anakonda. It will install not only Python, but also the Jupiter notebook app and many scientific computing and data science packages. Let's open Anakonda Dotcom and click on the download button. In the homepage. You have to pick one of the three operating systems Windows, Mac or Linux. I will show you how to install Anakonda on Windows, but the procedure is identical. If you're going to use the Mac or Linux version now, you must choose the best among the four provided options. Do you need Python two or Python three? And then there is a 32 bed or a 64 bit version depending on the windows you have installed. I know it sounds strange to maintain older version of the programming language as one of the main options. The reason for that is that a lot of companies had written Python to code and didn't have the resources to switch to Python three when it came out. Thus, Python two is there for legacy reasons. All new code, however, is written on Python three, including the code we will write in this course. In fact, Tensor flow is not available under Python two, so we will definitely need to go for Python three. When you choose between a 32 or 64 bit version, you can simply check your operating system. Newer computers are surely running with 64 bit processors, but if you would like to verify this before you begin, you could open your control panel menu from the start window, then select the system and security icon, then click on System Icon. In my case, it is 64 bit. So when I go back to the Anakonda website, I will select the 64 bit Python three version while waiting to complete the download. You can decide whether to leave your email to Anakonda. This is not a necessary step, so you could also select no thanks and you are good to go. When the download is finished, please double click on the file to run the application. What follows is nothing different from the standard Windows installer. Agree and press next. Until you have the chance to specify a destination folder, you could do this after selecting the browse button when satisfied with the indicated directory. Click next one more time. We suggest you only the second advanced option. The first option will automate more complicated processes, but causes some problems with previously installed software, while the second will register Anakonda as a default python on your computer. If you have not installed some other package, finally click, install and proceed until you see the window where you can finalize the installation. We will not use Anakonda Cloud just yet and we can learn about the Anakonda support later. So Allen tick both boxes. What you just install is the whole anakonda distribution, the python language, a text editor, many applications and packages. You also have the Jupiter notebook app. You can open the START menu and select the respective icon. From there, a new window will pop up. It will take a few seconds for the app to load. Once this is done, your Web browser will open a new tab with a Jupiter dashboard. In our next video will study the dashboard in more detail and will learn how to use Jupiter. See you there. Thanks for watching.

18.   

OK, so in this lesson, we'll do a quick tour of the Jupiter Dashboard as soon as you load the notebook, the Jupiter Dashboard opens each file and directory has a checkbox next to it. By taking an undertaking an item, you could manipulate the respective object. That means you can duplicate or shut down a running file. In addition, you can rename move and delete folders. The selection menu allows you to select all the files in the console of the same type by expanding this button, for example, you can mark all the folders or all the running files. In addition, you can check this little box that will directly select all the items on the page. The logic regarding the directories management is the same as the one of an operation system. Files can be grouped into folders and folders can contain other folders. From the upload button in the top right corner, you can upload a notebook into the directory you were in, the standard Explorer box opens and when you select a file, you can click on Open and it will immediately appear in your directory. Finally, you could expand the new button from the list that falls. You will most likely need to create a new text file, a new folder or a new notebook file. A notebook vial can contain code in any of the languages in your notebook section, when you create a new Python notebook file, it will be recorded in the eye python notebook format DOT IPY and be for brief, don't fear this other new name ie Python. Think of it as the predecessor of Jupiter. Consider the eye python notebook format as a legacy file format. All right, let's carry out a few operations, I will rename this folder to exercises. OK, done now I will create a new Python notebook file by expanding the new button and selecting one of the two Python formats I have here. Let's use the default Python format, the browser immediately opens a new tab for me. This is the interactive shell we mentioned earlier here. You will write your code and see its output. More on that in our next lecture. Thank you for watching.


19.  

OK, great, now that we know more about the dashboard, we are ready to examine the shell and see how we can code in Jupiter. The field you see here is called a cell. You can access a cell by pressing enter. Once you've done that, you'll be able to see the cursor so you can start typing code. The gray box is called input field. I will put a short code that says Acts is a less composed of four numbers one, two, three and four. After that, I can ask the computer to print this list for me by typing X. I can execute these commands in two ways, the first one is to hold control and then press enter by doing this. The machine will execute the code in the cell and I will stay there, meaning I will not have created or selected another cell. Observed that an output field with the same number as the input field appeared, input and output, fields of the same number are grouped together. The output represents the machine's response to your commands provided in the corresponding inputs. And the respective field cannot be modified, the second option allows for a more fluid code writing to execute the same code, hold shift and then press enter. The previous two commands are being executed and then a new cell where you can write code is created, if you use shift and enter, you can continue typing code easily. It is amusing that you can cut copy and paste cells, you can use the buttons on the main menu. Since we are keen supporters of ergonomics, we suggest you get used to the keyboard shortcuts when coding, you will mainly have to use the keyboard. So it's worth memorizing different keyboard combinations that would allow you to work faster. Let's see how we can apply shortcuts in practice. I will cut this cell by selecting it and then pressing the X key. Remember, you can always use the Arrow keys to navigate along your notebook file, so I will move up and then directly press the Vecchi to paste it. Well, my cell was moved up here below the cell I had selected before I had pressed the PVC. Now, I will proceed to copy the same cell, I will use the Arrow Keys again to go down this time and at the end I will press V again. Apparently, this cell was copied and pasted below. The other two buttons in the menu cannot be substituted by keyboard shortcut combinations, they allow you to move unselected cell up or down, just as I am doing right now. Observe that the corresponding output field moves along with its input cell. Lovely. Instead of carrying out a command with shift and enter, you might prefer clicking on this button with the mouse, the result will be the same. What is interesting is that when you work with word complex code that requires tougher calculations from the computer while running the code, a little star will appear over here before the square brackets. Sometimes this process might take too long to complete. So to stop it or break it, this classical stop symbol can do that for you. Three other shortcuts could accelerate your coding a lot. Suppose I have selected this cell after I press the A key, a new empty cell will be inserted above. If I select the same cell and press be another cell will be created just below. Imagine the last step was a mistake and I need to delete that empty cell in this case, it has already been selected so I can press the deck twice and it will readily disappear. Wonderful. All the cells we saw so far were code cells. Let's see what is a markdown cell. It is a cell that contains strictly documentation text not executed as a code. It will contain some message you would like to leave to the reader of the file to convert a selected cell into a markdown cell. You should either expand this list and opt for markdown. Or simply press the button press enter to access the cell and type some text when I run this cell, the output will be a simple statement. Now, if you want to turn a markdown cell back to a code cell, select it and opt for code from this drop down menu. Or press the wiki. To conclude, there are two advantages of using Jupiter first, when your code becomes longer marked down, cells turn out to be useful as they allow you to leave comments and explain the solution you've created. This is why practitioners love to use them. The other benefit of using this app is you can select and execute whichever cell you want to. You need not run all the previous cells to run a particular cell. This allows for solving a problem in pieces and saves a lot of computation time. This was a long but indispensable lesson, thank you for watching.


20.  

Finally, let's look at the libraries we will use in this course, the good thing about Anakonda is that many libraries among which Dumpy and pandas come automatically with it, that's a strong plus of anakonda. You don't have to install the main packages separately as you might have to do if you were to use some other software for programming in Python. Moreover, many packages related to machine learning also come with anakonda. Math plot lib. Ed Tools and Escalon. Apart from the automatically included packages, we will also take advantage of Tenzer flow to intenser flow data sets. The quickest way to install it is by opening your start menu and searching for the anakonda prompt. To install Tenzer flow, please type PIP, install tensor flow in the following way. Pip Persay means either pip install packages or pip install python. Strange, but true. Now press enter. This should install Tenzer flow to with all the accompanying packages. Now, to install Tenzer flow data sets, please type PIP, install tensor flow dash data sets. When the operation is done, you will be set up for the whole course. This is one of the quickest and easiest ways to install modules and packages on your computer in general.

21.  

We've been rambling about convolutional neural networks for quite some time now, and you may be getting bored already, although I do believe the theory part was necessary. I think it is now time to get more hands on. So in this section, we're going to do just that, create our own CNN and train it on the MLS data set. Exciting, right? If this is your first neural network, then you must be wondering what is the most data set if this is not your first, and then you must be wondering why the missed again? Well, in a nutshell, when talking about neural networks, there is a single data set that everybody has or should have heard of the M.S. data set. It can be considered as the starting point or the trivial problem in AML. It consists of 70000 images of handwritten digits from zero to nine. Each image has a size of twenty eight by twenty eight pixels and they are in greyscale. Each one is accompanied by a label denoting the digit illustrated. So this dataset is used for classification problems. All the images have been heavily pre-process to contain little to no noise and in general are quite sterile. Thus, a high classification accuracy can be easily achieved and even better due to the small inputs. It doesn't require a lot of computational power. These qualities make the amnesty data set the ideal testing ground for rookies. In addition, if you have followed our courses so far, you know we have already created a simple feed forward neural network on the emmis before. So that is only natural to do that here as well. Great. The missed images have been split into two sets, 60000 images form the training sets and 10000 are part of the testing. One noticed that there is no validation set since we are very well prepared. Theoretically, we know that this is unacceptable. We will need to create a validation data set to set an early stopping mechanism and prevent overfitting. But let's leave this for later on. For the time being, we should focus on creating a plan to tackle this problem first. What is the problem that we will be solving? Well, it is a simple classification problem. That is to say, our model will take a single image as an input and output the number that is written on that image. How about the methodology? Of course, we are going to tackle this task using a CNN. We will use a Jupiter notebook to write in Python three for the neural networks part itself. We are going to take advantage of the high level library tensor flow to tensor flow two is extremely useful as it takes care of all the little details, such as calculating derivatives back propagating and all the internal logic. All we have to do is indicate what layers we want to have in the model and any extra behavior we would like to have. This makes the ideal choice for training neural networks. All right. With that out the way, here's the workflow we are going to follow in this section. In the next lecture, we will reflect on our model and what layers to include in the network. Then we will pre-process the data we have so that it's ready to be trained on afterwards. We can create the neural network itself, train it and finally test it. So let's get going it.

22.  

Hello again. In this lecture, we will discuss the architecture we will use for our network architecture in this context means to outline what layers we are going to use, how we are going to connect them, and what some of the hyper parameters would be. So why is it important to construct this architecture now and not just start directly with the code? Well, as with any other project, it is a good practice to think about the big picture first. Sometimes the data set may dictate what our neural network would look like. Other times it may be the opposite. Given our model, we may pre-process the data in a different way to make it more suitable. In any case, it is encouraged to first think about the problem and have some sort of a plan to follow. All right. So we would like to build a convolutional neural network to classify the images from the Emmis data set. What should the first layer be in this case? Given that our input is the image itself, it is only logical to have a convolutional layer. First, we can't really have a dense layer as that would unpack the 2D image to a one D vector, which is far from our desired result. Now, in order to keep track of our network, we will visualize the layers in a diagram. There are many ways to do that. For this course we will have a rectangle represent each layer with arrows denoting the connections between the layers. Information about the layers will be provided through the use of colors and as a text inside the rectangle. OK, so our first layer is a convolutional one, here is its rectangle inside we have conv, which is short for convolutional. However, we are not done with this layer just yet. There are some hyper parameters that we need to agree upon. First, as you remember, a convolutional layer is made up of different kernels of fixed size. So let's decide what the size of our filters should be. Now is worth mentioning the main mantra of machine learning. Everything depends upon your specific network and data set. Indeed, generalisations about which hyper parameters work or which don't are very hard to make. Usually the only way to know is to just test and experiment with a lot of different values. Still, there is some sensible range of values that would work for the first running of the model. Let's think rationally about those. So far we have always discussed and considered square kernels. In principle, kernels could be rectangular in shape or any other irregular form. However, unless of the most specific of cases, a square kernel is the way to go. So we will stick with it. The shape is not the only restriction, though. Our square kernel should have signs with a size. That is an odd number to see why that is the case. Let's think of the function of the kernel. It should take one pixel on its surroundings in the original image and manipulate those values to obtain a new value for the pixel. Therefore, an essential quality of a kernel is that it can be associated with a single main pixel. That is the center pixel, of course. But what happens when we have an even size? For instance, four by four. You see there is no central pixel here, no single pixel this kernel can be associated to. That's why if we want to focus on a certain pixel, the allowed kernel sizes are always odd. Like three by three, five by five, seven by seven, nine by nine and so on. We skipped one by one as it only considers the value of one pixel and thus is equivalent to just scaling all the pixels. All right. But what are the suitable values for our particular situation? Well, our input is an image of size twenty eight by twenty eight. So we may not want our filter to be bigger than seven by seven because that might be too big compared to the image. On the other hand, in general it is believed that there is no minimum size for the kernel. So a three by three one could potentially work for this minimalistic CNN. Let's settle for the middle ground kernel size of five by five. To reiterate again, this might not be the most optimal solution, but it shouldn't be terrible either. Now we need to determine how many kernels of this kind we want to have. This is a bit more abstract, so it's not always obvious with the correct numbers should be in our case. Let's go for 50 kernels. Both the kernel size and the number of kernels are hyper parameters of our network. These values that we just arbitrarily chose may not be optimal, and we need to be prepared to check how our model would behave with different ones later on. For now, though, it is good to have some sensible starting point. The final decision to make is what activation function to use. We will use the regular RELU as it behaves well in most situations. Great. Now we have completed the first layer. So what's the next one? As we've already discussed, typically a convolutional layer is followed by a max pool one and we don't want to break this tradition now. So our second layer will be a max pool. We will also follow the conventions and have a two by two colonel with stride to so far so good in a true deep network fashion, we can now stack another convolutional layer on top of those two. This should be simple because we can just copy the previous two layers, right? Well, yes and no. Indeed, we can have the third layer be identical to the first one and the fourth layer be identical to the second one. However, let's think about the dimensions of it. So our input is twenty eight by twenty eight by one. The last dimension would have been three if we worked with color images representing the red, green and blue channels. But since we are in grayscale, it's only one. OK, this input is transformed by the first convolutional layer. We are using a five by five kernel and we are ignoring the edges. Therefore, the image will get scale from twenty eight by twenty eight to twenty four by twenty four. In addition, we have 50 filters in total, so our output from the first layer would be twenty four by twenty four by 50. Then we have a max pool layer as we know it only cuts the spatial dimensions in half. Thus when its input is twenty four by twenty four by 50, it will output twelve by twelve by 50. And here comes our second pair of layers, when we decided that a five by five colonel is an appropriate size, we were considering a twenty eight by twenty eight input's. Now, this convolutional layer is receiving a 12 by 12 spacial input. So having a smaller kernel, let's say three by three, might work better for this one. Besides having a three by three filter has the added benefit of reducing the number of parameters of our model, in fact, it reduces them by half as opposed to the five by five case, this would definitely speed up the training. All right. Not to be a killjoy, but I would like to stress this one more time. This reasoning may not be applicable in our situation at this point. The input to this layer is actually a set of feature maps with feature maps. It is not always straightforward to pick the right kernel size. Therefore, our choice may not have been ideal, but we have to start somewhere, so we will roll with it. We will leave the number of feature maps and the activation function the same as in the first layer. The feature maps are 50 with the RELU activation. Great. We now have the first four layers of our network worked out. Conceptually, however, we are not done yet at this point in the pipeline. The output is going to be five by five by 50, whereas we want our model to output 10 numbers, the probabilities of each class to be present on the image. So how can we achieve this? Let's use the stuff we already know. Our output from the fourth layer is multidimensional and we want to make it one day. This operation is called flattening. Conveniently, Tensor Flow supports a type of layer called flatten layer, which achieves this precisely. It unpacks the input to a long vector of numbers. In our case, that would be a vector of size one thousand two hundred and fifty as five times five times fifty equals one thousand two hundred and fifty. Now that we have a single vector, we know what to do. Displace a dense layer that outputs ten numbers. Those correspond to the ten different digits we want to classify. For those of you who need a refresher, let's have a quick recap on the dense layer. A dense layer is a transformation in which every output is the result of a linear combination of the inputs. So if we have 10 inputs labeled X1 to X 10, a single output is represented by this sum. An important characteristic of the dense layer is that it can have an arbitrary amount of those outputs, each being a different linear combination. Because of this, it is also referred to as a fully connected layer. What about the activation function of this dense layer, you may ask? Well, if you remember, when it comes down to classification and probabilities, there is one function that stands out from the crowd and that is softmax. It transforms the output into a valid probability distribution. In other words, the output from our dense layer corresponds to the probability of a digit being illustrated on the image. Marvelous. This is our final CNN architecture. Let's recap first, we have a convolutional layer followed by a max pool, a construction we talked about in the CNN introduction, then we have the same construction again, a convolutional layer with a max pool layer. Those first four layers are intended to find multiple feature maps to help us identify the digit. Then in order to actually classify the image, we have a flattened layer followed by a dense one. The final output is a vector containing 10 numbers that represent the corresponding probabilities of each digit. When we look at the big picture, our network seems pretty simple to convolutional layers, to max pool layers and one dense layer. However, as you saw, there are numerous details to consider. OK. Now we have the road plan for our quote, What is left is to actually write that code. So in the next lecture we will download and pre-process the Emmis data set to get it ready for training. See you there. And thanks for watching.

23.   

Welcome back. We are now finally going to write some code we will download and pre-process the data so that it is ready for the neural network, the preprocessing would include scaling the data, splitting it into train and validation sets and batching it. This is a rather simple form of pre processing. We can even call it preparing the data instead. The reason is that the Emmis data set is already heavily preprocessed. That is also the main reason why it is so popular. Let's jump straight to the code. All right, so here we are in this Jupiter notebook, the first thing we need to do is import the libraries we are going to use in this exercise for now. Those are only Tenzer flow two and tensor flow data sets. So we import tensor flow as T.F. and Tenzer flow data sets as TF D, we will use the former for the creation and training of the neural network and the latter to obtain the Éminence data set. Now, let's continue with the main program. First, we need to define some constants that we will use later on. You don't have to worry about those yet. We will explain them later as we encounter them. Why define them now if we are not going to use them right away, you may ask? Well, it is good practice to have your parameters defined in a visible and accessible place, usually in big projects. That would be a dedicated source file. But since we are working in Jupiter, we will be defining them at the top of our program. Usually one would figure out what parameters their program requires at a later point. Then they would go back to the top and create the constants there. We do it now so that we don't break the flow later on. Good. Now for the minus data set. Feel free to explore it in the documentation here on this page. Continuing with the code, let's download the Emmis data set. We can do that through the load function from the Tensor Flow Data Sets library. In our case, the function would return the whole data sets as well as some meta information about it in a tuple. Since we want both pieces of data, we will create two variables to store them, minus data sets. And amnesty info. OK, to achieve this, we have two PhDs that load name equals amnesty with info equals true and as supervised equals true, this command would download the most data set and store it on our computer. Thus, the next time you run it, it would fetch the data set from the hard drive instead of downloading it. Let's run the Selmo. Note that this operation may take a bit of time because you're basically downloading hundreds of megabytes of images. OK, now the variable Éminence data set contains the whole data set. That means it holds the training as well as the test set consisting of all the images and their labels. However, in this form, the data set is not that useful. We want to have the training and test sets separated that can be easily achieved as the Éminence data set variable is a dictionary with the two sets labeled simply as train and test. Here is the code Emmis Train and Emmis Test equals amnesty data set. Brackett's train feminised data set Brackett's test. That's it, we have now separated the different sets. OK, so we know that the images are stored in greyscale, this means that the value of every pixel is given by a single number that represents how white or black that pixel is. The number varies from zero to two hundred and fifty five in machine learning. However, we like numbers that are standardised in some way. So a common technique when dealing with images is to scale the pixel value to be between zero and one that can be achieved by simply dividing all pixels in the data set by two hundred and fifty five. Instead of going through all values in the data set, though, it is a better idea to create a function that can help us achieve that. We can then use this function in conjunction with the MAP method to apply it to every element in the data set. All right, I will call this function scale. It takes an image and its label as arguments. We will first cast the image as a tensor flow flow through to using the tensor flow cast. We do this to avoid some problems with data types. Then we simply divide the image by two hundred and fifty five. Finally, the function returns, the transformed image and the label. Here we go. We didn't do anything with the label as the image still represents the same digit. However, we had to include it in the function because it's required in order to use the map method. The method takes a function and applies it to all elements in the data set. It does not modify the data set in place, but returns a new one instead. That's why we need a new variable to store it. So here I will name the variable train and validation data. It is equal to minus train dot map and the name of our function scale, we can do the same for our test set and store it in the variable test data. When we run the cell, it will scale every image and save it to the corresponding data set variable. Great. So we are done, right? Well, not quite. As we mentioned in the first lecture of this section, the M.A. data set does not have a validation set. The validation set, though, is crucial to prevent overfitting. So we will need to split the training set manually to create a validation set. The first thing we need to decide is how big it should be. A reasonable size in machine learning is considered around 10 percent of the training set. Since we know the number of training images to be sixty thousand, we could calculate and hard code the size of the validation set to six thousand hard coding such parameters is not advisable, though, so we assign the variable number of validation samples to be equal to 10 percent or point one of the training samples. How to get training samples, though? Well, that information is contained in the info variable. That's the reason we kept that variable around. Therefore, we have point one times amnesty info dot split's. Train so far, we have extracted the info for the training set to get its size. We just add dot now example's. We will also cast this variable to a Tenzer flow integer. Since we are here, we might as well get the size of the test samples in a similar way. Amnesty info, dot splits test dot now examples. Now that we have the size of the validation sets, we need to take that many samples from the training set and extract them into a different set. There is one catch, though. The data set is often ordered or sorted in some way. For example, it could be the case that the first 6000 examples are of Xeros, the next 6000 are ones and so on. However, we need the validation set to contain data with the same distribution as the training data. To do this, we will shuffle the original training data to make it a random uniform distribution and then take the first 10 percent as the validation set. Shuffling the set is a straightforward process. With tensor flow data sets, we can simply use the shuffle method so we have train and validation. Data equals train and validation data. Dot shuffle, buffer size. You may have noticed we opted for one of the parameters we define at the beginning of the code, the buffer size, we use it when we are dealing with enormous data sets. In such cases, we can't shuffle the whole data set in one go as we can't fit it all in the memory of the computer. So we need to shuffle it in batches. The buffer size variable just states how big these shuffling batches should be. Know that the shuffle batches have nothing to do with the training batch size. In our case, amnesty is not that big of a data set, so we can shuffle it all at once. That's why the buffer size has been said to be as big as the whole training set. Sixty thousand. Running this cell has shuffled our data set, now we can proceed with splitting it into a training set and a validation set. We do this in two lines using the skip and take methods to get the training set. We write train data equals train and validation data. Dot Skemp, num validation samples, this line will return the data set except for the first and examples where NT in this case is no validation samples next to get the validation set, we analogously right train and validation data dot take num validation samples. This is the opposite of skip. It takes the first and example's there we go. By running this cell, we have split our data set into train and validation. Fantastic. However, we are not done yet. We still need to bash the data set for optimal performance of the network. Let me show you how this is done. Train data equals train data, Dot Bache. As with the previous operations, this one is straightforward to implement, it also includes the second parameter define in the beginning the batch size. We have set that one to one hundred and twenty eight. The batch size is another of parameter of the training process that can potentially have a big impact on the training time and the final accuracy. As with all hyper parameters, to find the optimal value, you need to experiment. As a general rule of thumb, though, a bigger batch size but not extremely big helps speed up the training process, especially if the training is done on a GPU. On the other hand, a lower batch size gives better test accuracy. It is desirable to set the batch size to a value that is a power of two, for instance, 30 to 60 for one, twenty eight to fifty six and so on. For this run we have settled on one twenty eight. The validation and test sets don't necessarily need to be batched as we don't backward propagate on them. However, the model expects these to be batched as well to get the proper dimensions. That's why we will batch them with a batch size equal to their number of samples. So we do that for the validation data with a batch size of NUM validation samples. And the test data with no test samples. Fantastic, that's it, we are finally done with pre processing the data in the next lecture, we will build our network, train it and test its accuracy. Thanks for watching.

24.   

Welcome to the most exciting part in this lecture. We will create our CNN and train it all with the help of Tensor flow, too. Now, let's outline the workflow when dealing with Tenzer flow to there are three major steps. First, we define the configuration of the network. We already did this conceptually in the CNN architecture part. Next, we compile the model we outlined in the first step. Finally, we proceed to train the model on our data using the model dot fit method. OK, let's get started. Our first task is to tell Tenzer Flow what our network architecture is. Here is the recap of what we agreed upon. We have a convolutional layer, then a max pool again, a convolution plus a max pool. Finally, we flatten our output from the previous layer into a one dimensional vector and get an output for each of the ten classes through a dense layer. All right, so how do we code this? Well, it turns out it is pretty easy to do with denser flow as it has all layers already defined. All we need to do is call them by name. So we will name our variable that stores the network architecture model pretty descriptive. It is equal to T.F. Dot Tiara's. Don't sequential. Sequential here refers to the fact that the layers come one after the other, they are in sequence. The argument for this is a list of all the layers, so here we will list all of the layers of our network along with their hyper parameters. Tenzer Flow also provides an abstraction for the different layers through the T.F. Dot Kiraz dot layers class. This makes the process very straightforward. Great. Our first layer's should be a convolutional one, we can enter that by writing T.F. Dot Kiraz. Dot layer's. Dot com, 2d. The name of the convolutional layer intenser flow is conv, the 2D part reflects our input, which is a 2D image, see pretty simple. Now we need to specify the parameters for this layer. First is the number of kernels or filters. As we've discussed previously, this is 50. Then we have the colonel size, which we agree to be five. Now we will state the activation function to be Relu. Finally, we need to specify the shape of our input data in our case, that would be a greyscale 28 by 28 image. So the input size would be 28, 28 one. That is the first layer with all its parameters. There are more settings you can tweak, such as the stride. However, their default values are sufficient for our purposes. If you wish to explore them, feel free to visit the official documentation. Now for the second layer, it is a max pool, it's Tenzer flow name is Max pooling to. So we have tfw dot kiraz. Dot layer's dot max pooling to. As a parameter, we have pool size equals two by two. This is the default value for this parameter, so we could have left it out for completeness, though, we will leave it. All right. Then in our model, we repeat those two layers, so let's just copy those lines and paste them here like that. There are a couple of parameters we need to change, though. First, we decided to use a three by three kernel for the second convolutional layer. So we need to change this five to a three. Second, the input shape parameter is needed only when the layer is the first one in the network, therefore we will remove it. The last two layers should be one, unpacking the output and one fully connected layer with 10 outputs, the tensor flow name of the first one is flatten, while the second one is dense. So we write T.F. dot kiraz dot layers dot flatten. We don't need to pass any parameters to this layer, that means we can continue with T.F. dot kiraz, dot layers, dot dense. As a parameter for the dense layer, we need to specify the number of outputs, in our case 10. That's it, we have outlined our model fantastic to check if the model has been initialized properly, we can take a look at its quick summary by writing model DOT summary. I will also add a parameter that defines the width of the printed summary to be 75 symbols. This is purely cosmetic with the purpose to fit the data in the line. OK, so running it produces this brief table view of all the layers, the dimensions of their output and their parameters, these last two columns are convenient as they save us the trouble of computing them manually, we can clearly see how the dimension of the input evolves throughout the network, something which we calculated by hand in a previous lecture. If you are wondering why the first dimension is none in all layers. That's because normally all data is batched. Thus we don't pass a single image of size twenty eight by twenty eight by one, but actually pass a batch of, let's say, a thousand images all at once. That is supposed to be the first dimension. However, the model does not know our batch size just yet as we haven't trained it. So it leaves this dimension as none. Another interesting thing to note is the number of parameters in our architecture. We see that this model has thirty six thousand parameters in total, all of which are trainable. But what are trainable and non trainable parameters? Well, trainable parameters are the weights of our network, the parameters that the model is trying to learn in our model. These correspond to the different numbers and the kernels and the final dense layer. All the parameters we've talked about so far are trainable. Sometimes, however, you may need to multiply your input by a constant number, say two point six, and you would want that to happen every time. In other words, this parameter should not be changed during the learning process. These kinds of parameters are non trainable. All right, last thing about parameters, we can see that the majority of this model's parameters come from the second convolutional layer. Why is that? Why hasn't the max pool before it reduced the dimensions of the inputs and thus reduce the parameters? Also, the first conveyer has a kernel size of five, while the second has a size of three. So why doesn't the first layer contain the most parameters? Well, the key here is to not overly concentrate on the spatial dimension and think a bit about the third dimension of the input tensor. At first we have a tensor with dimensions. Twenty eight by twenty eight by one. So each of the 50 kernels in the first layer is five by five by one. Remember, the last dimension of the kernel is the same as the input. However, the input to the second convolutional layer is 12 by 12 by 50. That means that the 50 kernels in this layer have to match the last dimension of 50, thus obtaining the shape of three by three by 50. So a five by five by one kernel has twenty five parameters, while a three by three by 50 has four hundred and fifty parameters. That's why the second layer contributes the most to the total parameters. The increase in depth of the input far more outweighs the decrease in the spatial dimensions. Great. Now that this is clear, let's continue forward. Some of you may have noticed that I did not specify any activation function for the last dense layer. Weren't we supposed to have a softmax activation there in order to convert the output to a probability? That is true. There is one caveat in this case, though. The thing is that including the softmax directly into the layer is discouraged as it's impossible to provide an exact and numerically stable lost calculation for all models when using a softmax output. In other words, it might be OK for most of your work with CNN, but sometimes the calculations may get messed up because of this. So what do we do in this case? Well, we can actually incorporate the softmax into the lost function itself. All right. Speaking of the lost function, intenser flow, there are already existing classes for the different loss functions that we can use to define an object variable. Recall that for classification problems, we need the Spar's categorical cross entropy loss function. This is as simple as writing loss. FN equals T.F. Dot Kiraz dot losses. Dot Spar's categorical cross entropy. This would be the lost function if we had a softmax activation. However, since we didn't add it, we need to set the parameter from Logits to true. This tells Tenzer Flow to incorporate the softmax into the lost function. OK, now we need to compile the model. This will combine the model with the loss, function and optimizer, and it will prepare our network for training. Again, we do this in one line model dot compile Brackett's optimizer equals atom loss equals loss. Field and matrix equals the list containing accuracy. By running the cell, we have prepared our network for training so far so good before proceeding with training, we have to think about Overfitting. We know very well that our main tool against Overfitting will be early stopping or stopping the training process when the validation loss increases, intenser flow early stopping is a callback. Callbacks are functions that are called at the end of each episode the appropriate code to define an early stopping mechanism. Intenser flow is early stopping. Equals T.F. Dot Queyras dot callbacks. Dot early stopping brackets in the brackets we have the parameters monitor equals validation laws, which indicates what property to monitor while training. Then we have min delta equals zero patients equals to verbose equals zero and mode equals auto. These parameters define that the program should stop the training process when the validation loss starts to increase for two subsequent epics. Of course, you are encouraged to play around with those parameters to better understand their behavior. Great. Now for the last thing we need to do, train our newly created network. To do this, we will use the fit method of our variable model. The parameters of this method are the important bit here, the first one is our training data sets, which we have in the trained data variable. Then we need to specify the number of Epic's here, we will use the third variable we defined at the start num Epic's, which we said to 20. Now, to implement early stopping, we need to pass it to the callbacks parameter. There are many other callbacks other than early stopping, and we may wish to implement more than one. That's why we specify the callbacks and a list. We just passed the callback variable like that continuing. We add the validation data. Finally, we will set verbose to to this specifies what to print out during the training process to means that it will print info only at the end of each epic verbose equals one, on the other hand, will display progress bars for every batch. That's it. This is all we need to right now. When we run this cell, the training process will begin. Bear in mind that depending on the machine you are running, this can take more than five minutes. Right? Notice how coding the actual network part is easier and quicker to write than the preprocessing if you know what you were doing. That is often the case, as you may have a clear idea on how to build your network. But the data set may give you a headache because of missing values on standardized variables and many other issues. OK, it looks like our network has finished training, however, it finished on epic nd way earlier than the twenty epics we set for the training. That is because of the early stopping we implemented. It identified that the validation loss started increasing here and after two Epic's terminated the learning process. Since we set the Restor best weights parameter to true in the callback, our model is now using the weights from this epic here with the minimum validation loss great from the printout, we can see that the accuracy of the model is around 99 percent. But is that the true accuracy? Obviously not. This is just the training or validation accuracy. However, that is not the final accuracy of our model. To get the actual accuracy, we have to evaluate the network on the test data set. We will do precisely that in the next lecture. Thanks for watching.

25.   

Welcome back. Last time we train a network to recognize the minus digits, it is time to test its capabilities. Now, we can do this with the evaluate method, which returns the loss and accuracy on the provided data set as we are aiming to test the network. It is only natural to use the test data set. We will also save the output to variables for future use. OK. Running the sell has forward propagated the test set, let's print those numbers in a pleasant format with enough decimal places like this. All right. Now, this here is the proper accuracy of our model. Notice it is a bit lower than the validation accuracy as it is supposed to be. Bear in mind, though, that the test accuracy is evaluated only at the end of all work related to the model just before exporting it. If we wanted to tune the different hyper parameters, we need to use the validation accuracy. That is because in the process of finding the best type of parameters, we run the model. Each time we make a change, then we check the validation. Accuracy has improved. After 10 to 20 different combinations, we may reach a set up of hyper parameters with outstanding validation accuracy. But these are not the best type of parameters. These are just the hyper parameters that fit the validation data set best. Essentially, we are overfitting the hyper parameters. That's why we need the test. It is a data set the model has truly never seen before. Fantastic. We have created our very own convolutional neural network, exciting stuff. We've managed to get 99 percent accuracy on the Éminence data set with it, and that was with a very simple model for the CNN with only two convolutional layers. This accuracy, though, is much higher than what we achieved with a simple feed for deep network. In the deep learning course, we managed to obtain around 97 percent, and those deep networks tend to perform better on the small scale. Sanitized images of the Éminence data set those twenty eight by twenty eight images are too small to extract the full potential of the CNN Nete. Having done all of the training and testing, you must be eager to see the network's capabilities in action. Well then let's feed one image to the model and see what the responses. This is the code that we will be using to achieve that, we will not go over it, but we have prepared detailed comments on it and you can inspect it on your own to see how it works. OK, now let's check with the first image. Looks like so we enter one for the index variable eye and run the cell. Well, that's the first image in the test set. It contains a handwritten too. We can see the corresponding label below it. The question is now, what does the model output when presented with this image? Well, let's run the next cell to find out. And this bar chart pops on the screen. It plots the probability of each digit to be present in the image as calculated by our model. As we can see for this particular image, the network correctly predicted the digit is a two with almost 100 percent confidence. Let's check some more images, take the 10th image, for example. And again, the model predicts a one with 99 plus percent confidence. Now for the 26th image, a three running the next cell, and surely the network predicts correctly. Continuing with 34, this time, there is a seven on the image slightly tilted to the left, let's obtain the model's predictions. Well, well, well, it seems like the network was confused, it predicted that there's 40 percent chance that this image represents a three. Thirty five percent for a two, around 17 percent chance for a one and only six percent for the true label of seven. So the network was fooled by the fact that the digit was skewed a bit to one side. OK, let's look at one more image, say, the five hundred and eighty ninth one. All right. So what we have illustrated here is a nine. But wait, the label below says eight. Apparently this image has been labeled as an eight in the data set, but it definitely looks like a nine to me. Well, that managed to fool even me, so I don't expect the model to be able to handle it. Let's see. Yeah. As we can see from the bar chart, the model is 96 percent sure that this was a nine. Interestingly, the second highest probability is eight with two percent. All right, we can continue looking at the results from each of the 10000 thousand images we have in the test set, you can explore that on your own. The code is included in the resources with this lecture. Great. Let's recap what we've done in this section. First, we got familiar with the data set, and based on that, we figured a simple CNN architecture to use. That was before starting to write the code. Then we spent some time pre processing the data sets so we could make the most use of it. Finally, given we knew what our model is, we quickly coated it with tensor flow to trained it and tested it. We have done a lot in this section, but there is still a lot to learn. For example, how to visualize parts of the learning process. To better understand our network, tune in the next section where we will introduce Tenzer board and answer this question. Thanks for watching.

26.   

Hello there and welcome back. In this section, we will discuss the basic concepts of Tenzer board and how to use it Tenzer board as an extension to Tenzer Flow that provides tools for logging useful data and visualization of numerous metrics connected with the neural network. This helps you understand how your networks behave and find the optimal hyper parameters to use. It is a nifty tool that every machine learning enthusiast at least needs to be aware of. However, Tenzer board has many applications, some of which are more specific. So in this section we will show you the basic stuff you can do with Tensas board without going into too much detail. First, we will long the training process and see how to visualize general characteristics. Then in the next lecture we will explain the concept of a confusion matrix and only after that we are going to perform hyper parameter tuning using tensor board. OK, let's get started. You can find the official resources concerning Tenzer board on its dedicated Web page here at Tenzer Flow dot org slash Tenzer board in the guide section. You can find all you need about this tool. It has many articles that outline different use cases. Since we will only show you the basics, feel free to explore any additional topics here. Now for this lecture, we will use our eminent example program from the last section. It is a good practice example as it is a relatively simple CNN and we already understand what it does and how it works. Luckily, Tenzer board is easy to implement into already existing code, we just need a couple of additional lines. In general, it works by exporting the quantities we are interested in to a logging file. The latter is updated throughout the learning process. All right, here is our code from the previous section. Our task is to modify it so we can take advantage of the basic logging functionality of 10 support. So let's get down to business first. We need to import an additional module date time. This will help us with the logging we mentioned before. Let's start from the top and import the date time package to our project. Next up is the pre processing of the data. Well, this step has nothing to do with Tenzer board. So this whole section remains as is. We will just run the cells quickly here. Now we arrive at the CNN parts since we don't want to change the network itself, the model and lost function remain the same. Again, we compiled and defined the early stopping by running these cells. Now we need to add a couple of lines of code. First, we will define the relevant directory where we'll log our data in this notebook. We will probably train our model more than once with different type of parameters. So creating different folders for the different runs would be a good idea to easily distinguish between them. We will incorporate the local date and time into the name. That's why we imported the date time module. This is a very neat way to have a unique folder for each iteration and even have them in chronological order. OK, so the variable where we store the location will be called log's der, it is equal to log's double slash fit, double slash, then we will append the current date time. So plus date time, dot, date time dot. Now, finally, let's also add some convention about the format we will display. I like year, month, day dash our minute seconds. So Dot Streiff time brackets and your preferred format make sure to include all this information no matter in what order to make sure your folders are truly unique. Now this line simply defines the path to store the long data and as a string, some folders are defined with a double slash to avoid confusion with the python escape symbol. You can change this directory to whatever you like, but adding the timestamp is strongly encouraged. So far, this has nothing to do with Tenzer board at all to be able to use it. First, we need to create a callback for it. We already implemented one callback the early stopping. In case you need a hint, a callback is a function or an operation that is executed at the end of each episode. Now, similarly to the early stopping, we will add Tensas board as a callback. We define the Tenzer board callback in just one line. Tenzer board callback equals T.F. Dot Queyras dot callback. DOT Tensas board. Brackets in the brackets we define log der equals log der and histogram frequency equals one. That's it, running this cell creates the tensor board carmac, but does not incorporate it into the training process. Recall that this is just a variable we need to pass to the callback parameter in the fit method to incorporate it into our model. We add this callback as another item in the callback list like that. A very important thing to note here is that the early stopping callback should always be the last element of the list. Otherwise, it bugs out and does not execute. Great, let's outline the changes we've made so far, we added the module date time at the beginning, then we define the directory where we save the log files and created the tensor board callback here. Finally, in the fit method, we added this callback, that's all. And now when we run all the cells, our network will be trained just as before, but the training process will be recorded. Let's speed the training a bit. OK, now we have trained our network and recorded the training itself, so what next? Well, if you have ever seen Tenzer board before, you probably imagine that we will start making some neat visualizations. We're almost there. So how do we access those visuals? Well, we need to start the actual tensor board application. We do this with the following two commands percent load tensor board. And present tense board. Dash, dash logged deer logs slash fit. The first command closed the extension itself and the second one starts it and states the directorate where we look for the locks, so we run this cell and here it is. That is the Tenzer broad extension. A word of warning, though, on Windows 10. Support has trouble starting if the extension has been running. So the first time you start it, it will run properly. But if you subsequently try to restart it or open a different directory, the extension will encounter an error. That happens because Windows cannot clean up the temporary data used by Tenzer board after you finish with it. Luckily, there is a quick workaround. All you need to do is write two commands in the show. First, open the command prompt. In there you need to paste the following two lines, one after another. Task kill. I am Tenzer Board, exi SGF and DTL sgu precent Tempy percent backward slash dot Tenzer board info backward slash star. These will end already active Tenzer board processes and clean the temporary data associated with Tenzer board so you can run it again if either of those gives an error. That's OK. You can ignore it. Once you have entered these commands in the command prompt, you can rerun the cell. Usually the first attempt to run the cell will result in a timeout. Then if you run it again, it will start up correctly. For more details on this issue, you can check this thread on GitHub Tenzer board in Jupiter Localhost refused to connect issue on Windows. The Tuckerman's are here. I suggest you bookmark this page or copy the two statements on some local file because you will need them every time you want to start another 10 support instance. All right, back to the code, let's take a look at the interface of the extension down here to the left, you can see the different runs of training we did with our network. We have trained it only once, but there are two runs displayed. That's because one is for the training sets and the other is for the validation set. When having multiple runs, you can choose which ones to display the data for by ticking or until the checkbox is next to them. OK. You can see up here that right now we have opened the scalars tab in this tab, different metrics such as the loss and accuracy are displayed. You can even log your custom scalars to be displayed here. Let's take a look at how the accuracy evolves with the Epic's by default. These graphs are smoothed a bit as to look more continuous. This may be useful when we have train the model for a big number of Epic's, but that is not the case here. So we will remove the smoothing with this slider to the left. We will set it to zero. On the graph we can see two lines. One is the training set accuracy. The other is the validation. One, we can see how the accuracy cross both sets increases throughout the training process. Notice that by hovering with the mouse above the graph, it displays the exact numbers. Below is a similar graph showing the loss of our model instead of the accuracy for our simple network. These may not be that useful, but for real, more complicated models. You may gain insights into their performance from these graphs. Good. That was the scalars tab. Moving on, we have the graphs tab. Let's click it and see what we get. Hmm. We can see all the layers of our model. That's because here is visualize the computational graph of our network. This is quite similar to the graphic we had when describing our model qualitatively. This graph shows all the layers and computations that are made in the network and how they are all connected. You can zoom in to see specific parts of the graph on the connecting arrows. You can also see the dimensions of the inputs and outputs, which can be very useful, especially with big models. When you click on a layer, a window with additional information pops on the right, you can also double click a layer to expand it. When you expand it, it shows all the internal operations carried out, such as the addition of the bias, the activation function and others. Ten support also provides functionality to name the different layers with custom names. So this graph view is very flexible. All right, this sounds great. There are other tabs, such as the distribution's one and the histograms in those you can see and track the evolution of the various Tensas as the network is learning. As you see, Tenzer board can be useful in many ways besides the things we've shown so far. You can also plot images or other data, including audio. You can track many complex metrics during the training, such as the confusion matrix, which is exactly the focus of our next video. See you there. And thanks for watching.

27.  

So far, when evaluating a model, we have only considered the accuracy and loss metrics today. We are going to introduce another useful metric related to classification problems called confusion matrix and see how we can implement it. With the help of 10 support. The confusion matrix, sometimes called error matrix, is a table that shows which categories our network excels at predicting and which ones it struggles to deal with. While not too complex as an idea, a confusion matrix completely takes the place of the accuracy metric. The reason for this is that a single accuracy number cannot show us the actual situation. For instance, say we have 80 percent accuracy on classifying images of dogs and cats. Sounds good, but what if I told you that it gets all dog photos, right? And only half of the cat photos. Right. Although the accuracy itself is not bad. This sounds like a lousy model. Now, the general question that we want to ask at the end of the training is where does the network struggle the most? Even if we've got ninety nine point nine percent accuracy, we will always want to ask this question, are the cats images problematic or the dog ones? Maybe they are equally hard for the model to distinguish. And this is why the confusion matrix is so important. They can show us what the model does well and what it finds challenging, knowing the problem will make improving the model much easier. All right. Now that you're convinced we should use a confusing matrix, let's see what it looks like. A confusion matrix is a table where on the Y axis we have the true labels of our data, for instance, cat, dog and Wolf on the x axis are the predicted labels. That is the ones our network assigned to the images, the cell show, the number of cases that conform to both axis. They are usually color coded, often with blue. For example, suppose we test a network with 20 images of cats, 20 of dogs and 20 of wolfs. When presented with all the images of cats, our model predicted that 17 of them are in fact an image of a cat. Two were a dog and one was misclassified as a wolf for the dogs. The network assumed two were cats, 13 were dogs and five were wolves. And finally, for the wolves, it correctly asserted that zero our cats. However, it misidentified two to be dogs and the rest 18 were correctly classified as wolves. Notice how the cells are filled with different intensities of blue. The higher the intensity, the higher the number in the cells. This makes it easy to read the Matrix at a glance, even when it becomes more cluttered with a higher number of classes. Another often applied modification to this table is its normalization. Instead of showcasing the exact numbers of classified images, it is often easier to work with percentages. So instead of having this five here, we ask How much is that out of the twenty dog photos? The answer is twenty five percent. So the cell shows zero point twenty five. That is done for the whole confusion matrix. In fact, most of the confusion matrices you will encounter will be normalized. However, it is important to remember that the normalization is done for rows, not columns. Thus, the sum of every row will always add up to 100 percent. But the sum of a column could potentially be three hundred percent for a three category case. Great, now reading this table is not that difficult in general, the assumption is that the majority of cases are classified correctly, otherwise the accuracy would be too low for the confusion matrix to be useful. These correct labels lie all on the diagonal, so only the deviations from this diagonal are important. The color here helps a lot, as most other cells should be around zero and therefore white. Those cells that have significant MIS classifications will stick out due to the coloring. For the example confusion matrix. We can agree that the network does not struggle much with images of cats and wolves. However, it often thinks that the image of a dog is a wolf. Given this information, we may begin to create assumptions about the data set. Maybe the wolves are very similar to each other. They might all have the same fur color and pattern. Maybe the data set has them all in the same pose due to the prevalence of that iconography in pop culture. Thus, the network has a much easier time learning what a picture of a wolf is. In contrast, the dog class may consist of wildly different breeds, some small, others larger. Therefore, it could have had a harder time recognizing the different breeds. So it learned that Chihuahua is a dog. But the Siberian husky is so close in appearance to the wolves that it can't cope with the fact that it is of the same category with a bulldog. Those connections are just assumptions, not facts. However, with the help of the confusion matrix, we may construct a testable hypothesis such as this one that can later, with more research, be verified or rejected. Good. Now that we've learned what a confusion matrix is, let's see how to implement it in the code. We will not delve into every single line of code, but just the general idea. This implementation has been taken from the Tenzer board guide on image data as it is clean and structured code and there is no need in reinventing the wheel. There are three main points and logging and displaying the confusion matrix that we will define in functions, one of them is to plot the Matrix visually in math plot Leyb, given the already computed values. Another is to convert the plot to an actual image. And the last one is about computing the numbers and logging the info to Tenzer board. All right, specifically for the confusion matrix, but we need these additional libraries and packages I o ed tools, Escalon, Lern, dot matrix and math plot, Leyb dot plot. Now let's inspect the code for the three functions. First is the plot confusion matrix, which has a very descriptive name. It creates the math plot lib plot. Given the numerical data, it takes the numerical values system and the class names as input. The first line creates an empty figure of the given size and inches. Here you can see the color map that is chosen is blue. Here is the title and the next three lines arranged the X and Y labels given the class names. This line of code here normalizes the matrix, that is, it transforms the numbers into percentages, the next block ensures that the next color would be visible regardless of the background shade of blue. For white background, it uses black text. And for a very blue background, it goes with white text. Finally, it states the axis labels and returns the figure. Next up, we have the plot to image which converts the return figure to PNG. Nothing much to talk about here. So let's continue with the next function. Finally, we have the log confusion matrix function. It calculates all the numbers and logs, the result from the previous two functions to Tenzer board. I have to point out, though, that we need to define this file writer variable outside the function. The parameter inside the brackets is the directory where we want to log the results. I will denote it with s.m standing for confusion matrix. So to calculate the Matrix, we first need to get all the predicted labels from the model which is done to the first two lines, then the Matrix itself is calculated by the Escalon package. After that, it creates the confusion matrix image using the previous two functions and finally logs the results for every episode. Let me just point out that these variables here are global. Those can be substituted with the names you gave them. Another point is that it is here where you define the class names. Be careful with that as the name should be in the correct order and it's easy to forget to change them when you copy your code. Great. At the very least, we need to define the callback that would go into the fit method. It basically says that at the end of each epic, python needs to run this function. That's it. Fantastic. Now let's see it implemented in the amnesty code. Here is the unmodified code from the previous section. The first thing to change is the packages to be imported. As we've discussed for the confusion matrix, we need i o ed tools, num pi, escalon, dot matrix and math plot Leyb. OK, next, we have the preprocessing, but it remains largely the same, except for a single addition at the end of all that stuff, we need to add this for loop. Its purpose is to extract the images and their labels from the validation data as an umpire. We need to do this since the variable validation data is that the custom data set type, if for a different project you are working with data that is in the form of arrays already. You can skip that part. All right. Then we have the part of the code dealing with a network. This code is actually going to remain the same. We just need to add the additional functions. A logical place to insert them is after the model dot compile line. First, we define the logging directory, it will again be logs fit. However, in contrast to the previous lecture, we will not use the current date and time. We will just enumerate the different runs. So this time it will be run one. Then we can copy and paste the functions we talked about. We have plot confusion, matrix, followed by the plot to image function and finally the log confusion matrix function. In this last function, we may need to change some variable names first. This model here refers to the variable of the model we are currently training. Fortunately, we have named our variable model as well. So that's fine. These two variables here refer to the images and labels that should serve as a basis for our confusion matrix. In our case, we are going to determine the matrix based on the validation set, so we must change them to images Val and labels Val respectively. That's why we added that cell at the preprocessing part. Finally, we need to specify the class names since we are classifying digits. These are simply the digits zero to nine. We have the three functions done, all we have left to do is define the callbacks. As we've discussed, we have this s.m callback to deal with the confusion matrix. We will also define the standard Tenzer board login callback, OK, adding those callbacks to the fit method. And we are done now. We have a functioning program that regularly logs the confusion matrix. Let's train the network and check the results. And we are back after the model has been trained. Let's get straight to the most exciting part, visualizing the confusion matrix. Now we start Tenzer board with a standard commands. However, we get an error. That is the problem of Tenzer board on Windows we mentioned earlier. So let's follow the fix we provided here. I am opening the command prompt in it. I will paste the two commands outline in the workaround. Here is the first and now the second. That's it. Now, going back to Jupiter, we can run the Tenzer board, sell it now starts to process stuff and after a while it is going to time out just like that. If I run it a second time, though, it starts just fine. Here it is the familiar screen with loss and accuracy graphs. If we take a look to the left, we see three logs run one train, run one validation and run one system. What we are looking for is the s.m one. It stands for confusion matrix. But why doesn't it show up then? We have selected the Cameroun, haven't we? Well, the confusion matrix is an image. Therefore we can find it in the image tab. There it is. However, it is rather small. We can't actually see the numbers. In fact, the confusion matrix always shows up, scaled down like this. We can scale it up by just left clicking it like this. Alternatively, we can open it in a new tab to see it in its original size, great. What info can we extract from it now? Well, in the amnesty example, we can't really see anything useful here since our model has huge accuracy. The confusion matrix reduces to just the diagonal later in the course when we work with our own data. The confusion matrix will come in quite handy. That doesn't mean you shouldn't experiment with it. In fact, for homework, I suggest you tune down the Amnesty Network on purpose so that it has lower accuracy and then check the confusion matrix. You can choose inadequate hyper parameters or just change the network architecture. The idea is to then examine the confusion, matrix and study which digits our models struggles with. All right, that's enough on this topic. Next time we will see how to tune the parameters of a network with the help of Tenzer board. Thanks for watching.

28.  

In our last lecture, we introduced the confusion matrix and implemented it with 10 support, this time we will do something a bit more advanced. Namely, we will try to tune some of the hyper parameters of our network using Tensor board. Good, the integration of Tenzer board in this case is a bit trickier, however, we will not focus on the code too much because our purpose is to show you what's possible and give you a general idea of how you can achieve it. You can always dive deeper into the code on your own if you need to. OK, so again, we start with our Imust code. This time we won't be logging our runs with the timestamp, so we don't need to import date time. Nevertheless, we need to import a hyper parameter plugin from Tenzer board dot plugins. Dot h prem's import API. Here, HP stands for hyper parameters, all right, again, the preprocessing section does not need to be changed, so we leave it as is. Now we need to define which type of parameters we will be testing. For this example, we will tune to hyper parameters. The first one is the kernel size in our convolutional layers, while the second one is the optimizer we train the network with. So starting with the filter size, we will define a variable called HP filter size, it is equal to HP dot h param. As parameters, we need to specify its name, which is filter size and the range in which we will test it, in our case, we will test it for a filter with size three by three, five by five and seven by seven. So we write HP dot discrete three five seven. Next we have the optimizer. HP dot h param. Brackets, Optimizer, HP, dot discrete. Adam and SGD. In this test, we will try out these two optimizers, recall that SGD stands for stochastic gradient descent and it is one of the simplest optimizers available, whereas Adam is a much more sophisticated one. So we expect that Adam would be way better than SGD in practice. This may not be a very useful test to make. However, for educational purposes we will compare the two. So far, we have defined what type of parameters and values to test. Now we need to set the metric that will we be interested in when comparing them. This would be the test accuracy. Thus we define metric accuracy equal to accuracy. Now we need to log some setup info, here is the code for that briefly, this relates to a file located in the H. Perram tuning subfolder of the logs folder. In this file, we state the hyper parameter variables and the accuracy metric. So far, so good. Moving forward, we now need to train the network multiple times with every different type of parameter value and test its accuracy. For this purpose, we will create a function that trains and tests our model. This is pretty much copy pasta of our existing code, just wrapping it into a function. However, there is a caveat. We will not hard code the hyper parameters, but instead we will pass them to the function. So let's get going. I will name the function in question train test model. The parameter passed, the function will be called H Prem's, it will be a dictionary containing the different values. Let's paste the stuff we will need from our previous code inside the function. First is the model of our network copy and paste. Then we need the loss function and the model dots compile line. Continuing, we have the early stopping. Finally, we have the training of the model with model dot fit and the evaluation of its accuracy. Now, let's make the necessary changes first, we will replace the kernel size inside the convolutional layers with each Prem's square brackets, HP filter size noticed that I do that for both convolutional layers. Thus, in this test, both will have the same kernel size three, five or seven. The other parameter we need to change is the optimizer, which I will set to H Prem's square brackets HP Optimizer. Finally, we need to return the test accuracy of this model. Great hope everything so far was clear. We will now pick up the pace of it and explain the rest of the code briefly. Here we have a function that would initiate the training process and log all the data. It takes two parameters the directory where we will log the results and the hyper parameter values here. It opens a file to write in this line writes the current values of the hyper parameters being tested. Then it invokes the training function with the appropriate hyper parameters, which returns the accuracy for this combination. Finally, we log this accuracy as well. All right, the final piece of the puzzle is to have a double for loop to loop over every combination of hyper parameters and invoke the function run for each one. So here is that piece of code. Here are the two for loops iterating over the respective hyper parameters. Then we define a dictionary with the current values and a name for the logging directory. These two print statements print out useful info to the user. Finally, we call the function run to train the network and log the results. There you go. Now our code is complete. But before we run it, there is one last thing to mention time when having multiple training runs of our model, the time it takes to complete may increase exponentially. For example, in our case we have three different values for the filter size and two values for the optimizer. This means that to exhaust all possible combinations, we will need to train our models six times. If, let's say, a single model is trained for five minutes, that means this program will need to run for at least 30 minutes. You need to take this into account every time you said something like this up. OK, we will now run this cell, which will train our network with all six combinations of hyper parameters and jump straight to when the training is over. Great. Now that all the computations are over, it's time to see the results. So let's start Tenzer board. We do that with the usual two commands, percent load extension, Tenzer board and percent Tenzer board. Dash dash log, Dyax here. You need to be careful to type the correct directory for the logs first. Don't forget, we have to enter the commands in the command, prompt the task, kill and delete one. Now we can safely start Tenzer board. So here is the visual interface. This time, the scalars tab is not of much use to us. What we want is the H programs tab standing for hyper parameters to the left. You can confirm which values of the hyper parameters to show. Now this table view shows how each combination has performed. You can sort it with this section here to the left. Let's sorted by accuracy in descending order that is from best to worst. It is immediately apparent that there are two clusters, one with the optimizer and one with Adam, where the runs with Adam perform much better, just as expected. Let's now switch from table view to parallel coordinates view, we can also expand the graph a bit, so it is clearly visible. This may seem a bit confusing at first, but it is actually pretty simple. On the left, we have the filter size, while on the right there are the optimizers. In the middle is the accuracy. Each line goes through exactly one filter size and exactly one optimizer, so it represents one run of the network with the particular parameters. The place where it intersects the middle line is the accuracy of this configuration. The graph demonstrates that all lines from the optimizer go to the bottom of the accuracy while the ones from the atom cluster at the top. So it is obvious that Adam performs much better. On the other hand, the different kernel sizes coupled with Adam performs somewhat similarly. Thus our size argument in the beginning is relevant. This goes to show that with complex things, as CNN's often it is important to test out many different configurations. Finally, there is the scatterplot matrix view, it consists of a series of graphs that plot accuracy against the filter size or against the optimizer with every run represented as a point. All right, so this was a somewhat brief introduction to tech support in this section, we covered the general idea of this extension and some of its more common uses. Remember, though, this is just the tip of the iceberg. Feel free to take a look at the documentation and official guides and to experiment with it on your own networks. So far, we have shown you how to use CNN to solve one of the most basic problems in computer vision, the finest example. Now I feel we are closing on a more serious, practical project. That's why in the next section we will introduce a couple of techniques for improving the performance of neural networks in general. See you there. And thanks for watching.

29.  

So far, we have considered what convolutional layers are, how they work and how they are incorporated into the structure of a neural network to obtain a convolutional neural network. Up to that point, it was all theoretical. Then in order to show it in practice, we implemented a somewhat simple CNN on the minus data set. Given that the Emmis data set is a very clean and simple one, it seemed like a good starting point to show how the theoretical stuff are coded in practice. However, the advantages that make the remnants a good foundation turn into disadvantages when it comes to learning how to construct models in practice and what to expect from them. You see, the Emmis data set is way too clean. First, the digits are always drawn in the center of the frame. Second, all images are black and white. All of these make it extremely easy for most networks to deal with the amnesty. Even a simple linear classifier can achieve accuracy of more than ninety two point four percent. Thus, this was not a challenge worthy of the usage of complex neural nets. Nonetheless, we still achieved our goal to learn the appropriate syntax for implementing science. But now there is another task on our list. It's time to learn how to deal with problems that arise from real world datasets, see what realistic results look like, and understand what the network actually does. To tackle this, we have prepared a very real data set with unique goals and challenges, but we'll have to wait until next section to work on it. Why in the next section? Why not start with it immediately? Well, the data said we will consider is rather complicated. So first we will try to optimize our seasons to better suit the problem. That said, there are a number of techniques that can help us optimize a model. And in this section we will explore three of the most common ones that improve the performance and predictability of neural networks, no matter if CNN or not. These are wait Dekay or Regularisation Dropout and Data Augmentation. If you are already familiar with these and know the specifics, just skip this section and jump straight to the practical example. All right. With this out of the way, let's start off with the first technique, regularization. Thanks for watching.

30.  

In this lecture, we will talk about regularisation, we will discuss its purpose and how it works. OK. At this point, you probably know it by heart, but we still need to remind you, if there was one thing that jeopardizes a perfect neural network, that would be overfitting once more. Overfitting refers to situations where the model has fit the training data so well that the model captures the noise and random fluctuations. Now, we have already discussed how to use the validation, said an early stopping in order to prevent OVERFITTING from happening. Unfortunately, while necessary, this approach is not 100 percent reliable. There may be certain situations where the validation loss stays the same or increases marginally, but the model is still in the process of obtaining valuable insights from the training set. Therefore, we don't want the early stopping to kick in at this moment. In theory, that can be countered by fiddling with the patient's parameter of the early stopping callback. Unfortunately, we are not always aware what the value should be or whether a particular value is any good. Overall, it is a good idea to have fundamentally different tools that combat overfitting, logically, different tools are useful in different situations and nothing stops us from employing them all. Good now. In what other way can we prevent Overfitting? Actually, there's a very clever answer to this question. Since Overfitting happens, when the model learns too much, we can combat this by interfering with the model's ability to learn. In other words, we decrease the model's capacity. At first, this seems like a counterproductive measure. After all, the whole purpose of machine learning is to make the machine learn. But of course, the devil is in the detail, and the extent to which we prevent the model from learning is the important part. All right, so how do we decrease the capacity of the model to learn there are various ways to achieve? That's probably the simplest one, is to make the model itself simpler. In fact, if you are employing a linear regression, no one will ever mention overfitting to you as the linear regression is very simple by definition. Now, downscaling the complexity to a linear regression is a bit drastic, isn't it? There must be some middle ground, right? Yes, and that's what regularization is all about. In a nutshell, regularization is a method used to reduce the model's capacity to learn more precisely, it restricts the capacity of complex models and encourages simpler representations of the solution. So far, so good. Let's get into the nitty gritty details. Technically speaking, regularization consists of including additional factors to the loss function to change the behavior of the training process. So the new loss is equal to the old loss, plus the regularization factor. And since the back propagation is solely based on the loss, changing the loss affects the optimization of weights. What does this mean for an actual neural network? Well, we can incentivize or penalize behavior. For instance, we can increase the loss when some unfavorable event occurs. Subsequently, the model will adjust itself to not trigger that event as much. Inversely, we can decrease the loss in a situation where we want to encourage the model to follow the desired pattern. Great, going back to our goal. We want to inhibit the capacity of the network, so how can we represent the complexity of a network with numbers that can be added to the loss function? Well, intuitively, if a model has learned a very simple representation of the solution, we would expect a significant portion of the parameters to be zero or close to zero. We can convince ourselves of the validity of this statement by looking at what actually happens if a weight is set to zero. Nullifying this parameter detaches the neuron from the network as if it doesn't exist. Do that for a large number of neurons and the graph of this network starts to look a lot more simplistic. Conversely, if the model is complex, most, if not all of the weights would be non-zero numbers. Therefore, we can conclude that the complexity or capacity of our model can be described by the values of the weights. The exact combination of these values determines the type of regularization. OK, now that we know what regularization is all about, let's get more concrete. In the next lecture, we will discuss L2 regularisation and its similarities and differences with weight decay. Thanks for watching.


31.  

Continuing from where we left off in this lecture, we will talk about the two to Regularisation Technik, discuss how it connects to wait Dekay. OK, last time we came to the conclusion that a regularisation technique can decrease the capacity of a model by adding some combination of the weights to the loss function, L2 regularisation is a specific type of regularization where the specific function we add is the L2 mathematical norm. L2 norm is the sum of all weights squared modulated by a parameter lambda. The sum represents how complex the model is, while the factor in front lambda is there to tune the effects. Lamda of zero implies the complete disregard the L to regularisation, while a big value may mean that the regularisation factor far outweighs the actual loss and thus renders the training useless. This makes LAMDA another hyper parameter to consider. Typical values for LAMDA may vary widely, but it can be as little as 10 to the minus five and usually doesn't go above zero point one. Great, the interaction between the normal on regularise lost and the additional factor has interesting effects on the optimization of the weights. What actually happens is that the weights that are not absolutely necessary for the model are scaled down as close to zero as possible. On the other hand, the ones that are essential for the performance of the model are barely changed. Thus, the purpose of L2 regularisation is to simplify the model by scaling down all non-essential weights. All right, so how does the update rule for the weights change? Because of the additional factor? Here's the original update rule when no factors are present in the loss. This signifies how the new weight should be obtained from the old one and the derivative. With respect to the loss here, BTA is the learning rate. What about the L2 regularisation case? Well, if you do the math, the update rule evolves into this one. We should know that the loss element here refers to the UN regularised loss. As you can see, the only difference is this extra factor of Lambda Times Hetta. So effectively, L2 regularization is decreasing the contribution from the old weights. That is why L2 regularization is sometimes called weight Waitiki. You will often hear the two terms used interchangeably, however, and I want to stress this weight. Dekay is actually a different thing. L2 regularization is not the same as Waitaki. To see this, here is the update rule for the actual weight k. When comparing the two equations, we can see that the weight to K one is missing in here, but what's the big deal? Lambda Lambda Times, etc.. It's all just some constant isn't it. We can just substitute Lambda Times Hetta with a new constant lambda prime and we've got the same equation right. Well if the learning rate is constant then yes. In the case of the stochastic gradient descent optimizer weight to K and L to regularization are indeed equivalent. However, for optimizers that use adaptive learning rate, this construct is no longer a constant. This is equivalent to having a lambda that changes values during the training. That means that the regularization effect would be different in the beginning and at the end of the training. In contrast, weight to K has a constant effect at all times. While both techniques are equally effective, it is important to be aware of their differences. All right, let's summarize what we've learned so far. Overfitting comes in different shapes and forms. Therefore we need different techniques to combat it. One such technique is to decrease the model's complexity using regularization. L2 regularization is one of the most popular regularization does. It consists of adding the sum of squared model weights to the loss function due to the form of the equation for the new weight. L2 regularization is also known as weight decay. However, weight decay is a separate concept that has slightly different update rule. Despite this, the two techniques are equivalent for a model trained with SGD optimizer. We have seen the intricacies of regularisation, but in the beginning, we promised to talk about dropout and data augmentation as well, and we are going to keep this promise as the next lecture is all about dropout. See you there. And thanks for watching.

32.   

Last time we introduced two different techniques to combat Overfitting, L2, Regularisation and Waitiki, in this video, we will consider a fundamentally different method to achieve the same result. The dropout technique. OK. As the name suggests, Drop-Out is a technique that allows us to deliberately drop some of the neurons during the training. How do we do that? By setting its output to zero, thus effectively removing it from the network, we randomly choose which neurons to drop from all neurons in a given layer at the beginning of each episode. So, for example, if we have a dense layer with one hundred outputs, we may randomly set 10 of those to zero and continue to the next layer. As usual, each ethic, a different set of ten neurons are removed, which allows all neurons to learn. Remember that we only do this during training, when evaluating and actually using the model. All neurons are present. O kay, that is not too difficult to imagine, but it seems a bit jarring. We are a moving parts of our network when training it on purpose, even worse. This is not done consistently, but in a random fashion. Why would we ever want to do such a thing? Well, the idea behind this intriguing concept is to make the neurons a bit more independent from each other. By removing some of their colleagues. The remaining neurons need to cover for them. They are forced to learn patterns about the whole data, not just some specific features they specialize in. That is why Drop Out is almost exclusively applied to fully connected layers. Dropout usually has one hyper parameter associated with it the percentage of neurons to be randomly dropped. So a parameter of zero point one means that 10 percent of the outputs or inputs would be randomly dropped to zero. Typical values for this parameter may vary between zero point one and zero point five. Another advantage of drop out is that it's very easy to implement an existing code and architecture, as you will see later in the practical example. And remember this technique as well as the L2 regularisation and Waitiki is applicable to almost all types of architectures, not just for convolutional neural networks. Good. Now some technical details about drop out. When we set 10 percent of the neurons to zero, we are not only forcing the other neurons to learn more, we are also, on average, decreasing the results from the next layer by about 10 percent because they are linear combinations of the previous outputs. This is how we introduce a change in the whole chain of computations by decreasing the size of the outputs pretty much uniformly across the next layers. So to work properly, the dropout needs to scale the remaining outputs up a bit easier computations. Let's take a look at a situation where we remove half of the neurons. That is a drop out of zero point five. Then if we want the next layer to work properly, we need to double the remaining half outputs. The good news is tensor flow. Does this scaling automatically for us and we don't have to worry about it. All right. That is enough about dropouts. The rest will be practice. But before that, join us in the next video to learn about data augmentation. Thanks for watching.

33.  

So far, we introduced techniques focus on helping the network perform better by dealing with Overfitting. However, in this lecture, we will do something different for a change, discuss a technique that actually focuses on the dataset rather than the network. When we introduce convolutional networks, theoretically, we pointed out how a normal network with only dense layers may struggle to identify an already learned image if it is slightly moved to the side. Back then, we said that convolutional networks do not have this problem. While this is not wrong, it is only partially true. If there is a small deviation in the position of an object, the CNN will still detect it. Problem comes when the change is larger. To restate a CNN can only learn the patterns present in the training data. If we trained it on recognizing genes, it won't be able to recognize shoes. This might seem obvious, but our perception on what the training data is often differs from what it is in reality. For example, let's say we train a cat classifier with numerous images of cats in different poses. We might think that's enough for the network to learn to recognize cats. However, what we may not realize is that the cats and all photos face a bit to the right. Thus, the network learns to recognize only cats that face to the right, since it has never seen cats looking to the left. The model most certainly won't be able to label this image properly. This is where data augmentation kicks in. The idea behind data augmentation is to change or transform some of the training data points so that there is a greater diversity in the areas we're interested in. This technique is most useful for, but not limited to image data. Since that's more or less the topic of our course, we must added to our anend arsenal. OK, what are some data augmentation ideas? Well, a common transformation, maybe mirroring the image that would be the technique we can take advantage of in view of our previous cat example, if we take our initial data set and just mirror it, we get additional images of cats that face to the left. Thus the model will be less biased and less likely to be fooled. However, a number of other transformations are also viable, including translating the object of interest in different parts of the frame, rotating it, flipping it vertically, stretching and resizing it, changing the color, balance and contrast, and many others. The moto of machine learning is that everything is problem specific and this applies in full force here. You can even add additional noise or blurring effects to the pictures if you expect the real input data to be of poor quality. All these techniques depend on what you expect your model to be able to cope with and whether it is present in the training set. Great. It is important to realize that the purpose of data augmentation is not to increase the network's performance. In fact, it may actually decrease it. Since it generates a new class of examples the model has to recognize. Its purpose is to expand the models capabilities beyond with a default training set provides and data augmentation is often the easiest way to achieve that, as obtaining a new, richer data set is usually hard. All right, that concludes the section on some of the techniques for improvement of neural networks performance. Bear in mind that the field of machine learning is vast and there are many more ways to do that. Some more specialized than others, however, will examine these three common techniques in a practical setting in the next section. In the next section will also finally tackle a real world practical example, a business case that will give you a better idea of how things actually turn out. So without further ado, let's jump straight into the next section. Thanks for watching.

34.  

So far, we have implemented convolutional neural networks only in, let's say, laboratory environment, however, real world projects and situations are not as straightforward as the amnesty example. There are lots of obstacles along the way and the results are not always satisfactory or what we want them to be. That is why in this section we are going to tackle a real business case, although it isn't 100 percent complete with all the different edge cases that may need to be considered. It's still perfectly represents a project you'd have to tackle at some point in your career as a data scientist. OK, so here is the situation. Imagine you are a fashion designer. Your brand has so far created thousands of models of different shirts, shoes, trousers, accessories, et cetera. And now you've decided it's time to open an online version of your shop. So you need to take photos of all the products, upload them to your online platform and label all the goods. Here lies the problem. Let's say you have the names of your products and maybe even the photos ready and available. But do you have all these products labeled with the specifics your online platform demands? Probably not. Moreover, going forward, you definitely intend to add more and more items to your shop and to go through thousands of photos and manually label them would take too much time and effort. It would be nice that this can be streamlined and automated, wouldn't it? I think you know where I'm going with this, but that's not all. Here is another situation. Picture this. You have a website where people can upload and sell their own clothes. Thus it is not your own products that are up on the platform. But any person can join and upload photos of what they want to sell to benefit the people who are searching to buy. You have set different categories that can be selected when creating a product. Is it a shoe or a T-shirt, male or female and so on. At first it all seems fine. All the products are labeled and are easy to search. However, you soon begin to notice that some people don't take the time to categorize their products. You shrug it off. Well, that's not my problem after all. It's their a product that won't be found and sold. With a little bit more thought, though, you realize that this actually hurts you a lot as the consumers have a harder time finding what they are looking for and the platform loses ground to the competition. So you decide to create an automatic system to assign labels whenever they are missing or wrong. Those two hypothetical situations illustrate the significance of the image classification problem, although that probably wasn't necessary, as most of you were aware of its importance. However, they also serve another purpose to provide real world use cases. For our practical example. That's right. In this section, our job would be to create scenes that classify and label images of clothes and other fashion items. We will walk you through a large portion of the process and explain our reasoning behind it. Exciting, right. In the next lecture, we are going to discuss the data sets and the objective in more detail. Stay tuned. And thanks for watching.

35.  

In this video, we're going to discuss the data set that we will be using and our objectives for this practical example. All right. So as we've mentioned in the last lecture, we are going to train a CNN to recognize and label different fashion items in order to train a network, though we need a data set. And the data set is a crucial point here. It dictates how the networks are going to be structured and what they will learn when considering clothing items. There are generally two types of images, images that show a fashion model wearing the particular item and images that show only the clothing item or accessory. So at this point, we had had to decide which type of photos to include in this example. When considering the images with a fashion model, we imagine that the networks may learn to understand that the clothing item is targeting a female audience not by its intrinsic characteristics, but because the model wearing it is a woman. In the same way that CNN may learn that when only the top part of a male model is visible, the image shows a t shirt. However, we wanted to create networks that recognize the clothes themselves, not ones that make assumptions based on the people wearing them. So we opted for the second type of images, those that only show the item framed against a consistent background. OK, we have created the data set ourselves by collecting such photos and labeling them. And here are the images we extracted. In contrast to the amnesty example. These images here are not in grayscale. They are in color. Each of them has dimensions of 300 by 400 pixels. However, for this practical example, we have scale them down to 90 by 120. There are 16000 in total split into three main categories glasses and sunglasses, trousers and jeans and shoes. Since our goal is to learn to label the different items in a useful manner. We need more than just those three labels. That's why every class has additional subclasses. The glasses and sunglasses only have one subclass. As you probably guess, it's whether the photo shows a pair of optical glasses or a pair of sunglasses. Trousers and jeans are more complicated, apart from trousers and jeans. We can also differentiate between male and female. Thus there are four variations in total male trousers, female trousers, male jeans, female jeans. As for the shoes, it's the most complex category of all, with 11 subclasses in total. There are, of course, male and female shoes. The men choose subclause consist of boots, trainers or sneakers, sandals, flip flops and slippers as one subcategory, then formal shoes and finally others. The women's side includes boots, ballerina shoes, trainers or sneakers, high heeled shoes, sandals, slippers and flip flops again and others. All right. Our objective in this section will be to return all labels associated with a given image. For example, given this photo, we should assign the labels shoes, female and high heeled shoes to it. Good. As we've seen, this data set is not a simple linear one with only one type of labels. Considering all the different labels and sub labels, it seems inefficient to try and create only one network that needs to deal with all of it. In fact, it is much better to create different networks for the different tasks that are of the same level. Let me illustrate what I mean. We can say the three main classes are on the same level. Therefore we can create one network that will perform the primary classification of the image. Is it an image of glasses, trousers or a shoe? Once it has concluded that this particular image is a pair of glasses, we can run a different network to label it either as optical glasses or sunglasses. These two networks involved are entirely independent, so in order to solve the problem, we can divide the task into smaller bits that we can solve independently. Great, we will do just that in the next lectures, starting with the primary classification following this video. There is a short info about the data set with all the numbers you need to know. Thanks for watching.

36.   

Welcome back. In this lecture, we will quickly go over the conversion of an image file to an umpire. We will not get into details, rather, we'll just show you the main idea. All right. So last lecture, we saw the images in our data set. However, neural networks don't work with image files. They work with Tensas. Therefore, we need to convert all those images into numpty arrays that tensor flow can work with. Not only that, but the same computational power and memory. We need to resize the images from 300 by 400 pixels to 90 by 120 pixels. This is a standard thing you may want to do when dealing with computer vision. So how do you do it? Well, one of the easiest ways in Python is to use the image module from the pillow library. It provides a lot of nifty methods for working with images. We will only use a couple of those, but you can explore them all in the documentation page here. Let's take this image here as an example. It is in the same directory as the notebook file we will use. So here we import numpty and the image module. Let's also save the file name of our image. Now, loading the image into a variable is as easy as writing image equals image, dot open file name. After executing this line, we can manipulate the image using the variable great, if you're curious to learn more, here is a short list of functions and methods that may come in handy when you deal with images. OK, now that we have our image in the variable, we can visualize it with the show method, it opens it with a default photo viewing program as we have not done anything yet. It is still the same photo. Next on our list is the task to scale it down. We can do this with the thumbnail method. This method resize is the image in place. So once we apply it, the variable will contain the new transformed image. It also preserves the aspect ratio and that is exactly what we want. So we write image dot thumbnail. In the brackets, we must specify the new size, which will be 90 by 120, so far, so good. Now let's visualize the transformed image image dot show. We are greeted with a smaller version of the same image. OK, we have resize the image. We now need to convert it to an umpire. And that is really simple. Just past the variable to the Asira function. Like this image or a. Equals ENPI dot as Arae image. There you go. We now have an array representation of the image stored in the variable image, very smooth and easy this way. We converted all the images into numbers for our data set as a final note. Let's check the dimension of the array. ENPI dot shape imagery. 120 by 90 by 3:00 a.m.. Wasn't it supposed to be just 90 by 120? Well, the three is there, since this is a color image, that means each pixel has three values, a red, green and blue value. As for the spatial dimensions. This is just the convention in the field. You can picture it as if this is a list of all the rows and there are 120 rows. All right. In the next lecture, we will lay the groundwork for some of the code we will use in this section. Thanks for watching.

37.   

In this video, we are going to discuss some of the important code concepts so that later on when we write the full code, it will be less confusing. Of course, we won't linger too much on a line by line basis. You can inspect the code thoroughly on your own. Cool, let's think a little bit about how this business case is going to work. We will obviously need to create and train models, lots of models, actually, so we will have to log their progress and other metrics to compare them and decide which one does better. With that in mind, we will use Tenzer board extensively with the purpose of logging and visualizing these metrics. So let's quickly recap the structure of Tenzer boards. Hyper parameter tuning. First, we define the type of parameters and their values at the start and write this information in the logs. Then we define a function to train a model with a given set of hyper parameter values. This function returns the test accuracy of this model. After that, we define another function with a purpose to start the first function and log the result. Finally, we have a nested for loop that iterates over every set of hyper parameters and calls the run function. So all in all, for every different combination of hyper parameters, we train and test to model and log the result. For more information, you can check our video and the topic in the Tenzer Broad section or look at the official Tenzer board guides. All right, so we have our hyper parameter tuning set up, but what metric should we track in order to compare them? The obvious one is the accuracy of our model when dealing with classification. However, there is another useful metric that we discussed in the Tenzer board section confusion matrix. Here's a quick recap. A confusion matrix, sometimes called an error matrix, is a table that shows the categories our network struggles to deal with on the y axis of the table. We have the true labels of our data on the x axis or the predicted labels. That is the ones our network assigned to the pictures. The cells show the number of cases that conform to both axes. They are usually color coded, often with blue good. Now let's recall how the matrix is implemented. There are three main points in logging and displaying the confusion matrix that are defined in functions. First, we have the plot confusion matrix, which plots the Matrix visually in math plot Leyb, given the already computed numbers. Then there is plot to image, which you guessed it converts the plot to an actual image. And the last one named Log Confusion Matrix was about computing the actual numbers using learn and logging the info to Tenzer board. Finally, we should define the callback to start the whole process. That is how we will implement the confusion matrix and the upcoming code in the next lecture. Speaking of in the next video, we will solve our first task, the Primary Classification Network. See you there. And thanks for watching.


38.  

Welcome back, let's quickly recap our objectives for this practical example, we have a data set of images of different clothes and accessories. Our goal is to create networks that will label all these images accordingly. We explore the labels and sub label structure of our data, and given its complexity, we decided to break it into several smaller objectives that are solvable by a single model. The first of these is the primary classification. That is to label the image as one of three options glasses and sunglasses, trousers and jeans or shoes. And that is exactly the topic of this video. The Primary Classification Network. All right, let's get started. First, we need to think about what our approach would be. We could, for instance, agree on certain complex network architecture and try to find the optimal hyper parameters for every many task. Or we might try out a thousand different architectures without too much tuning of the hyper parameters. The truth is, as always, somewhere in the middle to decide on what to do. However, we need to be aware of the limitations. A rule of thumb I heard once was that a possible model could be made in two weeks, a good one in three months and a great model in more than a year. Diminishing returns definitely play a big part in machine learning, and we certainly don't have a year to spend on just this practical. Even more so as those timelines are probably for computers with powerful GPS or clusters of computers with more than 20 times the speed of the ordinary laptop I use to train the networks. Furthermore, we have numerous, many models to create. In light of all of this, we would proceed as follows. We are going to do several of the tasks in this practical enough to teach you how to proceed and solve them. We will explain all of the methods and techniques employed leading you to apply them on the rest of the tasks to sharpen your understanding. In fact, this practical is open ended, meaning it allows you to set yourself a different goal and accomplish it. We will simply provide you with a general framework to solve those goals. All right. Now let's see what the plan is. First, we will concentrate on the main labeling. Then we will continue with finding the best approach for the subclasses of trousers and jeans. And finally, we will keep it off by applying the three techniques we mentioned in the previous section, L2 Regularisation Dropouts and data augmentation. That being said, we will not focus much on the primary classification as it is the most similar case to the next example we already did. Good with that out of the way, let's get down to it. First, the packages for our purposes, we are going to need all these packages, IO, itour tools, Escalon and math plot Leyb for the confusion, matrix and numpty for the data sets. Of course, Tenzer Flow and Tenzer Board are there for well, you wouldn't be here if you didn't know their purpose. Moving on swiftly. We can't do machine learning without a data set. If you recall, we highlighted the structure and contents of our data sets earlier in this section for this problem. The data sets we will need are called primary categories with all three variations, train validation and test their compressed in NPC format, which is a no by extension for storing multiple Dampier's. Each category has the same structure. They consist of two arrays, one containing all the images and another with the label associated with each image. The encoding is like this. Zero represents glasses and sunglasses. One is trousers and jeans and two is shoes. OK, to retrieve the data sets, we can use the ENPI Dott load function and state the file path to the NPC file. I have chosen to store them in a folder called Data Sets now, in order to import the data sets, we write those three lines. Now we have to separate the inputs and the targets in our case, those are the images from the labels. Let's look at the training set, which is stored in the variable data train. The process of extracting an array from it is as simple as treating it as a dictionary. So to get the array with all the images we write, images, train equals data, train brackets, images. Same for the labels, only this time we change images to, well, labels. And that's our two arrays for the training set, the names for the different arrays are generated when creating the NPC file. So if you create your own data set, there's no need to follow the same name convention as me. OK, we can repeat the same code with a change of variable names for the other two sets. So far, so good to continue this streak of data preparation, let's pre-process the numbers a bit since we are using no higher res for the data as opposed to the tensor flow data sets object. We use that the amnesty example, the preprocessing part is rather simple. We can easily scale the pixel values between the standard zero and one and one line images. Train equals images train divided by two hundred and fifty five. Of course, we do the same for the validation and test images as well. That's neat, but it is not the only benefit of using the umpires when working with this data type tensor flow can automatically shuffle and bash the data set for us. We do it with the fit method, so our work with the preprocessing part is over. We will continue with the architecture and training of the model in the next video. Thanks for watching.

39.  

Continuing from where we left off. It's time to think about the actual network architecture. So the first question is how much work our model needs to do. Well, we've got three classes of data to be separated, glasses and sunglasses, trousers and jeans and shoes. If we think about it, these are actually very distinct classes. I mean, the trousers are vertical while the shoes are horizontal and the glasses are mostly transparent. The expectation is that our network should not struggle with this task. Therefore, even a simpler model could work here. That's why we are going to follow the same structure as the one we had in the amnesty example. It consists of a convolutional layer, followed by a max pool layer, again, a convolutional and a max pool layer, then a flat thin layer to unpack the tensor and finally a dense layer with three outputs that represent the three classes. What about the parameters of the different layers? Well, we need to fine tune them for the optimal performance of the model on this particular data set. And of course, we are going to do that with a tensor board hyper parameter tuning structure. Given the architecture, we have four parameters to tune the filter size and the number of filters of both convolutional layers. The values for the filter sizes that we are going to consider are three by three, five by five and seven by seven. The values for the number of filters we are interested in are 30 to 64. Ninety six and one twenty eight. However, given the four parameters, this makes one hundred and forty four combinations. Taking into account the fact that this network is trained on a laptop, it would take around 15 to 20 hours for the grid search to end. So in the interest of saving time and resources, we will make two simplifications. The filter size of the second convolutional layer will be set to three by three by default, and the two conveyers will have the same number of filters. That means the only parameters that we will tune are the filter size of the first layer and the common number of filters. This adds up to 12 combinations of parameters, which is much better. Remember, since we are doing a grid search, every single additional parameter or value is multiplied, so the number of combinations grows extremely fast. All right, now that we know what our objective is, let's Kodet. We have done all of it several times already, so I will walk you through it just a little bit faster. Good, let's start with defining a couple of learning parameters here. First is the maximum number of Epic's which we will set to 15, then is the batch size initialized as 60 for. Now, given that we will implement early stopping, I don't expect to ever reach the 15 EPIK limit, but the variable is still here to prevent the training from becoming too long. As for the batch size, you may ask why we didn't include it as a hyper parameter to tune. Well, in general, the batch size may help the network to reach the minimum quicker, but it does not have a great influence on the accuracy of the final results. That and the fact that introducing the batch size as a parameter to tune will increase the train time significantly made us leave it as the rule of thumb. Power of two, that is 64. Good, so defining the hyper parameters without bogging down into too much detail, we create the two variables to represent the hyper parameters and enter the values we're interested in. Then we lock them with the file writer. We should now decide where to create this file in this section. I will use the following structure. There will be logs folder where every log is written to inside. There are folders for the different models. We will test and in every such model folder reside the two types of logs, one for hyper parameter tuning and one for the training process logs, even though for the main classification task we will test only one type of architecture. I will still use the folder model one. All right, the administration is completed. Let's get down to the actual modeling. Next up is the train test model function, the heart of our coat. This is where we build and train our model. However, we would also like to track different metrics associated with the training process other than the final accuracy logged by the hyper parameter tuning. So we will track the training process with a standard tensor board callback, but we will also log the confusion matrix. OK, let's get to work, starting with the model we created in the familiar way tf dot tiara's dot sequential and add all the layers. As we've discussed already, the number of filters and the filter size parameters for the first one are varied, so we identify them by their corresponding variables. Note that the input shape is defined as one 20 by nine by three as those are the dimensions of the umpires. Be careful not to have 90 by one 20 here instead as that would raise an error. In the end, the dense layer has an output of three, as that's the number of categories we have. Moving on, we defined the lost function. And compile the model with the atom optimizer. Then we create a variable for the logging directory we will use this one is in the fit folder branch. The next item on the list is to implement the confusion matrix, if you recall, it consists of three function definitions, one that plus the confusion matrix, given the numbers, another that converts this figure to an actual image, and the third that calculates the numbers and logs the image. So let's just copy and paste the relevant code from our earlier lectures. There are just a couple of things we need to tweak. First, we have the directory of the file writer. I am going to have it in the directory we created earlier, but within an additional folder called s.M Standing for Confusion Matrixx. The other matter is the class names we need to provide them inside the log function right here. Fantastic, the last portion is where we define the callbacks and train the network, this time we have to Tensas board callbacks, one that log's training data and the other for the confusion matrix. Then we have the early stopping Carlebach, it has the usual parameters, patients of two and restore Bestway set to true. And now we have arrived at the point where we train the model. We have the fit method with all the parameters for the input, data and labels, we can just pass the numbers as they are and tensor flow will have no problems. OK. Next up is Batching. And here comes the point we made in the preprocessing stage, if we set the batch size parameter here, Tenzer flow would automatically shuffle and batch the numbers we supplied. That's why we didn't do this earlier. We continue by adding the callbacks. Then the validation data is supplied as a tuple of the umpires, and we set the verbosity to two meaning limited printable information as to not clutter the screen. Great. Now, the last step is to evaluate the model on the validation set in order to produce the accuracy metric that will be returned. It is important to make this evaluation on the validation set, not the test set, as we are yet to finalize our model. That was the train and test function. Let's take a step back, though, and see what information we gained from that. So we train a model for every combination of the hyper parameters and return its accuracy. We are also logging the confusion matrix in each epic of each model, as well as the standard training data. But the model itself is lost as the variable that contains it is not logged subsequently. If we wish to continue the training or test that exact same model with a different data sets, we would not be able to. That's why I will also export the model to a file as well as all the other logs, and yet again, we can easily do this with a single line of code model dot save and then the path to where it should be exported. In my case, I will save the models to a subfolder called models. Unfortunately, an important caveat is lurking in this simplicity on Windows, the save method cannot write to file parts longer than two hundred and sixty symbols. So if you're working directory is buried deep down, you may get an error when trying to save a model in that directory. The solution is to use shallower directories like this one. Another point about the model saving is that they take a lot of disk space, and if you save everything, you may fill as much as 100 gigabytes. We have surmounted the hardest part of the code, so this feels like a good place to take a break, let's rest for a bit and return to the code in the next lecture where we will finish the run function, train the model and see the results. See you there. And thanks for watching.


40.   

Welcome back. It is time to finish the primary classification. As we outlined at the start in this cell, we will place the run function, its structure is always the same, so I will paste it directly. Next, we have the for loops that will pick every combination of the hyper parameters we need to iterate over all the values of the filter size and all the values of the number of filters, we define those allowed values at the top of the notebook, remember? Then we package those values in a dictionary and start the chain of functions that will train and log the results. And basically that's it. That is all the code for this architecture of the primary classification task, we can now take a breath as this is not only the code for this task, but for most subsequent tasks as well. Yes, we will actually copy this and use it for the next problems, because the only things we need to change are the different logging folders and the model itself. And we can do all that with a few clicks. Fantastic. Well, now you must be eager to begin the training and that's exactly what we are going to do. So let's run this cell and initiate the process. This is going to take some time because each model is trained for roughly five minutes. I suggest we cut straight to the end. The network has been trained with all the different combinations and the data is hot. So let's check it using Tenzer board, we will start with the already familiar code here. First, let's take a look at the hyper parameter tuning. Now, recall that before running Tenzer board, we had to write two commands in the command prompt. So starting with CMD, we first have a task kill command and then a delete command to erase the temporary files. Fine. Now we can run the cell. Of course, the first time we will get a time out running at a second time displays the results. Great, let's look into it. We go to the programs tab at the top and are greeted with this table view. It is a good idea to order the runs at a decreasing accuracy, so here on the left, we set sort by two accuracy and direction to decreasing. Wow, it seems that our network has done exceedingly well with this task, those accuracy scores of more than ninety nine point nine percent or even better than the amnesty example, it truly seems that the different image classes are extremely easy to distinguish. On top of that, the majority of runs have such high accuracy and all of them are above 99 percent. That's an amazing result. If we take a look at the hyper parameters that created the highest scores, we can't really see any pattern here, at least from the table view. So let's point our attention to the scatterplot view and extend the bottom part to see the whole graph. We can't really see any correlation between the hyper parameters and the accuracy here, either, nor for the filter size, neither for the filter, no. So the conclusion is that this task is so easy that whatever we do, we get an almost ideal 100 percent accuracy. And that is the reason why we won't investigate the primary classification any further and we won't create different architectures or use any regularization techniques as there is just no need for that. Let's recap what we did so far. Well, it was a fair amount of work we took on the primary classification task to create a CNN that distinguishes between an image of a shoe trousers or glasses. To achieve this, we settled on a relatively simple model consisting of two convolutional layers, two max pool layers and the compulsory dence outcome layer. We decided on this configuration because it keeps the training times lower and allows us to check for different hyper parameters. In terms of code. We imported the data sets and pre-process them created the functions for the hyper parameter tuning and confusion matrix and log the training process. Using Tenzer board, we tried out the model with a filter size of three, five and seven and number of filters set the thirty two. Sixty four, ninety six and one twenty eight. The results were impressive, with more than ninety nine point nine percent accuracy across all different combinations of parameters. OK, so we are done with the primary classification, but there are many other many tasks still waiting to be solved. In the next couple of videos we will be dealing with the trousers and jeans category and its unique challenges. In the meantime, you can practice and hone your skills with the glasses category. Your task is to create a model that will discriminate between pictures of glasses and sunglasses. All right. You can take some time to solidify what we did today and continue with the next lecture. See you there. And thanks for watching.


41.   

I hope you have practice with the glasses and sunglasses data set and are now comfortable with the code structure and purpose, as we will now go much quicker over the next model's code. In the next couple of videos, we will be dealing with a subtasks regarding the trousers and jeans. The task is the following. Given an image that has been classified by the primary model as belonging to the class. Trousers and jeans please determine its type and gender. That is, whether it is trousers or jeans and whether it should be in the female section or the male one. Of course, if the main model has classified the image incorrectly, all that doesn't matter. That is why it is important to have a good model for the primary classification which we have in our case. OK. Now, the objective of this task does not actually imply how we should proceed with it. It just needs these two labels to be assigned to the picture. We know how to associate an image with a single class, but how do you assign it to labels? More importantly, those are not two labels of the same type as is the case when a movie can be considered to belong to more than one genre. The different classes like comedy, drama, crime are of the same type. They are all genres. In our case, the labels are independent of each other and of a different type. One is gender. The other distinguishes between trousers and jeans. Well, there are multiple approaches we can go for in this task and we will investigate the two most logical ones. The first option is to do the same thing we did for the overall task in this section divided into many tasks and train different models for them so we can have a model that will determine just the gender. There would also be another network that will just deal with the type of pants, trousers or jeans. As for the second option, the idea is to combine the different labels into one. So given we have two labels, each with two classes, that makes a total of four combinations. What we can do is set each of these four combinations as a single class, thus combining the two labels into one. This would allow us to have a single network as per normal that will classify the images great. Both methods have advantages and disadvantages and it is not all obvious what the correct technique for our data set is. If we employ the first option with the many tasks we will need to train and tune to different networks from scratch. They might require different architectures and hyper parameters, which makes this option potentially computationally expensive and demanding the way we outlined it. Both models are on equal footing. However, there's also the matter of hierarchy. In certain cases, it may be wiser to have one of the models first and then, depending on its outcome, to have other different models. For example, we can have the gender model first and then have a classifier that determines the type label for men and a classifier for women pervious reasons. This approach is called hierarchical classification. This may work if men's jeans and women's jeans are nothing like each other. This would make a single model that determines what jeans are and puts all of them in a single category. Extremely difficult to achieve. Things to consider in this case, though, are that we will need to train even more networks. Three in our case and the lower the level of the network, the less data is available as the data set needs to be split for the different outcomes. Those were the cons, but there are still some pros with this technique. The advantage of defining these many tasks is that we might still get partially correct answers if gender is really easy to determine. But the type extremely difficult. Having many tasks devoted to each of these means that it will almost always correctly guess at least the gender. And if we know which label is the easier one, we can construct a hierarchy with the easier one on top, which might help the models below cope better with their more difficult tasks as they are more focused on a particular subset of the general data. All right, the second option has its own potential drawbacks, too. It combines all labels into one, giving all labels equal weight. However, that means it may struggle when there is no correlation between the different labels or they do not represent similar things. This also means that if one of the tasks is easy, there is no guarantee that it will manage to guess it right as the labels are combined and the outcome is determined by all features, not just the ones relevant to the easy label. So if there is a great disparity in the difficulty of classification, this is not the best option. As for the pros, this technique allows for the training of only one model, thus decreasing the computational demands and allowing for more time to fine tune the hyper parameters. It can also take advantage of the whole data set and the correlations between the different subcategories. That's great. So in summary, what is the technique we should employ in our situation? Well, to be fair, I can't decide just by looking at the data set, combining the labels does not seem to have any obvious problems, nor does the mini task approach. So we will try out both of those methods and compare them at the end to see which one is better. OK, in the next lecture we will get started with the technique of combining all the labels into one. See you there and thanks for watching.

42.  

Welcome back. As we've discussed, there are a couple of approaches that could work in the task to assign two labels to the trousers and jeans category. In this video, we will deal with the method to combine the different labels into one. OK, the two labels that we should determine our gender and type, that is whether it's female or male and whether the images of trousers or jeans, if we are to combine these labels, clearly there are four resulting classes male trousers, female trousers, male jeans and female jeans. So now we are going to build a network to assign one of those labels to each image. I have named this method trousers and jeans, all because it does all the work in one go. Therefore, this will be the name of the folder where the code resides. All right. I proposed to test three models for this task. The first consists of only a single convolutional layer and the final dence one. The second is the structure we're already familiar with, the one with two conveyers, and the third is a variation of the previous one with an additional dense layer just before the last dense one. Great. As we mentioned last time, we don't need to write a whole new program for every model. We can just copy the one we've got and adapt it a little to fit the new task. So here is the copied code of the primary classification task. What do we need to change? Well, first, let's take a look at the data set for this task. We have created a data set that has the combined labels. It is called Trousers and Jeans Dash all just like before. I have placed it in a folder called Data Set. OK, this data set consists of three numpty arrays, one for the images, one for the gender label and one for the combined labels. The combined labels are named simply labels, so we don't need to change the cell. Next, let's skip the hype of parameters for now and jump to the model outline, we can delete all the extra layers and leave just the comms flattened and dense layers. Remember, our first model to test is a very simple one, and we just want to see how it behaves. A very important thing to remember change the dense layer output. Previously, we had three classes. Therefore, the dense layer had an output of three. Now we have four classes, so it needs to be four. And now it seems that the hyper parameters actually fit perfectly. We are going to try out filter sizes of three, five and seven. A number of filters of thirty two. Sixty four, ninety six and one twenty eight. The last thing to change is the class names for the confusion matrix. They are in order trousers, male jeans, male trousers, female and jeans, female. One could consider parameter sizing this part as well, but let's not do it now. Fantastic. Those are all the adjustments we needed to perform. Now we are ready to begin training, so let's run the cells and skip to the end of the training. Here we are an hour later and the training is complete. Time to check the results. Entering the two command prompt statements to clear the temporary log files and starting Tenzer board. Let's sort the table on descending accuracy. As we can see, the accuracy fluctuates mainly between 60 and 70 percent. This is not bad for such a simple model. For reference, a random classification would yield around twenty five percent accuracy on four classes. Now, let's check how the different type of parameters performed, switching to the scatterplot view and enlarging it to see the whole graph. The first one is for the filter sizes and the second one shows the number of filters. Hmmm, it looks like the results are evenly distributed across the different parameters. There is no significant difference in using 64 filters as opposed to one twenty eight, for example. This might be expected for such a simple architecture. This is just a test model anyway, to see if it works properly. So let's move on. All right. Now, it might be a good idea to check out the confusion matrix. To do this, we need to start Tenzer board with the fit folder, not a param tuning. So we need to repeat the two lines in the command prompt and start the correct answer board. In order to see the Matrix, we need to switch to the image, view and choose which matrix to view the confusion matrices are stored in the folders ending with. So let's check the first four such runs. And the matrices appeared. That's great, they are a little bit small, though, to enlarge them. You can left click on them and to view them in their proper size, you can right. Click and press open image in new tab. Another thing you may notice is the slider above the image. It represents the current epic. In fact, we long the confusion matrix info at the end of every epic so we can actually monitor its progression throughout the training. There's one more detail to watch out for since we employed early stopping the model we are interested in is not the one of the last epic. The best model is the one on the third to last epic as we've set up the early stopping with patients of two. Therefore, in our case at least, we need to check the confusion matrix two steps before the end. That is the best one. OK, now let's inspect it. Remember, the vertical axis shows the true label while the horizontal one represents the predicted label. Thus the diagonal is when the model correctly predicted the class. What we can see is that if the model is shown an image of male trousers, it perceives it as one of the female trousers 18 percent of the time. The male jeans are sometimes confused with male trousers and female jeans. When looking at the female trousers and jeans, we can see that the model correctly identifies that they are female but struggles a bit with identifying their type. All right, we've seen that everything works properly. Now let's look at the real networks. Repeating all the previous procedures, we copy the code and rename it to Model to. Then we change the architecture to the familiar to conflate one. Of course, we need to change all of the log directories with model two as well. As for the hyper parameters, we can add the size of the second convolutional layer to. Again, we can do the same for the third model we would like to test. Copy the file, change the name and directories. And amend the model architecture. This time we add another dense layer with an output size that will be a hyper parameter, we will tune we will look at an output size of 256, 512 and 10. Twenty four. So we are going to leave those two programs running and return with the results. All right, here we are a couple of hours later, and the models have been trained, so let's look at the results of the hyper parameter search. On the left are the results from the second model. While on the right, we have the third model with the extra dense layer. As we can see, the second model is an improvement from the very simplistic first model, in fact, it's around 10 percent better. However, given that the first model is a super basic one, I expected an even bigger increase. There is also a slight increase in the accuracy in the third model compared to the second one, but not too much. As a matter of fact, it could be just due to the sampling of the data. A different shuffle or a different sample may have model two and three reversed in terms of accuracy. The different runs all hover around 75 percent accuracy. But is this good? Well, it's definitely not a bad result compared to the twenty five percent prior probability. It could even be considered great. However, it is nothing like the absolutely amazing results we've had in the primary classification task. If we look at the big picture, though, it is unreasonable to expect such a high accuracy for this task. At the end of the day, we are trying to distinguish between trousers and jeans and their associated gender. However, by default, some of them are indistinguishable. I mean, look at those two images. One of them is female, the other male. But we can't really tell. Come to think of it, 70 percent plus accuracy is not bad at all. In light of the data set in the task, with three more months worth of tinkering, we may be able to bump those numbers up to eighty five percent accuracy. But keep in mind that it's highly probable that we'll never reach ninety nine percent no matter what we do. That's just the nature of this task. This is a valuable lesson which shows that when we use actual machine learning for practical purposes, we don't get the neat results we wanted or expected. So far, we were discussing the accuracy, but which hyper parameters brought the best results? First, I'll check on the model to scatter plots, to see all of the plots, I'll extend the bottom part. Well, all of the different dots seem to be spread pretty uniformly across the different parameters, not only for the filter size, but the filters number as well. If we take a look at the same place for Model three, it is the exact same situation. All parameters seem to perform pretty much the same. Therefore, the behavior of our models is very stable. This might actually be a positive thing because it indicates the model can probably generalize better and we will not overfit through tuning the hyper parameters in a specific way for this exact data. Of course, if given more time and resources, we could build more and more sophisticated models, maybe gather more data in the first place and all the jazz. I encourage you to experiment with that if you feel like it. But for this example, we are going to leave it at that. We have trained three models for the case where we combine the labels into one. Next, we will create a hierarchical structure of models and then we will compare the two approaches. Thanks for watching.


43.   

Welcome back. Last time we approach the task of assigning two labels to the images by combining them into a single one, this time we will create different networks to solve the problem for each individual label. This process will consist of two steps. First, we're going to build a network that will predict the gender of the intended buyer based on that result. We will have two more networks to classify it either as trousers or jeans, one network for male and one for female output, to be precise. In this video, we will build and train the different networks separately. We will not actually create the application that stacks them one after the other. We will build something close to this application in the next lecture where we are going to compare the two methods. OK, let's get started with the gender label. Given our previous results, we decided that there is no need to create and test three different models. So for this parts, we will play around only with the most sophisticated CNN architecture so far, the one with two conveyers, two max players and two dense layers. Let's copy the model three code from the previous lecture. Of course, we change all directory names to model one. And the labels for this task to be the gender array. Finally, we change the output of the last dance layer to two. And fix the class names of the confusion matrix, because we only have two labels for this task. Male and female. This tactic is becoming kind of a habit now, right? Continuing, we train the network and the results are in. Let me help intenser board and sort the different runs by accuracy. This time, the network is performing in the upper region of the 80s, close to 90 percent. This is a surprisingly good accuracy for classifying the gender of genes, which frankly are not always easy to differentiate. Regarding the hyper parameters, we are yet again greeted with an even spread. At this point, we are getting used to this trend, so no need to linger too much on it. Since we are creating a hierarchical classification, we now need to build two networks to label the type one for male trousers or jeans and another for female, only these networks can be constructed and trained equivalently, as we've done so far. That's why we believe them. As an exercise for you, you will need to inspect the data set file in order to pick the right ones for these tasks, then create and train the actual networks. You may write the code from scratch or copy the previous one and change whatever needs to be changed. It is up to you to make this choice. The point is that while these lectures are educational, it is easy to miss some small details. When you're just watching a video, you will find that these seemingly simple tasks often generate tiny errors that need your attention. Now you have the opportunity to deal with those sooner rather than later. All right. For now, we will just show you the results we obtained. Both models performed equally well on the task with the same accuracy distribution as the gender model between 80 and 90 percent. Surprisingly, though, the hyper parameters did have an effect. What we found was that the first convolutional layer performed worse with a kernel size of five by five or seven by seven. The difference was not that significant, but the three by three kernel is the better choice. The other parameters have equal distributions as expected. Great. So far, we have train networks to do all of the many tasks for the trousers and jeans category. It is now time to test which technique is better suited for this data set the technique that combines the different labels into a single type or the hierarchical classification. For this purpose, we need to set up a program to stack the different networks. That's exactly what we will do in the next lecture. See you there. And thanks for watching.

44.   

In this lecture, we will compare the performance of the techniques we employed in the last two videos. All right. Given that we have two different labels, gender and tape, a single accuracy score would not suffice for this comparison. A simple metric to help us in this case, though, is to award the network one point for every correctly determined label. Thus, if the network correctly predicts the gender and type, it gets two points. If it manages to guess only one of the labels, it is awarded one point. And in the case of no correct predictions, it gets no points. Of course, a simple but elegant system. Not only that, but it is flexible as well. If you value the accuracy for one of the classes more than the others, you could reward correct guesses for this label with more points. In our case, I don't see a reason to value them differently. Thus, both get one point. So far, so good. We have agreed on a scoring system. Now we need to apply it. The idea is to load the models that had the highest accuracy for the respective task and score the performance on the test data set. OK, at this point you have all the skills necessary to create code to compare the two approaches yourselves. I strongly encourage you to attempt this on your own with the models you've trained so far. Nevertheless, in this video, we will code our version of this application so you can come back and check it. All right. For this notebook, we're going to need only the number and tensor flow packages. Then we can import the data set, this time we get only the test as we won't be training any networks, just testing their performance instead. Next up, we extract the images and labels from the data set. Bit of a roadblock at this point might be the type label in the data set itself, we have included the gender label and the combined one, but the Senate does not contain a type label. What we need to realize here is that the combined label actually contains both the gender and type, we just need to extract the type from it. So how do we do that? The combined label consists of trousers, male genes, male trousers, female and jeans, female. Numerically, these are represented with zero, one, two and three. For our purposes, both trousers classes should be assigned zero and both jeans classes one. Do you notice any pattern in the numerical labels of those two groups? Let me help you, the trousers group consists of even numbers zero and two. In contrast, the jeans have odd numbers, so the solution follows this logic we can take the combined label array and find the remainder when it is divided by two. Even numbers yield remainder zero and odd ones yield one. Therefore, the type label array is type test equals data test labels percent to. Perfect. Just remember to scale the image pixel values, even though we are not training the networks, we still need to scale the values as that's the expected input. That's why many models have their preprocessing included as preprocessing layers at the start of the network. Let's download the models were about to test, I have copied them to the folder and rename them for easier orientation. We've reached the part where we need to score the two approaches, so let's define the two score variables initially zero. The idea now is to loop through every image in the data set, get the predictions of the models, compare them with the actual labels, and change the two score variables accordingly. So we write the for loop. For I in range of length of images. Let's start with the combined labels model. First, we obtain its predictions for the current image. We can do this simply by passing the image to the model like that. Note that the model expects to receive a list of images to classify, so we can't simply write images, tests I. We need to take a list slice resulting in a list of one element. All right, this variable now corresponds to a numerical likelihood score for each class, the actual predicted class is the one with the highest score. Therefore, to get the predicted label, we must find the index of the highest score. This functionality is exactly what the numpty function ARG Max provides. So we have obtained the label of the image as determined by the model. Next, we need to determine the score. As already discussed, the type label corresponds to the groups of even and odd labels. This means that the check, the validity of the type prediction, we can take the remainder of the division by two and compare it to the true label. If they are equal, we increase the score of this approach by one. We can do a similar trick for the gender label, the first two of the combined labels correspond to male, while the latter two are female. Taking advantage of this information, we realized that an integer division of the label with two returns, the correct gender label again, if it is equal to the true gender label, we increment the score by one. With that, we have finished the combined label section. We now continue with the hierarchical classification by analogy to the previous model, we find the label predicted by the gender model. This time, we can perform a simple equality check with the true label again, we increase the hierarchical score by one. Now, depending on the result from the first network we run, one of the other two, if the label is zero, meaning mail, we evaluate the mail type model. Otherwise, we have the female type model. And that's it, the for loop is complete. Essentially, this whole program is just extracting the predicted label of a model and a couple of well-placed ifs great. Now, let's run the cell, it takes a bit of time to go through all the images, but nothing compared to training a network when it finishes executing, we will have the final scores stored in the score variables. So let's print them in a nice format with a bit of text for better readability. And here are the final numbers. Well, the scores are very close to each other, meaning there is no significant benefit to one approach. In that case, the preferred method might be the one with less computational stress on the system and easier usage, that would be the network with four classes into one combined label, it has a little bit better performance to boot as well. Fantastic. What about your experience? Did you get the same results, given the models you train in the previous videos? What if we take the best performing models for the respective networks? Does the balance remain the same? I encourage you to answer these questions as an exercise. If you want more practice on creating models and comparing different techniques of combining them, feel free to check out the shoes category of our data set. You can follow the same procedure as we did for the trousers and jeans, like combining all the classes. But you can also experiment with different techniques. It is important to realize that this practical is open ended. The choice is in your hands on how much to practice, what data sets to explore and what architectures to try that aim to get the most out of it. Our journey will continue next time when we try to improve the performance of our networks through the techniques we discussed in the previous section. Thanks for watching.


45.   

Hello there and welcome back. In this video, we will try to improve the accuracy of our trousers and jeans all model by employing L to regularisation and drop out. We have outlined and discussed those techniques in the previous section. We had already performed type of parameter tuning on this network, but couldn't find any significant difference in its performance. Since we can't improve it through tuning it, we might as well try to achieve a better accuracy through the aforementioned techniques. All right. So here's the code for the most sophisticated trousers in jeans architecture we created since we will be implementing L to regularisation and drop out on it. We will fixate the parameter configuration that gave us the best results. Therefore, I will replace the hyper parameter variables with the appropriate numbers in the model architecture. We won't get rid of the hyper parameter tuning structure as a whole, though, because both techniques have a high parameter that we will investigate. OK, now we can begin the investigation. First, we will try out L2 regularisation. Quick reminder, L2 Regularisation refers to the method of modifying the loss function with an additional factor. That is the sum of the models weights squared. The objective is to drive the unnecessary connections to zero, thus simplifying the model and preventing overfitting. So how do we implement L2 regularization in code? It's actually relatively simple. We need to add the regularization to each layer in the network through the kernel regularization parameter. The value that we pass is T.F. Dot Hera's dot regularizes dot l to. Inside the brackets, we need to state the value of the lambda parameter, that's precisely the reason why we left the hyper parameter tuning structure intact. We will check the behavior of the model to a couple of different values for this parameter. So let's create a variable for this lambda. But what range of values should we test out? Well, there is a huge domain in which to regularisation could be helpful. Usually Lamda can be as low as 10 to the minus five and get as high as point one, though rarely that high. Since that is a huge range, we will have our values on a large scale, starting from ten to the minus five, then five times ten, the minus five following is ten to the minus four and so on. Now we can add the variable inside the bracket here. In practice, though, this regularization does not take all the weights of the model, it considers only the way to the layer it belongs to. So we need to add this parameter to all layers that have trainable weights. Fantastic. Now we just need to fix the for loop in light of our changes and we are good to go, right? Well, not quite. A very important detail here is that changing the laws function affects the early stopping callback in this callback. We told Tenzer Flow to monitor the validation loss and stop training what it increases in two consecutive epics. However, our validation loss now has additional factors that drastically change its behavior, especially whether it increases or decreases. Therefore, we need to supply the unmodified loss to this callback. We can do this with a few changes up here when we define the last variable, we can give it a name by which to reference it later. I will name it Spar's Categorical Cross Entropy. Not very imaginative, but very descriptive. Then when we compile the model, we simply add this variable to the metrics list. Now we can reference this unmodified loss from the early stopping call back with a name we've given it. That's it. Now we can train the network as usual, we will skip to the end of the training. All right, here are the results. The scatterplot graph is not very informative, almost all values of the lambda parameter are clustered to the left, indistinguishable from one another. We can easily fix this by changing the scale of the x axis from here. Let's set it to quintile. Great. Now, what do we see from this graph? Well, we notice a huge drop in accuracy for the two biggest values of the parameter. No surprise there. However, the rest of the values fluctuate up and down a bit. Crucially, though, there is no significant boost to the network's performance. This could be expected at the end of the day. It does not seem that our network is fitting the training set so well that it over fits. If there is minimal overfitting, we can't expect a technique such as L2 regularisation to help. It just seems that the data we have is inherently difficult to learn. Although we've already tried to regularisation, we might as well check the drop out technique. This refers to the technique in which we randomly sent some of the outputs of a dense layer to zero, thus helping individual neurons to learn more general patterns about the data set. The implementation of dropout in code is actually super easy. Barely an inconvenience, it has its own layer object, intenser flow. Therefore, to apply dropout to the first dense layer, we can add this line after it. The parameter for the dropout layer's states, how many neurons to drop? That said, zero point one means 10 percent of the outputs of the dense layer would be set to zero. Again, this is a hyper parameter of our system and we will create a variable for it. The dropout rates we would be interested in are 10 percent, 20 percent, 30 percent and so on, up to 90 percent. It is unusual to see rates above 50 percent, but since we are here, we might as well check them. All right, we can reverse the changes related to the lost function we made and train the network. And here is the data. But that looks strange, the accuracy goes all over the place with sharp changes from one value of the parameter to the next, even more surprisingly, some of the values above 50 percent actually perform better than the ones below that threshold. This result reminds me a lot of noisy data, when a certain operation is dominated by randomness, the results look pretty similar. So we've got a hypothesis. The accuracy of our network fluctuates a lot due to random noise and the tensor flow operations and you to the initialization of the weights. This means that the hyper parameter tuning we've performed so far can't lead us to any significant conclusions. This is the hypothesis. To test it, we will train our model with the same set of parameters and initial conditions 20 times. We will omit the drop out or L2 regularization and just focus on the base network from the spread of the data. We can quantify the influence this random noise has on the accuracy. For ease of work, I will use the same structure we've had so far and define a hyper parameter, which is the index of the current run. It goes from one to 20. Now we are ready to run the whole thing. Here's what we obtained. The mean accuracy across all 20 runs is seventy three point seventy five percent, the standard deviation or in other words, the spread of the data is two point sixty five percent. The maximum accuracy achieved by the network is seventy eight point six percent, while the minimum is 67 percent. This analysis points to the fact that all fluctuations we've seen in the accuracy so far have been almost entirely caused by randomness. This, in turn, means that the models for which we saw a marginal improvement with L2 regularisation or dropouts actually had nothing to do with those techniques and everything to do with random chance. To be fair, if we were to perform this practical rigourously, we should have trained the model for at least 10 runs with the same type of parameters and every network so far, instead of having one data point for each set of hyper parameters, we would have obtained 10 data points. In turn, we should have compared the average accuracy to determine the best set of parameters. But of course, that would have inflated the training time drastically, meaning we would probably still be on the primary classification task. All right, despite these results, the techniques of L2 regularisation and dropouts can potentially still have beneficial effects. It could be the case that when we introduce dropout, the standard deviation decreases. Thus, even though the mean accuracy is the same, we can stabilize the optimization by reducing the fluctuations. We can test this new hypothesis with the same experimental setup. Only this time we will include drop out of zero point one and zero point two. Again, we run each configuration 20 times. And these are the results. Unfortunately, there is no change because of the drop at the mean is still the same as is the standard deviation. So in this video, not only we couldn't improve the performance of the model, but we discovered that it was dominated by randomness, a bit disappointing, right? Well, the thing is, in real machine learning projects, we are often restricted by the quality and size of our data. Many times due to the nature of the task at hand, we can't achieve our desired performance. That is a long winded way to say that we don't always achieve the neat results we were aiming for. And that's fine, given more time and data. We could probably get accuracy of up to 90 percent on this example. We may inspect up close every individual feature map to see what exactly is happening inside our net, but we don't have the time and resources for that. So let's call it a day and continue to the last topic of this practical data augmentation. Thanks for watching.

46.  

So far, we've dealt with symmetrical images, ones of trousers, jeans and glasses. The shoes category, however, contains photos of shoes, flippers and other, all of which face left or right. But in real life, our model could encounter some images that have the opposite orientation right to left or just some other non side view. If we want our model to recognize these, we need to train it with such data. And one possible solution is to go out and extract more images for the data sets. We can also do a little trick, augment the data we already have. If we just flip our images, we can get the right to left orientation present in our data. This data augmentation approach has limitations because we can't transform the original images to show a top view of the shoe. But at least we can solve one of these problems. All right. As stated, we will deal with the shoes category. It is similar to trousers and jeans as the shoes have a gender label and a type label. Therefore, similar to the trousers and jeans that we can have a model with combined labels that I will call shoes all this time, we will not consider the model itself as that's not our focus. We have already created and trained it with the available data. These are the results. Now, the question is, what should our approach be and how would we achieve it? Well, in this data sets, we have around 5000 images. It is entirely feasible to flip all of them and add the flipped version to the original dataset, obtaining 10000 images. So how do we obtain a mirrored version of our original image? Well, the images are actually numpty arrays, right? So if we want to flip an image, we simply need to reverse the values inside the arrays. This is a well known company functionality that we can achieve with the flip function. The tricky part is to determine which areas exactly should be flipped because flipping too much may result in rotating the image rather than mirroring it. Let's recall the dimensions of our empire, the first dimension referring to Axis zero is the number of images. It represents the list of all images. The second one. One hundred and twenty in length is all the rows of each individual image. This is axis one. Then we have a dimension of 90, which represents all the pixels in one row. It is designated as access to. Finally, we have each individual pixels, red, green and blue values, this final array has a dimension of three and is the axis three array. Since we want to flip the image horizontally, we can flip every row that should do the trick. So we need axis one, right? Well, no, as Axis one represents the list of all rows, thus flipping, it would place the last row as first and so on, this would have the effect of flipping the image vertically. What we need is access to as we need to reverse the order of all pixels in every row. Of course, when in doubt, you can always print the flipped image with the help of math plot lib, and it would be obvious that the wrong rotation has occurred. In fact, doing this is highly recommended. Now the code is straightforward. We mirror all three data sets with this simple command enpi dot flip the data set in question axis equals two. Then we can combine the flip data sets with the original ones through the use of the concatenate function and obtain the final data set. Since the label of an image does not change when we flip it, we can add the original label array to itself. And that's it, as simple as that. Fantastic. Now, after running cells, this network was trained to recognize both orientations of the shoes. It even retains similar accuracy levels to the regular model, so we actually didn't lose much performance due to the data augmentation. That's great. Well, that was it for this mighty behemoth of a practical example, of course, the only way to get the most out of it is to practice with these data sets on your own. You can even get much better accuracy with different model architectures or techniques, try to resize the images or perform data augmentation on the other categories by translating the objects of interest. Be brave, experiment and get comfortable with these kinds of operations. As for this course, there are still two sections left to explore. The next one is unexpected failures. See you there and thanks for watching.

47.   

So far, we have created some neural networks to perform a variety of tasks from classifying handwritten digits to tagging clothes for online shopping, even though we have achieved high accuracy for these models, we still don't really know how they work. In reality, understanding what a neural network managed to learn is an ongoing research in the field. We usually say that machine learning is just like a black box. We don't know why the model makes certain predictions. Of course, this does not mean that there aren't any meaningful insights for us to gain. It is still well worth dissecting this black box. That's why academics spend a lot of time trying to understand how our trained models make their decisions. This may help them better predict any unexpected or plain wrong results in the long run, and there are a lot of ways in which a seemingly good network may exhibit unwanted behavior in this lecture. We are going to take a look at some examples where neural networks break in order to gain insight into possible ways for improving them. So the first thing we need to understand about machine learning is that it's not confined to the same constraints as the human mind. We may not notice it, but our thoughts and thinking process are quite biased as we subconsciously limit our possibilities and purpose to fit a common, unspoken narrative. That is to say, we are not good at thinking outside the box for a machine. Such a mental box does not exist in the first place. In general, human beings do not consider the whole space of possibilities. Ultimately, this can be both a positive and a massive hindrance. Let's consider a highly simplified, yet illustrative example of this point. Imagine long time in the future. In a galaxy not so far away, we are in a war with aliens to maximize the efficiency of our defense systems. We decide to use machine learning, so we make a highly sophisticated model that has access to our most advanced technologies robots, satellites, the Internet, sharks with lasers, whatever future technology exists. Now, let's say the particular problem that we use this model for is to minimize the chance of enemy missiles killing humans on Earth. That seems like a reasonable task. So how can this go wrong? Well, the goal of the network is to prevent enemy missiles from wiping humanity. Nothing is stated about the friendly ones. Thus, the model may come up with the solution to nuke all humans. After all, if no humans exist, they can't be killed by the enemy. Upon observing what the network actually learned, the engineers may fudge it a bit as a rule to prevent it from killing people and call it a day. But then again, the model may decide to capture all humans, put them in a spaceship and send them in a random direction somewhere far from Earth. The same goes for real world problems. What we think our goal is may in fact be different from what it actually is. This example demonstrates how setting reasonable seeming goals to small models may often result in undesirable solutions. And if you think my made up alien story is far fetched, well, you better take a closer look at the following Real-Life situation that happened to Amazon. As you probably know, Amazon is a huge company with more than half a million employees. You can imagine they invest a lot in human resources and hiring new talent. In 2014, they decided to use a neural network to help them recruit new personnel. The goal of the model was to make a decision as close as possible to that of a human recruiter. Given a CV of a candidate, they train the model using their own H.R. archives as training data. This all seems reasonable and quite doable, unfortunately, things took a wrong turn. What happened was that the model learned to penalize female applicants. It look for the word woman, as in women's chess club, and penalize that application. How did this happen, you may be quick to judge that previous Amazon azar's had discriminatory hiring practices, and since this is the training data for the model, that's what it learned. Well, this may or may not be true. You see, the issue probably stems from unequal prior probabilities. Amazon may have hired more men than women simply because more men applied in the first place. At the end of the day, though, there were some bias in the training data, whether that was the distribution of people applying or the recruiters hiring practices are both irrelevant to our purpose. What matters is that any bias present in your data set will be picked up by the model. Luckily, Amazon was swift to notice the mistake of their algorithm and stopped it before it entered service, the mall recruiting engine was discontinued in twenty eighteen. All right. I think this example illustrated the points, but if you're still not convinced, let's take a look at one last example of how fragile machine learning can be in 2014. A paper called Intriguing Properties of Neural Networks showed how small perturbations to the input can have a big impact on the output. One of the things the research team investigated was the performance of a convolutional neural network trained on image net image net as a data set for image classification. With more than 2000 categories here on the left hand side, you see some examples of images that CNN initially classified correctly. The researchers then changed those images just a little bit. They transformed them into the images on the right for the human eye. There is no perceivable difference. They look identical to us. Thus, we would expect the network to classify them in the same way to now in the middle. We can visualize the difference between the original and transformed images. These represent the pixel value difference magnified 10 times to everyone's surprise, when the network tried to classify the transformed images on the right. It decided that all of them are ostriches, and that was done for hundreds of images, not just the three shown here. Furthermore, the team formalized their findings and documented an approach to constructing these small variations in order to confuse the CNN. This is a good illustration of how fragile machine learning models can be. You may achieve high accuracy even on the test data set, but that does not guarantee your network is stable. While these experiments were made by well-intentioned academics, misusing this knowledge could have serious implications. Imagine you were writing a face recognition software for security purposes. It would be a real disaster if people were able to fool your model by putting on an indistinguishable amount of makeup or simply transforming the image in real time through their own malicious software. All right, in this video, we've outlined a few problems associated with neural networks, by no means are we saying and ends are bad. We just want to illustrate that understanding the behavior of your network is particularly important. OK, in the next section, we will discuss some of the popular CNN architectures that have been developed. Thanks for watching.

48.   

So far, we have examined a couple of convolutional networks whose architectures were our own creation without in-depth research. It is now time to take a look at what the pros have been doing on this front. In this section, we will delve into some of the more popular and influential CNN architectures. All right. One of the fronts on which different networks were tested against each other was the so-called image net, large scale visual recognition challenge. This was basically a challenge for algorithms to see which one could best classify or detect objects in 1000 classes from the image net data set. The challenge was not explicitly meant for neural networks, and in fact, the first winners were not deep networks. Actually, the Illest VRC was a series of competitions. The first one took place in 2010 and the last was held in twenty seventeen. In the last one. Twenty nine of the thirty eight competing teams had accuracy greater than 95 percent, so the organizers moved on to more difficult projects. Nevertheless, this competition contributed to a lot of advancements in computer vision. In the next lectures, we will take a look at four different winners of the competition Alex Net, VG, Google Net and Resonate. We will gain an understanding of the underlying concepts behind their design as we go forward. See you there and thanks for watching.

49.  

In this video, we will take a closer look at Alex Net, Alex Net was the winning entry of the last Vassy contest in 2012. The fact that a deep learning algorithm had won the competition for the first time made it even more special. For the most part, it was this convolutional network that started the spree of CNN research and architectures. In the following years, Alex Net was larger than previous seasons used for computer vision tasks. It had 60 million parameters, 650000 neurons and took six days to train on to five. Eighty three gigabyte Jebus. Nowadays, this may not sound as impressive. You probably have a better video card right now. For its time, however, the 580 was considered a high end GPU. Today, there are much more complex scenes that can run on faster use very efficiently, even in large datasets. But back in 2012, this was huge. What about the architecture of the network itself, Alex? And it consisted of five convolutional layers, three max pool layers and three fully connected or dense layers. As you can see on the graphic, we first have a convolutional layer followed by a max pool and then repeated again a conveyor and a max pool. This is similar to our architecture for the amnesty example. Then there is a section with three convolutional layers connected directly, one after another, followed by the final max pool. After this, two fully connected layers are placed with a final softmax classifier at the end. Overall, nothing too crazy happening here, just a solid CNN configuration. Of course, that was the very first large scale CNN to have good practical results. At present, you can easily build the Alex net yourself and train it on your own machine. All right. Now let's take a look at the parameters of the different layers. The first convolutional layer consists of 96 kernels of size, eleven by eleven with stride for the second convolution has 256 five by five filters, while the last three all have three by three filters. All the max pool layers have three by three filters at stride to a departure from the usual two by two with stride to note how the filter size decreases as we go deeper into the network from eleven to five to three. Finally, the fully connected layers have four thousand ninety six neurons, with the last one having a thousand as it should return the scores for the one thousand classes. OK, so what made Alex Net successful? Besides its architecture, this network was the first one to use Relu as an activation function. All convolutional layers had RELU activation. In addition, the image data wasn't just fed into the network, but heavy use of data augmentation was employed. Drop-Out was applied to an SGD plus momentum was the chosen optimizer. Remember, that was back then when Adam was still not conceived, while Adam Grad had just been released. Alex Net is not as impressive by today's standards, but it is the first network to prove CNN effectiveness in practice. With that in mind, I challenge you to replicate the Alex net yourself for homework. Next stop VTG. Thanks for watching.

50.  

Continuing on with the different architectures today, we will talk about VTG Net following the success of Alex Net in 2012 in illest Vassy 2013. The winner was a network with basically the same architecture but optimized parameters. Interestingly, the competition held in 2014 made way for the next big leap in science. So what happened? There were two main competitors in that year. Google Net and VTG VTG finished first in localization while Google Net was the winner and classification. Both networks were pretty close in results. We will examine them now, starting with the revolution in 2014 had much to do with a number of layers. While previous generation networks such as Alex Net had around eight layers VG and Google that had 19 and 22 respectively, the additional depth of the network increases the non-linearity of our model and thus its predictive power. So let's see how they managed to achieve that. Here is the graph for Vogue. As expected, there are a lot of convolutional layers stacked together with the occasional max pool. At the end there are the usual three dense layers. So what is it that allows for so many layers? Well, the more observant among you may have noticed that all the convolutional layers have kernels of size three by three, not 11 by 11, seven by seven or five by five, just three by three. That drastically reduces the amounts of weights in a single layer. For example, a single eleven by 11 kernel has one hundred and twenty one weights, while a three by three kernel has only nine weights. So in theory we can stack 12 three by three filters with the same amount of parameters as a single eleven by eleven. Felter. One point to consider here is that the three by three filter gives us a smaller, receptive field. However, that is only true for the first layer. Two such layers actually have a receptive field of five by five, three have seven by seven and so on. That way we can achieve an effective, receptive field of eleven by eleven with only five layers. Ultimately, this network could achieve better results with fewer parameters and in a shorter time span. Despite all this, it still requires ninety six megabytes of memory per image on the forward pass, and it's more than one hundred and thirty eight million parameters. Interestingly, the majority of those parameters, around 100 million, are contained in the first fully connected layer. That is huge. Overall, the VG network is an improvement, but it is still not as computationally efficient as one would assume. And here comes Google Net. More on that in the next lecture. Thanks for watching.

51.   

 Let's pick up where we left off. In the 2014 edition of the Illest Vassy competition, Google had performed the best in the classification task. The team behind it achieved this results by having a paradigm shift. They aim to create a very computationally efficient network. To this day, the CNN remains among the highest in efficiency, and the develop concepts have been copied by many others. All right. So how did the Google engineers accomplish this? To answer this, we need to study Google. Architecture first. Wow. Look at all these connections and multiple modules per layer. How can such a convoluted architecture be efficient? Let's try to unravel this beast. This network has 22 layers. That's six more than 16 and three more than VTG 19, inspired by a paper called Network in Network or nine Google that also uses one by one convolution and global average pooling in the spirit of computational efficiency. The global average pooling replaces the fully connected layers at the end, which contain the majority of weights and a network. Thus, the total number of parameters of this system is twelve times less as compared to that of Alex Net. But the main feature of this net are these sections here. As you can see, the architecture consists of this section repeated and stacked on top of each other. This is called an Inception module. Here's a fun fact about the origin of its name. The authors have admitted that the name Inception was indeed inspired by the non-paper and the famous Internet meme. We need to go deeper from the movie. Inception just goes to show that these high profile engineers are normal people just like us. OK, now back to machine learning. So let's examine this Inception module more closely. The whole concept behind it is to design a good local network topology that is to be a computationally efficient and useful standalone module. The main idea is to be able to stack these modules so that the whole is greater than the sum of its parts. How is that achieved? Well, let's take a look at a reduced version of it, the so-called naïf inception module. We have the inputs coming from the previous layer here. Now, instead of feeding it into the next filter, as usual, we apply it to numerous parallel filters. So the same input gets introduced to a one by one, three by three and five by five convolution, as well as a three by three max pool. Thus we have many different kernel sizes all in the same layer. Then the output of every operation gets concatenated into one tensor depth wise. This output then gets passed on to the next layer or in this case, to the next inception module. As good as it may all sound, this configuration has one big problem. To illustrate it, we will need to calculate how the dimensions get transformed by it. Suppose that our input is twenty eight by twenty eight by 256 and the number of filters in the convolutional layers are one twenty eight one 92 and 96 respectively. For compatability, every convolution in the max pool will preserve their spatial dimensions. So we will only look at the depth. We already know that the depth of the output from a convolutional layer is the same as the number of filters. So that's one twenty eight one 92 and 96 for the convolutions. The pooling returns the same depth as the input, making it 256. Now all those different outputs are concatenated together in depth. So the final output has the same spatial dimensions as the input that is twenty eight by twenty eight, while its depth is the sum of all the other depths. In that case it amounts to six seventy two in the end from an input of twenty eight by twenty eight by 256, we get an output of twenty eight by twenty eight by six seventy two. A significant increase. The total number of operations performed in this module is eight hundred and fifty four million. OK. We see that the problem lies in the fact that this naive Inception module is too computationally expensive. That's exactly the opposite of the goal we are trying to achieve. What can we do about that? The solution is to add three additional one by one convolutional layers like that. These are called bottleneck layers, and their purpose is to reduce the dimensionality and number of operations. All of those have 64 filters. If we calculate how the tensor dimensions transform in this graph, we will produce an output of four hundred and eighty depth. This reduction comes only from the rightmost bottleneck layer, the one after the max pool, as it produces an output with depth equal to its 64 filters instead of the input depth of 256. So if that is the only bottleneck layer that reduces the depth, what is the purpose of the other ones? Well, they do not reduce the output depth. Instead, they reduce the input depth to the convolutions. Thus they are reducing the number of computations needed in this revised Inception module. The total number of operations is three hundred and fifty eight million, down from eight hundred and fifty four million. Great. Now the network is constructed by stacking these mini network modules on top of each other at the beginning of the whole thing. There is a conventional linear section with three conveyors and two pool layers, while at the end we have the usual softmax classification. But what are those two bits sticking out of the inception modules? Those are used only in training. They are auxillary classified outputs that help in the back propagating of the network. Remember, in order to train the network, the result compared to the true target is used to adjust all the weights in the net. They are updated by back propagating the gradient from the end to the beginning. Since this net is deeper and larger, the gradient may start to diminish. As we go further, these auxiliary classification outputs are there to inject additional gradient at lower layers. Fantastic. This architecture looks complicated at first, but once we consider it at a basic level, we are able to understand the rationale behind it. Next stop is the winner of the 2015 competition resonate? Thanks for watching.

52.   

This is the final CNN architecture that we will look into, it is called Resonant and is the winner of the 2015 edition of VRC. In fact, it's swept all classification and detection categories in VRC and Kocho having 152 layers. This network kicked off the revolution of depth. And you've got Google. That's 22 layers were a lot in general. A model with more layers should always perform at least as good as a model with a lower number of layers. That's because a solution by construction is copying the learned layers from the shallower model and then setting additional layers to identity mapping. However, deeper models are generally harder to optimize. So how did the team behind Reginette managed to overcome this issue? Well, they design the network to learn what to change in the input instead of what the output should be. Let me illustrate this. Suppose we want to stack to convolutional layers in a normal model. We would just append them one after the other. So if we have an input X and the two layers transform that input with the function F, the output of this block would be by definition F of X. The job of the network during training is to adjust this function F through the weights in the layers in order to produce the closest output to our target. Thus the network is trying to learn f, which is the desired output. Now let's see how this was implemented in Reginette once more. Here are the two convolutional layers, one after the other in Resonant. They added the input again after the calculations of the conveyers. They called this a residual block. So if the input is X and the two conveyers transform it with the function F, they would produce F of X. But that is not the final output of this block to produce the final output. They added the input again, so it becomes X plus F of X.. So what does this mean? Well, during the training process, the model is trying to learn what F should be. However, in this case, F is no longer the final output. This is just the change we need to make to our input in order to get the desired output. The difference between the two approaches is subtle to make an analogy here. Imagine you are ordering something online and the subtotal is thirty dollars. However, this amount doesn't include taxes based on the information you have provided and the items you've added. The website needs to calculate the respective tax rate and the new price. The question is how would the two different approaches achieve this? Well, a normal model would just output thirty three dollars. That's the final price. It never really learned that the proper rate is 10 percent, which then needs to be added to the subtotal. It just knows that given the current circumstances, a thirty dollars subtotal transforms into a total of thirty three dollars. In contrast, a residual block would say that's three additional dollars because of the 10 percent tax rate. Basically, it cares about how things change, not so much about what they transform into. This works better when generalizing. Why so? Well, it is easier to learn that a person from a particular country would have 10 percent taxes than it is to learn that if the initial price was thirty, the final one would be thirty three. Makes sense. This is obviously an oversimplification of the whole process, but you get the idea in practice. Normal models don't learn in exactly the same way. But it turns out the residual blocks approach works better for most networks with lots of layers. All right. Going back to Resonant, its architecture consisted of many residual blocks stacked on top of each other. In turn, every block consisted of two, three by three convolutional layers. So at the heart of it resonate is just a collection of around 150 stacked convolutional layers wherein every two layers the input is summed with the output. Great. Some of you may already wonder if we could combine the concepts we've discussed so far with resonance. And the answer is yes. And this has actually been done. Oftentimes the residual blocks contain the one by one convolutional bottleneck layers. There is also an Inception version four, which combines resonant with inception modules and much, much more well done with this, our journey through convolutional neural networks comes to an end. Throughout this course, we have been shaping up your knowledge and understanding of the principles at work so that the universe of research papers and articles looks less daunting. Now you have the opportunity to venture out, learn about neural networks and why not even contribute to this growing field with your own research? Speaking for myself, I can definitely say I had a great deal of fun creating this course, and I hope it was a great deal of fun for you to stay safe and expand the boundaries of computer vision. And as always, thanks for watching.

