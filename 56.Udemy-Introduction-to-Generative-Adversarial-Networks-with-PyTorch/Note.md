1.   

[1.png](./images/1.png)
[2.png](./images/2.png)
[3.png](./images/3.png)
[4.png](./images/4.png)
[5.png](./images/5.png)
[6.png](./images/6.png)
[7.png](./images/7.png)
[8.png](./images/8.png)
[9.png](./images/9.png)
[10.png](./images/10.png)
[11.png](./images/11.png)
[12.png](./images/12.png)
[13.png](./images/13.png)
[14.png](./images/14.png)
[15.png](./images/15.png)
[16.png](./images/16.png)
[17.png](./images/17.png)
[18.png](./images/18.png)
[19.png](./images/19.png)
[20.png](./images/20.png)
[21.png](./images/21.png)
[22.png](./images/22.png)
[23.png](./images/23.png)
[24.png](./images/24.png)
[25.png](./images/25.png)
[26.png](./images/26.png)
[27.png](./images/27.png)
[28.png](./images/28.png)
[29.png](./images/29.png)
[30.png](./images/30.png)
[31.png](./images/31.png)
[32.png](./images/32.png)
[33.png](./images/33.png)
[34.png](./images/34.png)
[35.png](./images/35.png)
[36.png](./images/36.png)
[37.png](./images/37.png)
[38.png](./images/38.png)
[39.png](./images/39.png)
[40.png](./images/40.png)
[41.png](./images/41.png)
[42.png](./images/42.png)
[43.png](./images/43.png)
[44.png](./images/44.png)
[45.png](./images/45.png)
[46.png](./images/46.png)
[47.png](./images/47.png)
[48.png](./images/48.png)
[49.png](./images/49.png)
[50.png](./images/50.png)
[51.png](./images/51.png)
[52.png](./images/52.png)
[53.png](./images/53.png)
[54.png](./images/54.png)
[55.png](./images/55.png)
[56.png](./images/56.png)
[57.png](./images/57.png)
[58.png](./images/58.png)
[59.png](./images/59.png)
[60.png](./images/60.png)
[61.png](./images/61.png)
[62.png](./images/62.png)
[63.png](./images/63.png)
[64.png](./images/64.png)
[65.png](./images/65.png)
[66.png](./images/66.png)
[67.png](./images/67.png)
[68.png](./images/68.png)
[69.png](./images/69.png)
[70.png](./images/70.png)
[71.png](./images/71.png)
[72.png](./images/72.png)
[73.png](./images/73.png)
[74.png](./images/74.png)
[75.png](./images/75.png)
[76.png](./images/76.png)
[77.png](./images/77.png)
[78.png](./images/78.png)
[79.png](./images/79.png)

Course Agenda
================

Hello and welcome to a new course on generative adversarial networks. GANs with PyTorch. This is your host Mustafa Comer Dean. My name is Mustafa and I'm a top rated machine learning engineer and I have been in the software development industry for over 10 years. I've been working with many startups on solving challenges in e-commerce. Computer Vision recommender systems and financial trading in the year 2015 Ian Goodfellow, a PhD researcher and a machine learning engineer at Google introduced the concept of generative adversarial networks in which there is a min-max game a competition between generator model and the discriminator model.

The generator is trying to generate images that are very close to real images and the discriminator is trying to find out whether they are real or fake. The idea may seem intuitive but it has a very dramatic effect in the area of deep learning and machine learning and it has introduced and opened the door wide to a lot of applications that go beyond imagination. For example we can see here a video two sequences generated by a generative adversarial network in which it learns the dance the poses, the motion from an input sequence and uses it to generate a completely new random sequence for a different person that may not even exist in real life.

This is only one application of generative adversarial networks. A lot of applications exist in entertainment in games in graphics augmented and virtual reality. And even for generating synthetic data sets for medical imaging and health care very recently PyTorch a deep learning library introduced by Facebook has been the state of the art and the favorite tool for most machine learning and deep learning researchers and implementers who want to quickly prototype their ideas without worrying too much about syntax issues.

PyTorch gives you the flexibility to customize every single aspect of the neural network training starting with the Forward and backward path the gradient the propagation and how the loss function is calculated and optimized. This is our all required features for implementing and generative adversarial networks GANs and if you come through you will find it the most widely adopted Deep Learning library tool for implementing GANs in almost all of the research papers available on GitHub and on arXiv and this course will learn the most commonly used the modules and ideas for GANs and do it have the right balance between theory and implementation will take you from zero to hero by introducing the simple ideas first and then incrementally adding up to those ideas and following a stream line of ideas towards the most advanced and complex implementations of GANs in Section 1 Introduction to PyTorch for GANs we will learn the basics of PyTorch so whether you have used it before or you are coming from a background in TensorFlow or Keras will introduce you to the forward and backward propagation mechanisms the autograd mechanism and how to implement the custom loss functions and section two generate handwritten digits with vanilla GANs.

We will implement to the very first GAN ideas introduced in the paper published by Ian Goodfellow in 2014 and will use it to generate images similar to the ones found in the MNIST dataset that is handwritten digits in section three generate specific digits with conditional GANs. We will learn how to control the output of the generator by adding a condition so that we can get images from a specific class with a specific label. For example when working with MNIST dataset we don't want to get a random digit or a random number but we only want to get number one or number two for example.

We also learn the Wasserstein or the Wasserstein loss function in this paper. A lot of ideas were introduced that help improve the performance and increase the quality of the images generated by the GANs in section four diving deeper with a deep convolutional GAN. we will learn about convolutional networks, strided convolutions transpose convolutions and how to use convolutions for both generators and discriminator. Moreover we introduce the idea of multiple loss functions and how to use them to fine tune the results and increase the accuracy and the quality of the generated images.

I have come across the research papers that you use up to 40 loss functions in order to get a very fine grained fine tuned results from GANs. This is a very essential skill that we will learn together along the way. The GAN model that we will implement in this section will be trained on the UC Birds dataset and will use it to generate images of birds and use multiple loss functions to control the accuracy of their generator. The first loss function will tell whether the image is real or fake.

The other loss function will tell whether the image belongs to the right class that was given as a condition to the discriminator or not. I will see how this improves the accuracy and the quality of the generated images by the generator model in Section 5 generate realistic human faces with progressive GANs. We use one of the revolutionary ideas in the area of GANs that is progressive. GANs this idea was applied on celebrity data set in order to generate very realistic celebrity like human faces even a human being will not be able to tell whether these images are real or fake.

We will introduce the concepts behind progressive GANs and implement a progressive GAN and train it on the celebrities dataset. We will also learn a lot of hints tips and tricks for optimizing the performance and fine tuning the training of GANs in section 6 generate videos from other videos. We will introduce the Vid2Vid GAN introduced by NVIDIA very recently and from which we obtained this animation in the previous slides we will learn how to implement it how it works and how to fine tune it will turn it on the YouTube dance video dataset and we will follow the ideas from the very beginning starting with the U-Net architecture passing by the Pix2Pix GAN and finally learning the Vid2Vid GAN by the end of this course you will have learned a lot of concepts and get your hands on a lot of projects that will help you in your career path and also in your future research I shall be waiting for you in the first section.

Let's get started.

2. PyTorch Forward and Backward Propagation
===========================================

[80.png](./images/80.png)
[81.png](./images/81.png)
[82.png](./images/82.png)

See the notebook `1.Lecture-1-Forward-Backward-Propagation.ipynb` for the code.

Hello and welcome to the first section. Together we shall embark on a journey through the advanced and the complex generative adversarial networks. We will learn the basic building blocks that you can stitch together to build any kind of GAN you dream of. This section is a refresher on the PyTorch deep learning library. But you will find it amusing even if you are already familiar with PyTorch. This section concludes with a video lecture on how to build a custom loss function.

This skill is essential for developing your own GANs and in general Deep Learning Solutions and in the first lecture Pytorch forward and backward propagation, we shall review the very first basics of deep learning and implement LeNet-5 architecture let's get started in order to get the most out of this course. I have a few hints for you. First download the course materials and the coding exercises and open them on another screen as you watch the video lectures.

Feel free to post the lectures and make changes to the code and see how it affects the output. Second there are lots of reading materials that I attach both on slides and in the course notebooks. I believe you can learn a lot by reading through them. So don't miss that experience. Third there would be coding exercises that will not be graded but it will give you experience and challenge your understanding of the content explained in this course. Finally feel free to join the Slack channel of this course to discuss the assignments share ideas ask questions and collaborate with other learners who have taking this course.

The back propagation algorithm is the learning mechanism that allows the machines to reach such astonishing levels of accuracy. It starts with input, inputs represented as a vector x that is fed into the network. The inputs are multiplied by the weights of the synapses between the neurons then the output of the product is summed and passed through a non-linear activation function that allows the neural networks to outperform a traditional machine learning techniques in that we can learn a representation for non-linear manifolds.

This is called the forward path. The results are then compared again to the ground truth responses using an objective function. The loss which is then used to update the weights in the backward path using the chain rule for calculating partial derivatives. I would highly recommend reviewing the chain rule from any reference on elementary calculus in the year 1998. LeCun and his research team published the LeNet-5 architecture for handwritten digits recognition.

The paper is considered one of the pioneering papers that made it possible to reach the state of the art of performance in various deep learning applications. The input image is fed into convolutional blocks, each block consists of a convolution layer, a non-linear activation layer and a max pooling layer. Then the output is fed into a feedforward network and finally through a softmax activation to obtain probability distribution over target labels.

I have attached the reference to the paper so that you can read it and explore many concepts in the now the coding exercise. Let's start by opening a new terminal window and typing change directory C.D.. PacktPub-GANs-PyTorch-2019. And then let's launch Jupyter notebook. This should initialize the shell with a notebook inside the directory where the course material is located. We start with Section 1 we first start by installing the PyTorch library and torchvision library.

You can find the right version for your CPU or GPU and all your operating system using the PyTorch official documentation on the Getting Started page then we run a few tests to make sure that our installation was successful by importing torch and torchvision and checking for the version and checking whether CUDA is enabled or not. In my case I have got a GPU GTX 1050 and that's why I've got cuda.is_available returning true.

Don't worry if you don't have a GPU I will show you how to get that done. Using Google Colab as a nifty feature of Jupyter notebook you can run them remotely using the command on the screen Jupyter notebook that dashboard and you specify the port number and then there's dash no dash browser and then you specify the IP using dash dash IP. I use it when I am running code from my laptop onto the computer a machine that has got the GPU installed.

This way I can move and have more portability when running machine learning models. If you find any trouble when you are installing PyTorch and if you are using windows here you are a reference link for troubleshooting the Windows installation as we have explained in the previous slides we are going to implement LeNet-5 architecture which is a series of convolutional blocks to do that in Python. It is very simple. We import torch. We define a class LeNet-5 that inherits from torch.nn.Module and then inside the constructor we inherit from the superclass using super method and then inside the constructor method.

Def underscore underscore it underscore underscore we trigger the parent the constructor using the super method and then we define the network using torch.nn.Sequential. We divide the network into two modules the convolutional part and the feedforward part. I'll show you in a few steps why with it is done this way. Each convolutional block is defined using three layers: Conv2d, ReLU and MaxPool2d. It is worth mentioning that activation function ReLU.

It's not explicitly mentioned in the paper or originally published by Yann LeCun but it is a convenient to add this non-linear activation to the network to make the layers more robust and versatile itchy convolutional layer takes as a parameter the number of input channels in the first case. Since we are training a grayscale image the in-channels number is 1 and then we use 6 filters or 6 layers 6 channels out-channels and we define the kernel size to be 5 by 5 as instructed in the original paper with a stride of 1 and we instruct the bias to be true.

That means this layer will learn both the filter values for the convolutional layers and the bias added to them. Next we apply a non-linear activation, ReLU. And finally a MaxPool2d of kernel size 2 by two and a stride of 2. We do the same for the next convolutional block but with 16 filters and then we have a third convolutional block with 120 filters and the output will be passed from here onto the feed forward network the feed forward network self.fc is a torch.nn.Sequential layer with two linear layers.

The first one is 120 by 84 and the second one is eighty four by 10. Finally we use a softmax activation to get the probability distribution of the outputs. Now that we have defined the structure of the network we have to define the mechanism by which the forward path will be implemented. We defined that using a method def forward which takes the self as the parameter and then the batch which is the images that to be propagated through the network forward and backward.

First we pass the batch through the convolutional block. We get the output return ret and we change the dimensions because the output will be three dimensional because convolution and output is almost three dimensional. So we use the ret.view and we pass it the batch dimension which is batch that size of zero. That is the number of samples in this batch the learning. Usually we use 32 for the batch size and we use negative ones so that PyTorch figures out the rest of the dimensions on its own.

By here we know that the output would be two dimensional. The batch size by the output from the last convolutional layer of the convolutional block. And then we take the return rate pass it through the feed forward network and return the output because PyTorch does not implement a flatten layer just like the one we have in Keras or in TensorFlow. That's why inside the forward path we have to divide the convolutional blocks and the feedforward blocks into two different blocks and in between we do the flat in using the view method is upon us.

Hint visualization is very critical in implementing and debugging a deep learning models. So I suggest this library hidden layer and destroy it using pip install graphviz and hidden layer and then import hidden layer and the build graph and I pass the new object from LeNet-5 architecture we have just implemented and we pass a patch of 1 sample and each image is 1 channel grayscale and 32 by 32 in width and height and then when we run this code it prints a visualization of the network that we've just implemented we can see you starts with 5 by 5 convolution 2 by 2 pooling, ReLU, to another convolution of block and it goes on.

Interesting expand see on the left side the gradient path or the directed acyclic graph of the gradients which starts with constant in shape and then gathers squeeze and squeeze and concatenation finally then we go to reshape this is because we have used inside the forward path the method to view with the negative one dimension. So this when we do this we we append new nodes to the graph of the network that PyTorch applies internally to smooth in its operations as we explain in the next lecture the network goes on until we reach the softmax layer at the output.

I would suggest that you play around with hidden layer. I have attached the reference to the documentation of this a library and you can use it for visualizing gradients of visualizing GAN loss and accuracy as well. Yann LeCun, head of AI research at Facebook is a noble prize laureate and Turing Award winner. I personally follow him on both Twitter and Facebook to stay up to date on advancements in the field. This animation illustrates the performance of LeNet-5 on handwritten digits.

Here you are a short link to the course of discussions. Thank you and see you in the next video lecture.


3.  PyTorch Autograd Mechanism
=============================

See the notebook `2.Lecture-2-Autograd.ipynb` for the code.

[83.png](./images/83.png)
[84.png](./images/84.png)
[85.png](./images/85.png)
[86.png](./images/86.png)
[87.png](./images/87.png)
[88.png](./images/88.png)
[89.png](./images/89.png)
[90.png](./images/90.png)
[91.png](./images/91.png)
[92.png](./images/92.png)


Hello and welcome. And this video lecture PyTorch autograd mechanism we talk in-depth about the way PyTorch updates its weights and calculates the gradients. This is an essential principle if you want to understand and design your own novel Deep Learning ideas. Let's get started automatic differentiation is an algorithm that allows computers to perform derivations of complex formulas. There are two modes of operation for automatic differentiation.

The forward accumulation which is illustrated in this slide. It starts by representing the formula as a directed acyclic graph (DAG) and traverses the graph from the bottom to the top. Each node represents an operation such as addition or multiplication. Each edge represents the dependency in the calculations the very top node which is the output is represented in terms of the derivatives of its parent nodes and the same applies for each intermediate node in practice forward accumulation is less efficient.

So the reverse accumulation is the one commonly used and implemented. In this case the graph is traversed from the top to the bottom. The outputs are readily available at each node without having to evaluate the lower nodes. That's why back propagation can benefit from this behavior and the calculations can be performed much faster. Now let's discuss the automatic differentiation APIs in PyTorch. Let's start with torch.nn.Module. This is a wrapper for deep learning layers and functions that maintains the state.

The state here means the gradients of the variables the gradients are automatically calculated for us next. torch.nn.functional is the underlying functionality formulas and equations to be performed. It is mostly implemented in PyTorch or C language for efficiency but it doesn't have a state that means you have to maintain the gradients manually. Next we have torch.nn.Parameter which is a wrapper for trainable variables such as weights bias and convolution filters values each tensor in PyTorch has a requires_grad attribute which instructs Python to maintain a graph to calculate gradients for these variable.

Each tensor has a grad attribute that is populated with the gradients of this tensor. When they are calculated on the other hand grad_fn is a derivative formula used to calculate the gradients for this tensor. Now let's understand the autograd mechanism using a very simple example. We started by importing numpy as np and we import torch.nn.functional as F this is a convenient use by many developers to import this module.

Then we define it tensor using torch.from_numpy that will import to the tensor from a NumPy array into a torch tensor representation with defined NumPy array to have four samples. And if you focus on these samples you find out that these samples belong to the XOR problem 0 6 0 0 0 0 x or 1 is 1 1 x or 0 is 1 1 x or 1 is 0 and we define the type to be numpy.float32. So this represents a dataset set a very simple data set so that we can understand the autograd mechanism was a very simple flexible example and then we defined the outputs of the ground truth which is 0 1 1 0 using the same way we have converted it from NumPy to PyTorch tensor.

So assume that we've got only one neuron with two inputs so that neuron will have weights 1 by 2 or 2 by 1. We have done it this way so that the product is correctly calculated so we reverse it one instead of two and then we define weights. That requires_grad equals True we have set the attribute that instructs PyTorch to apply the gradient mechanism to this tensor another tensor for this one neural network is a bias we initialize it using torch.randn and here we've got only one neuron with 1 bias and we pass requires_grad as a parameter to the constructor.

So you've got two ways to instruct PyTorch to use the gradient for a tensor either by setting that attribute explicitly or by passing it as a parameter to the constructor of a tensor now we use F.linear which applies a dot product between the inputs and the weights and the bias and you have both the inputs the weights and bias and we've got the result which is four elements for each input sample we can see here. Also that as a parameter of the tensor we've got grad_fn which instructs the kind of formula or the function used to calculate the gradient for this tensor so we do the same but this time we save the output in a variable named preds for predictions and we calculate a loss.

A very simple L1 norm which is an absolute error loss function by subtracting outputs minus predictions or preds and then we get the mean value for this loss. So if we expect that the attribute of preds as a tensor grad_fn then we see the formula or the function used for calculating the gradient. Another one which is the mean is used to calculate the gradient of the loss. Here we can see that for each tensor there is a different gradient function.

And then if we print the weights.grad we get nothing because the gradient is not populated yet. How do we populate the gradient. We use loss.backward. This will initiate the backward path or the back propagation or the reverse mode of the auto differentiation algorithm to calculate and update the gradients for each layer. Now if we print weights.grad we see that it has calculated the derivative for each of the inputs to be negative half.

Of course this calculation is not very accurate and this will not be used to get the optimal results on the X or dataset. But now you can see and visualize the mechanism of the autograd algorithm. You can try to play around with the bias and its gradients as well. Another thing that you can observe here is that we've got two values for the gradient for the weights because here we've got one neuron and two input nodes. So for connections between the two elements of the input vector and the one you we have got two synapses or two edges and each one of them has a weight.

From here you can see why you have got two values for weights. Thank you. And see you in the next lecture.


4. PyTorch Custom Loss Function
===============================

See the notebook `3.Lecture-3-Custom-Loss-Function.ipynb` for the code.

[93.png](./images/93.png)


Hello and welcome to a new lecture, PyTorch custom loss function in this section we learn an essential a skill that is going to prove beneficial in almost all aspects of deep learning the ability to design your own loss functions will empower you to build advanced networks that break the norms of the field. Let's get started in a research paper published in 2017 studying the effect of the loss function choice on the accuracy in deep learning as in the attached reference on the screen.

The authors have tested many less frequently used loss functions and compared them to the more common loss functions such as mean square error and log loss in information theory. The concepts of entropy and cross entropy describe the amount of information held in a particular bit and the probability distribution describing such information. The information theoretic learning makes use of these concepts in order to learn the weights of the model that best describe a phenomenon.

Cauchy-Schwarz loss function is an example of a distance function between two probability distributions. The first distribution is the one obtained by the softmax layer at the output layer of a deep learning model and the second distribution. It is the one related to the one-hot encoded target labels of the given samples the mathematics may seem scary at the beginning but it's very simple. The numerator is a product between the outputs and the ground truth labels and the denominator serves as a normalization to downscale the value the logarithm is used to shrink the value range further in finally a negative sign is used to inverse the values and convert the objective into a minimization optimization.

Hence it is very convenient to minimize the loss function in almost all the reference papers in the literature. So in a nutshell this function describes the distance between the probability distributions of both the model outputs and the ground truth labels and through the training process. We try to minimize such a distance. Next we implement a custom loss function for training. The LeNet-5 architecture on the MNIST dataset. We start by importing torchvision and then we initialize the dataset from torchvision.

torchvision.datasets.MNIST we specify the directory for the dataset to be downloaded slash data slash mnist. We set this first variable for the training set by specifying trained parameter to equal to and then we specify the download variable to equal to so that it can download the image dataset from the cloud. And then we apply the transformations array that will be applied for every sample of the dataset. In our case we apply only padding to increase the size from 28 by 28 to 32 by 32 by padding 2 pixels at each dimension.

And then we convert the image to a tensor that will bring the image in the values between 0 and 1. Originally it is in the range between 0 and 255 but applying to tensor will convert it to a byte tensor in the range of between 0 and 1. Feel free to apply any preprocessing function you feel it will improve the performance. You can apply and normalize. You can apply center crop. You can apply any other of the available transformations or even a lambda transform that you define on your own.

I would encourage you to play around with the transformations and observe the performance boost or degradation as a result. Next we define another variable for the testing dataset and we apply the same except here we set the parameter train equals False. Next we specify the batch size to be 32. I would encourage you to play with different values 32. It sounds like a most convenient value to be used when you are not sure what value to use or if it is not explicitly referenced in the original paper.

And then we use another utility function from PyTorch which is a data loader and we pass their variables we have created in the previous a step for the dataset. into this data loader and this data loader is the actual function that will generate the generators. The Python generators that will yield the samples in batches from the data set. This allows PyTorch to efficiently perform data loading without loading too much data into the memory with specified parameters to a data loader.

The variable for the training set the batch size and we specify whether shuffling should be true or false for the training set. I would set shuffle to be equal to True and then we set the number of workers to 4 depending on how many workers you have got on your CPU or GPU. We do the same for the testing set except this time with set shuffle equals false as it should become clear in the next steps. Why was it shuffle equals True for training and equals false for testing.

Is that because the generator mechanism of Python it keeps looping over and over again without stopping over the data set. And here we get the concept of an epoch and the epoch. We've got the concept of a batch and step so simply as a batch is 32 samples and the step is iterating over one batch and then inside the epoch we iterate over steps so we can literally iterate over many steps in one epoch not necessarily the number of steps required to cover the dataset in its entirety.

We can go beyond the dataset size here. That's why we can shuffle the dataset and we get random sample at each step or we can set the number of steps not to cover the entire dataset. Maybe only 30 percent 70 percent or so. This is a flexibility that can prove useful for you later in your career. We don't do that for the testing because we want to test the entire testing set and see how our algorithm or model is performing on the testing set. In this case we use torch.cuda.is_available to specify the device.

Our training will be performed on don't worry if you don't have a GPU you can perform that on you but it will take longer and the code will run smoothly without errors. Since we have a defined device to be generic regardless of what happens next. Now we reach the climax of this section by implementing the custom loss function the Cauchy-Schwarz divergence. we start by defining an encode one-hot method that will convert a label from a single digit to a one-hot encoded vector of zeroes and only one in place of the target label.

For example if their target label is the digit number three then we will get an array with 0 0 0 1 and zeroes for the remaining digits and so on. So we start by converting that tensor from the GPU to the CPU using .cpu(). And then you convert it to NumPy and then we use syntactic sugar from numpy so that we can convert the digit into a numpy array of one-hot encoding by initializing an array numpy.zeros all the values would be those that would be the size in the first dimension of the batch size.

And then in the columns we've got 10 columns for 10 digits in this dataset and then we use the third line. Line number six B of numpy.arange. Edit size and comma eight that will say for every element in this batch and for every column that is set as a target 3 5 6 and so on we set the value to 1 the rest to remain zeros. Finally we convert it from number to bytes tensor using torch.from_numpy. And then we convert it to floating points that float.

And finally we converted back to the GPU or CPU using the two device we will see why this method is useful as we implement the Cauchy-Schwarz divergence loss function so over here we define a class CSD standing for Cauchy-Schwarz divergence we make it so that it inherits from torch.nn.Module in the constructor we invoke that constructor of the parent using super method and then in the forward path we pass in the outputs from the model and the ground truth target next we define a variable Y which is the one-hot encoded vector of the target labels and here if you're coming from Keras or TensorFlow background you find it a little bit weird because you have got one-hot encoding layer in Keras and TensorFlow but this is not the case in PyTorch and you have to implement that in your loss functions depending on your logic and how it is performed I know we apply the formula from the slide denominator is defined the numerator which is the sum of the dot product between the outputs and the target label y transpose so that we can perform the dot product and the sum is performed along the vertical dimension the columns that's why we set the dim dimension equals one for the denominator it is simply the L2 norm and we can do that using torch.norm and passing the variable outputs and comma 2 for L2 norm, 1 for L1 norm and so on.

and we multiply it by the L2 norm of their target labels Now I have got both the numerator and the denominator calculated we apply the logarithm natural logarithm using torch.log numerator over denominator and we multiply the output by negative 1. And finally because we need to apply the backward using only single value we use torch.mean that will return a scalar value one single element for all this batch and propagate the loss through backwards here we need to know two observations the first observation is that if we are applying a custom loss function that is not continuous then we have to define a custom backward method and then backward method is defined the same way we've done for the forward instead of passing the outputs and the target we pass the gradient from the previous layer and we perform as a special case for calculating the derivative and then we return to the new gradient.

The other observation is that this method may not be as fast as the built in methods in PyTorch because they are inherently built using C language or a torch language but we have optimized it by making sure that all our operations are performed as multiplications or additions or divisions using the basic built-in methods efficiently implemented by PyTorch now in order to perform the model training we define an object of the LeNet-5 architecture and we convert it to the device we are using GPU and we name it network next we define the optimizer that will perform the actual update of the variables how much will it update the variables after each forward and backward pass and even we defined the network parameters at the variables to be optimized and the learning rate to be 0.05.

I would encourage you to play around with the learning rate and see whether you've have got better results using a smaller or higher learning rate. Next we define the loss function which is criterion which is an object of the class we have just implemented Cauchy-Schwarz divergence. Again try to play around with different loss functions implement your own loss functions and use built in loss functions in Python. Next we perform the training in the form of epochs inside each epoch we need to make observations of three steps.

The first step here when we convert the data set to the device we are using for the training, GPU or CPU. The second step here when we initialize the gradients to be zero for the optimizer so that we are not accumulating gradients from previous epochs. And finally the third step here. We apply the backward pass for the loss function and then we run the optimizer step so that it can update the weights. You can see here that I have trained this network for 120 epochs and the loss has been decreasing from epoch to epoch now that we have trained the network we need to evaluate the performance on the testing dataset we do that by setting network.train to False so that any operation that we are going to perform next will not affect the actual network and the weights that have been updated as extra guarantee that we are not performing any backward.

We use torch.no_grad so that no gradients would be computed and updated. And next we calculate both the loss and the accuracy. I have trained them network and I evaluated it and I've got a loss of one point fifteen and the accuracy of ninety two point twenty percent and I am very sure that you can do better and the assignments and share your results with your colleagues and let's see who gets the highest performance on this data set.

Here you'll find reference to all the papers that I have mentioned throughout this section and over here at the very bottom of the notebook you find a reference of the Google Colab notebook file of this exercise so that you can run it on Google Colab using GPU and TPU and obtain the same results. If you don't have a GPU at home in this YouTube video I have reviewed many examples of loss functions and I hope you find it useful another influential professional in deep learning is Aurélien Géron and I would recommend you to watch his video on information theory concepts such as entropy and cross entropy in order to get a deeper understanding of the concept.

You could also follow him on Twitter for continuous updates in the field. This is an optional assignment and I would like you to read the paper on loss functions and pick one of the discussed loss functions and implement it based on what you have learned in this section. Feel free to discuss the assignments on this lecture congratulations you have made it through Section 1. See you in Section 2 where the fun begins. Thank you.


5.  

[94.png](./images/94.png)
[95.png](./images/95.png)
[96.png](./images/96.png)
[97.png](./images/97.png)
[98.png](./images/98.png)
[99.png](./images/99.png)
[100.png](./images/100.png)
[101.png](./images/101.png)
[102.png](./images/102.png)
[103.png](./images/103.png)

-- Introduction to GANs
=======================

Hello and welcome to a new section in this section generate handwritten digits with Vanilla GANs. You should learn the basics of GANs and implement your first one based on the paper published by Ian Goodfellow in the first lecture. Introduction to GANs. I shall review a quick historical background of generative models and introduce the first building blocks of GAN models. Then I shall share with you some amazing applications of GANs. There has been many attempts to train models that can learn a probability density function from a training set and then generate new samples that are drawn from the same distribution methods such as Markov chains Boltzmann machine deep belief nets and variational autoencoders.

These methods were lacking in one aspect or another. Some were not analytically solvable and required settling for an approximate lower bound as a work around while others were proven to overfitting variational autoencoders were ahead of them all with impressive results. And at the same time had a sound mathematical explanation generative models are designed to solve the problem of probability density approximation. The problem is best of visualized by this diagram.

The blue color indicates a probability density function for one dimensional data set. The green line on the other hand represents the probability density function of the samples generated from a statistical model the generative model is trained iteratively to make the density of its generated samples as close as possible to that of the original data set. In simple words the model converges when the green curve overlaps the blue curve in the year 2014 and Goodfellow and his team proposed a novel approach to generative modelling.

It is by far one of the major breakthroughs in the field of deep learning. The idea is very intuitive. They train two adversarial models. One is named the generator and the other is a discriminator The generator is trying to generate samples that look as real as possible. On the other hand the discriminator is trying to find out whether the samples are real or fake The generator is given noise as input drawn from a random distribution. Its job is to learn a mapping from a random distribution to the original data distribution.

The discriminator is given as input during training both real and generated samples the output of the discriminator is a zero for fake images or one for real images generative adversarial networks caused a very strong vibe on their release. Next we review some of the entertaining applications of GANs can be used to generate artistic paintings. These paintings are for sale or 9 GANs that come. You can see that these paintings cannot really be distinguished from the ones painted by human artists.

Which face is real that come is a game where you are given two human faces. One of them is real and the other is generated by a GAN. Then you are asked to click which one is the real one. I must tell you that I get confused and choose the wrong one sometimes. I think you should give it a try Nvidia has been pushing the frontiers of deep learning applications in general and especially GANs on the NVIDIA GauGAN playground you can find them where you draw regions with a brush and choose category for each region such as clouds, land, snow, forest or mountains.

Then the model will generate natural scenes based on your drawing the link is on the screen. If you'd like to give it a try retail business is one of the most potential areas for applications of GANs. There are many attempts to revolutionize the shopping experience. In this YouTube video you can see how games are used to generate images of models wearing fashion items. The poses, the motion the faces is all generated by a GAN and the next video lecture.

we shall discuss the GAN loss function in detail. Thank you and see you in the next lecture.

6.  

-- Working of GAN Loss Function
===============================

[104.png](./images/104.png)
[105.png](./images/105.png)
[106.png](./images/106.png)
[107.png](./images/107.png)
[108.png](./images/108.png)
[109.png](./images/109.png)
[110.png](./images/110.png)
[111.png](./images/111.png)
[112.png](./images/112.png)
[113.png](./images/113.png)
[114.png](./images/114.png)
[115.png](./images/115.png)
[116.png](./images/116.png)
[117.png](./images/117.png)
[118.png](./images/118.png)


Hello and welcome to a new lecture. In this lecture. Working of the GAN loss function. We shall discuss the min-max game the training pattern of GANs maxout activation functions and a quick review of GANs evaluation methods generative adversarial networks can be formulated mathematically as a min-max game the generator is trying to minimize the number of samples that a discriminator detects on the other hand the discriminator is trying to maximize the number of fake samples it detects in this equation x denotes real samples and Z denotes noise so d of X is discriminator output for real samples and D of G of Z is discriminator output for generated samples since the output for generated samples is expected to be zero we subtract it from 1 to make the formula correct.

It is however found in practice that the discriminator can easily find out fake samples at the beginning of training hence the gradients are almost zero and the generator cannot update its weights to match the efficiency of the discriminator because of this gradient saturation problem is solved by changing the objective of the generator that is by making the generator maximize the probability that its output is not detected by the discriminator. This way the generator has a better chance of learning in the graph below the blue curve represents the gradients in the case of min max formulation which is almost plateau for the first training iterations and the green curve represents the new gradient which is of higher values you may ask what if the generator is trained first then the discriminator afterwards.

This would cause a short circuit and the discriminator will learn to mark all generator outputs as fake both the generator and the discriminator are trained in tandem the discriminator is trained first for K steps where key is an integer in the original paper Key was equal to 1 but it is found in practice that K equals 4 yields much better results. That is the discriminator is trained for four steps and then the generator is trained once the training ends when the probability density function of the generator is equal to that of the original data set all the mathematical equations on the screen right now are equivalent for those who are interested in the mathematical proof of convergence I have put a reference to the paper at the end of this section in an earlier paper published by Ian Goodfellow and his team they contributed a novel activation function.

maxout activation which is a generalization of ReLU and Leaky ReLU activation. These are the ones we have used in the first section. maxout is a more robust and avoids overfitting. It does not also suffer from vanishing gradient problem. The downside however is that the number of parameters is doubled for every single neuron which leads to a high number of parameters maxout activation is the one used by Ian Goodfellow and his team in the original paper of GANs and many other papers published afterwards.

That's why it is good for us to know how it works the red curve represents the output of a maxout activation which is given two linear functions. In this case the green and the blue lines it is also called piecewise linear activation. This has the advantage overall since it has steeper slopes which allow for better gradient flow in the network. Let's review a quick example of maxout in this example we have a hidden layer of a network and the maxout is performed with k equals 2.

That is the max is taking over every couple of neurons. Think of maxout as another hidden layer that is only connected to K units of previous layer and that performs max over them. Now let's move to another idea which is the evaluation of generative models. There are a few ways to evaluate the results of GANs. Some of these methods also work for generative models in general but each has its own downside. For example Parzen window which is a kernel based density estimator fails to capture high dimensional features such as of images.

Second the Euclidean distance this one is prone to overfitting since the generated images don't have to be exactly the same like the original ones. Third we have maximum likelihood estimation which in most generative models is intractable. That's another way of saying that it cannot be solved analytically. Maximum Likelihood Estimation works by giving the wrong benchmark. The downside of maximum likelihood estimation is apparent when it gives it the wrong benchmark for a lower bound.

For example if we have one model and that one has a lower bound on its value and this lower bound is not used on the other model which we are comparing. In this case we will not be comparing an apple to an apple. The last evaluation metric on our list today is the Inception score which can be misleading as well because it is relevant for images similar to ImageNet dataset but irrelevant. Otherwise that's why the evaluation of generative models is an open area of research in the next lecture.

We shall get our hands dirty and implement our first GAN. Thank you and see you in the next lecture.

7.  

[119.png](./images/119.png)
[120.png](./images/120.png)
[121.png](./images/121.png)
[122.png](./images/122.png)
[123.png](./images/123.png)
[124.png](./images/124.png)
[125.png](./images/125.png)
[126.png](./images/126.png)
[127.png](./images/127.png)
[128.png](./images/128.png)
[129.png](./images/129.png)
[130.png](./images/130.png)
[131.png](./images/131.png)
[132.png](./images/132.png)
[133.png](./images/133.png)
[134.png](./images/134.png)
[135.png](./images/135.png)
[136.png](./images/136.png)
[137.png](./images/137.png)
[138.png](./images/138.png)
[139.png](./images/139.png)
[140.png](./images/140.png)
[141.png](./images/141.png)
[142.png](./images/142.png)
[143.png](./images/143.png)
[144.png](./images/144.png)
[145.png](./images/145.png)
[146.png](./images/146.png)
[147.png](./images/147.png)
[148.png](./images/148.png)
[149.png](./images/149.png)
[150.png](./images/150.png)
[151.png](./images/151.png)
[152.png](./images/152.png)
[153.png](./images/153.png)
[154.png](./images/154.png)
[155.png](./images/155.png)
[156.png](./images/156.png)
[157.png](./images/157.png)
[158.png](./images/158.png)
[159.png](./images/159.png)
[160.png](./images/160.png)
[161.png](./images/161.png)
[162.png](./images/162.png)
[163.png](./images/163.png)
[164.png](./images/164.png)
[165.png](./images/165.png)
[166.png](./images/166.png)
[167.png](./images/167.png)
[168.png](./images/168.png)
[169.png](./images/169.png)
[170.png](./images/170.png)
[171.png](./images/171.png)
[172.png](./images/172.png)
[173.png](./images/173.png)
[174.png](./images/174.png)
[175.png](./images/175.png)
[176.png](./images/176.png)
[177.png](./images/177.png)
[178.png](./images/178.png)
[179.png](./images/179.png)
[180.png](./images/180.png)
[181.png](./images/181.png)
[182.png](./images/182.png)
[183.png](./images/183.png)
[184.png](./images/184.png)
[185.png](./images/185.png)
[186.png](./images/186.png)
[187.png](./images/187.png)
[188.png](./images/188.png)
[189.png](./images/189.png)
[190.png](./images/190.png)                               
[191.png](./images/191.png)


-- Implementing GAN Training Methodology
========================================

Hello and welcome to a new lecture. In this lecture we are going to implement the base GAN model and apply the training methodology. Let's get started we start by opening the Jupyter notebook for this section. Section 2 which is available in the course material. We start by importing the necessary libraries torch, numpy and matplotlib. And don't forget to use the directive matplotlib inline so that the figures will be rendered inside the Jupyter notebook next to it make sure that CUDA is available.

If you've got CUDA installed and you've got a GPU. And if you don't have a GPU don't worry. You can always use Google Colab which is available at the end of this section a reference to the same code on Google Colab afterwards which defines a device. If CUDA is available the device would be the GPU cuda device. Otherwise we use the CPU. This is important to ensure that the code runs whatever you have as a GPU or you don't have a GPU know we define a global variable noise_dim for noise dimension.

This defines the length of the vector of the noise that will be given to the generator so that the generator will use that noise which is sampled from the random distribution Gaussian distribution to map the random distribution to the data distribution available in the training set. In this case MNIST dataset it is convenient to define the noise dimension to be 100. You can play around with other values as well next to define the generator model similar to what we have done in Section 1.

We define a class generator which inherits from torch.nn.Module inside the constructor def underscore underscore and it underscore underscore we invoke or trigger the constructor of the superclass by using the method super and then we define the sequential model which is the actual model design we are using in this case. We have three fully connected layers. The first layer is linear which is a fully connected dense layer which has got twelve hundred or 1200 neurons and then followed by ReLU activation function then followed by a dropout which is used for regularization.

Remember it acts like bagging or an ensemble of models. This helps the model through training to choose the best one by deactivating some of the links connecting some neurons in the hidden layers. Finally the last fully connected layer is given sigmoid activation which is a squashing function which squashes the output to be either 0 or 1. Remember that when we import the MNIST dataset we converted the data to a tensor and that automatically converts the pixel intensity values from 0 to 255 down to from 0 to 1.

And this way when we apply the sigmoid activation we ensure that we always get output of the generator for the pixel values between 0 and 1 and that will act similar to the original samples available in the data set. Finally inside the forward method we defined the operations to be carried out during the forward pass. We reshape the model to get a two dimensional matrix that's of the batch size and then of the other size by using the negative one to mean whatever is the next size going to be.

And then we pass the batch through the fully connected network which we have defined above. Finally we return to the output now let's define the maxout activation function which we have discussed in the slides in the earlier lecture again we define a class maxout inheriting from torch.nn.Module in the constructor we invoke the superclass and then we set the attribute of the class self.num_pieces to be equal to the number of pieces passed as a parameter to the constructor and then inside the forward method we define the operations to be carried out through the forward pass remember forward method is given a batch and the batch in this case is two dimensional matrix.

Since we are working with flattened images in future sections we may deal with three dimensional and four dimensional tensors but this is not the case in this example. First we start by using the method assert which throws an error. If the condition given does not hold in this case we make sure that the second dimension, that is the shape at index one of the batch, is divisible by the number of pieces which we assigned in the constructor. For example if we have got an input the second dimension is 625 and then we have got the number of pieces to be five.

We ensure that 625 modulus 5 is equal 0. Otherwise it throws an error. Next we reshape the input batch to be four dimensional. The first dimension will be the shape of the batch size. The second dimension we divide the original second dimension by the number of pieces. And here we use double slashes which means integer division. The third dimension is the number of pieces. So for example if we have got a batch of 64 samples and then we have got the second dimension of 625 then the first dimension after we reshape the input tensor will be 64.

The second dimension will be 625 divided by five which is one hundred twenty five. The third dimension will be five and then if we've got the remaining dimension we use them. This is only used to make the method more generic and applied for other high dimensional tensors. Next we use the max method available in PyTorch. We use ret.max along the third dimension dimension equal to 2 because the counting starts from zero. So too is a third dimension so that will return the maximum value along with the number of pieces that will give us a tensor of dimensions batch size multiplied by hundred twenty five.

If the original dimension was 625 which is the original dimension divided by the number of pieces and then we return to the output next we design the discriminator model similar to the generator we inherit from torch.nn.Module in the constructor we define the sequential model fully connected layers. But be aware here that we are using fully connected layer and maxout activation and then fully connected layer. So the out features are from the first fully connected layer 625 the maxout number of pieces is 5 then the input features for the next fully connected layer is 125 and then we repeat again by making the out features 625 and maxout activation and then another fully connected layer and so on.

Finally we apply a sigmoid activation function to do the squashing but this time we mean by zero is interpreted as a fake sample a generated sample and one is a real sample, a sample that's available in the training set or a generated sample that cannot be distinguished from the original dataset. Finally we defined that forward method and apply the same re shape to get 2 dimensional vector or tensor and then we apply the fully connected network on the batch and return the output now that we have defined the model and activations in the next lecture we continue the implementation and apply the training methodology and see the output.

Thank you and see you in the next lecture.

8.   

-- Implement Vanilla GAN on MNIST Dataset to Generate Digits
============================================================

Hello and welcome to a new lecture. In this lecture we're going to continue the implementation of the base GAN model and apply the training methodology. Let's get started in the previous lecture we ended by designing the model for the generator and the model for the discriminator and we applied the maxout activation function and the other details of the model designs so we continue by importing torchvision which contains the data set. Next we define the flattening transform which is a transformer to convert the two dimensional images to one dimensional flat vector and then we import to the images data set from torchvision and apply the transformers in the following order.

First we convert to tensor using torchvision.transforms.ToTensor which converts the intensity from the range 0 to 255 down to 0 to 1 and returns a byte tensor. And then we apply a flatten transform next we define a global variable the batch size I set it to 32. You can play around with the value in order to fine tune the hyperparameters and then will define a data loader which uses the data train from the torchvision dataset we have defined above and is given the batch size and we don't forget that we set shuffle to True and we set the number of workers I set it to 4 continue to 2 or 1 as you like.

Next we do the actual GAN training we started by creating a new instance of the generator and the discriminator models and we convert them to the device we are working on. Now we get an object in a variable generator and discriminator next we define the optimizer remember that we need an optimizer for the discriminator and another one for the generator. And you can set a different learning rate for each one of them and different momentum. In this case we are using stochastic gradient descent and we pass the model parameters either the discriminator or the generator and then we set the learning rate.

In this case it was set to 0.02 and then we set the momentum to 0.5. Next we defined the loss function. We use a loss function, criterion, which is binary cross entropy loss now things get more interesting. We define the real labels. That's when we say that the sample is real to be one and we define the fake label when we say that the sample is fake to be zero. Remember that torch.ones returns a matrix of the batch size and one dimensional column and then we convert it to the device so that it is converted either to the CPU or the GPU for the fake labels we use torch.zeros returns two dimensional matrix of zeros the first dimension is the batch size and the second dimension is one column.

Next we define a variable test underscore set. This is going to be a fixed noise that we are going to test along the training epochs so that we see for the same noise how the output of the generator is different. This is kind of a consistent way to see how the network evolves with time test_set is a variable of torch.randn that's random normal. The first dimension is 16. So we are using 16 samples and then the second dimension is the noise dimension.

Finally we convert it to the device we are working on, the GPU. Next we set the number of epochs to 512 and the number of steps to be the length of the train loader divided by integer division. The batch size since we are going to be creating visuals of the output of the generator for each epoch. Then we create a directory. If it doesn't exist by importing os and then if not os.path.exists. Visuals the name of the directory then we use the method makedir so that we can create the directory.

Here is the actual training for each epoch in the range of number of epochs. That's five hundred twelve and then for each step of the train loader we enumerate the train loader. Remember that it is a generator enumerate will return the index of the iteration variable i. And then the images and the labels. Since we are not interested in the labels in this exercise we use underscore and we get the images in the variable images. If I is equal to the number of steps then we break the loop since the train loader generator will not stop automatically it will keep on going on and on fetching more data.

Actually the same data but will cycle over them since it is a generator. Next we perform the actual training of the discriminator. Remember that we can train the discriminator more than the generator so we use a variable key. In this case I set it to 8 for underscore we are not interested in the index of the iteration, and range it so that is K. Which we have discussed in the lecture slides. We perform this training. The real images are going to be converted to the device by using images to device and then the fake images will come from the generator.

At this point we have not trained the generator yet so they will be very easy for the discriminator to find out whether they are fake or real. But for the next iterations, the next epochs of the training they will be more challenging as you will see when the exercises the input to the generator is random noise from a random normal distribution. So we use torch.randn and then we give it the dimensions of the vector the batch size for the first dimension and the noise dimension for the second dimension.

The batch size for the first dimension and the noise dimension for the second dimension and again the two device so we trigger the method zero_grad. zero_grad on the discriminator optimizer. So we give the real images to the discriminator and save the output in real_outputs and give the fake images to the discriminator and saves the output in fake_outputs. Next we apply the loss function twice one for the real outputs and the other for the fake outputs.

So criterion is given the real underscore outputs and the real underscore labels which is ones in this case and next time is given the fake_outputs and the fake labels which is zero. In this case the first one is a variable we named D underscore x so that you can relate it easily to the equation we have discussed in the slides and D of G of Z so they can relate it to the second part of the equation we are using log 1 minus the G of Z so we apply the backward on both D of X and the backward pass on D of G of Z independently that will accumulate the gradients but will not update the weights till we use discriminator_optimizer.step that will perform the actual update of the weights according to the learning rate that will train the discriminator for K steps.

Next we train the generator we use a random noise tensor of two dimensions batch size and noise dimension and we convert it to the device we are working on and save the output in a variable named Z we use the method generator.zero_grad so that the gradient will be reinitialized or reset before we apply any training we pass the noise to the generator generator of noise and then the output of the generator would be passed to the discriminator and would be saved in a variable outputs.

So what we are doing here is that we are passing random noise through the generator and then passing the output image to the discriminator to decide whether it is real or fake and then we are using the criterion the loss function to decide whether the output is actually real labels or ones. In this case we are trying to maximize the generator ability to generate real images and see the loss the loss would be saved in a variable loss loss will be used to apply the gradients loss.backward that will calculate the gradients according to the loss and then generator optimizer step will update the weights next every ten epochs we are going to visualize the results we use the test_set we have defined above as a fixed random noise and we detach it from the GPU and convert it to the CPU using .detach().cpu() and then we reshape it like this using the view method.

The first dimension would be the batch size which is 16 in this case the second dimension is the channel, which is only one channel. Since it is a grayscale image and then we reshape it using the view method the first dimension will be the batch size. In this case we are using 16 samples. The second dimension is one which stands for the channel since it is a grayscale image of one channel. The third and the fourth dimensions are the width and height of the image.

Twenty eight by twenty eight which is the size of any image in this dataset. We save the output which will be the generated images from the generator that has been trained in the previous steps. Next we use a utility method available in the torchvision using torchvision.utils.save_image that will create a grid of images for the generated images and then we'll save it in the file which we are passing and the second argument by using os.path.join that will create a.

According to the environment we are using either Windows or Linux. The first parameter is visuals so the directory. The second parameter is the name of the file. In this case we are using placeholder .jpg or jpeg and then we format bypassing the epoch number cast it as a string and then use zero filling of six zeros just in case you want to try 10000 or more epochs for the training. Next, nrow is the number of rows for the grid that it will create.

I'm using four rows the padding between each image is going to be 10 pixels and the padding value will be the color one or the white color next to use the generator that was trained for 512 epochs. Remember that the visualized method above is invoked every ten epochs but since we are trying for 512 epochs the last one will not be visualized. So we are visualizing it here. We do the same generator we pass the fixed noise test_set and then detach using .detach() and then detach from the GPU using torch .cpu() and then we reshape using that view first dimension batch size negative 1.

The second dimension 1 for grayscale and then for width and height 28 by 28. And this time I would like to introduce to another method from the torchvision API, torchvision.utils.make_grid and this will return a grid variable a tensor actually of the same specifications of the number of rows, padding and pad value for the generated images. The one that we have used above is save_image which will save the grid to the actual file on the disk.

But this one will return a tensor. Next we convert the grid from bytes tensor to NumPy tensor using grid.numpy and then using a numpy method, the transpose to change the order of the dimensions. So remember that the grid now has the grayscale or the channel as a first dimension and then the width and height and the subsequent two dimensions. While we want it to be the opposite so that we can display it using matplotlib. So we use the numpy.transpose and we use the second dimension and the third dimension as the first two dimensions by indicating one comma two and then the first dimension indicated by zero to be the last dimension.

Finally we use matplotlib API is plt.figure and pass the figure size in inches 16 by 16. And we turn off the axes and use the method plt.imshow to display the image here what we have got after training the model 512 epochs all the samples are the same the generator mode is the same. It is only outputting the mode for digit number one. This is an interesting problem upon which section 2 and Section 3 are based. However I am not satisfied with this result and I will show you another notebook in a moment where I have upgraded the results.

But the point here I would like to highlight that this is a very common problem when training GANs which is called mode collapse at the end of the notebook you find a reference to the Google Colab notebook file that includes the same exercise as this one. If you don't have a GPU you'd like to try to run the code on the GPU in the cloud in the course the material you you'll find another file by the name named section 2 Dash. Version 2 where I have updated the learning rate and the momentum as follows to be learning rate point 0 5 and the momentum point 7 for both the generator and the discriminator as you can see here.

I have trained the discriminator for K steps which is equal to 16. That is the discriminator is trained sixteen times more than the generator over here you can see the output after 512 epochs and you could recognize the digits and they are more like the endless dataset letters. So for example the first one we have got something between eight and 6. The second one is somewhere between five and the same third one fourth one are all five. Here we have got something else.

I'm not quite sure what it resembles but here we have got something that looks like three and then something that looks like it. And then for the third row we have got more lines for the fourth row. We have got 9 9 5 9 so that's much better than the previous result. Still we cannot control the output. We cannot set conditions and we cannot make sure that the conditions are obeyed. These are things that we are going to discuss in deep detail in Section 3 and section 4.

So stay tuned again here you have got Google Colab notebook link for the file which I have also uploaded to the cloud. In case you'd like to try it on Google Colab So that concludes our discussion of Section 2. I hope that you have enjoyed it but to make things more challenging. Here are some ideas that I'd like you to try so implement the same base GAN model on Fashion-MNIST dataset and try some of these ideas so implement the base GAN model we have discussed throughout this section on the Fashion-MNIST dataset and try some of these ideas try to train only on one class on three classes try to fine tune the value of the dropout layers try uniform noise instead of random Gaussian noise and then instead of sigmoid at the generator output try to use tanh and make the required changes and also try to find out the optimal number of pieces for the maxout activation using cross validation and finally try to visualize the gradients.

This is very essential step and there will be some of the discussion in Section 3 and section 4. to inspect the gradient flow in the network. Finally I would like to recommend that you read the original papers on the generative adversarial networks by Ian Goodfellow and his team and the maxout networks in which he proposed that the maxout activation also by Ian Goodfellow and his team. There is a good open source book Deep Learning by M.I.T. press and I would like to recommend that you read chapter six section six point three point one for rectified linear units and their generalization in this case it talks about maxout activation and Chapter 20 section twenty point fourteen which talks about evaluating generative models and how this is very challenging and an open research question congratulations on finishing Section 2.

Thank you and see you in Section 3. Stay tuned.

9.  

-- [Coding Exercise] GAN Evaluation Metrics Inception Score
===========================================================

Hello and welcome to a new coding exercise. And this exercise we're going to learn a new evaluation metric for GAN models which is named the Inception score. Let's get started the Inception score takes its name after the inception architecture proposed by Google in the year 2014. One of the pioneering architectures and convolutional neural networks that performed a high score on the ImageNet classification problem. The ImageNet has thousand classes and the network is designed so that it has thousand output neurons for each label of the classes in the data set.

The architecture of the Inception score allows for very deep networks by having auxiliary networks additional output layers that are put in the intermediate layers. This way they provide gradient flow in the shallow layers of the network meaning that because of the vanishing gradient problem inherent in very deep neural networks the designers of the inception architecture designed an auxiliary output that's a secondary output not actually essentially one for the output.

And this one is placed not at the end of the network but at intermediate places of the network. And then when the loss is calculated on the outputs of these intermediate layers there will be a gradient flow in the network that finds its way through to the very first layers of the network and hence it solves the vanishing gradient problem inherent in deep learning. The idea behind the Inception score that we are going to use a pre trained Inception network to evaluate the performance of the GAN model.

Let's see how this works. the Inception score aims at providing a metric for two values. The first is whether the image is correctly classified by the Inception network. That means if we have a network GAN model that generates images of let's say canines we feed the image to the inception network which is pre trained on thousand classes of image it and these thousand classes they do actually include images of canines and cats and so on. So the output of the network will be a classification distribution over all of the thousand classes with a peak, a very high value a very high probability value at the label corresponding to the canine class by reporting this value what we get is that we have a measure an objective measure of the image quality that is very close very similar.

And this was proven by the authors of the Inception score paper. This is very close and very similar to the evaluation of a human being. That means if given the same image to a human and given the same image to an inception network and calculating the score the values whether the image is real or fake of a desired object a canine for example will be very close. So that helps us in the design and evaluation of generative adversarial networks because we need an objective way to assess the quality of generated images.

Remember we had to look every epoch what the images are generated or every ten epochs and then manually inspect the quality of the images. This is not feasible at larger scale and it cannot be very decisive in deciding the best model so there must be a mechanical a mathematical term that helps us better evaluate the performance of GANs on the other hand. We wanted the model to output a variety of classes not necessarily as we done an MNIST number one or number two or number three but we want it to have a uniform distribution equal number of samples generated from each class.

For example if the image rates the number 1 2 9 or 0 to 9 then generate 10 numbers for 0 then images for one thing images for 2 and so on for each class in the dataset should have its equal proportion to the total output of the images. This is called the inter-class diversity and this is also one of the qualities that is measured by the Inception score. So as you can see here on the left side of the slide we have the kind of probability distribution that is given when we have a classifier and given an image and then we have a high probability at the label or the neuron corresponding to that class.

And this is how it looks like if we try to plot the output of the softmax layer and on the right side we have a uniform distribution. It means if we have three classes elephant cat and dog then we want each one of them to have the same number of generated images done by the GAN model. So having these two kinds of probabilities that gives us two probability distributions and if we recall we have a distance measure between probability distributions that is the Kullback-Leibler divergence which is a member of the f-divergence distance metric family in the probability theory we have already introduced one of these divergence distance metrics which is Cauchy-Schwarz distance in the first section and the following sections we will have a very deep comprehensive discussion of the Kullback-Leibler divergence but for the sake of understanding the Inception score let's put it simply this way the Kullback-Leibler divergence is a distance metric between two probability distributions the higher the difference between the two probabilities the higher the Kullback-Leibler divergence distance that more similar the two probability distributions the lower the value of the Kullback-Leibler divergence distance so we can benefit from this kind of metric to actually maximize the inception score because the inception score will be at the end the mean or the average Kullback-Leibler divergence between the two distributions the distribution of the class labels given the generated images and then their average values over all the samples in the dataset or the generated dataset for all the classes so this way we have the conditional probability working on the horizontal axis and the probability of the class label on the vertical axis and then by giving them as to probability distributions to Kullback-Leibler divergence.

We make sure that we get a very high distance meaning that we always get this kind of distribution. The ideal label distribution when the image is classified correctly by the inception network and also we are getting a uniform distribution when the images are very diversified and different from each other for different labels. So let's see how this is formulated in terms of mathematics so the mathematical formulation is the mean of the Kullback-Leibler divergence between two probabilities probability of Y given X and the probability of Y Y here stands for the label the class label and X stands for the generated images.

So what we are trying to calculate is actually a distance the further the distance between these two probabilities the better the Inception score the more close they are to each other the lower the Inception score and hence it is worse. Some caveats happen when using the Inception score. For example our network our GAN model may generate many images for each class and they are all equally likely according to a uniform distribution. But for each class it will output the same image and will not output diversified images for the same class.

The inception score helps us to measure the inter-class diversity but it still fails on the intra-class diversity. This is one of the open questions. If you happen to pass by any of the research papers addressing this problem please pass it to me and share it with your colleagues. In this course on the course forum. Otherwise it may be a good motivation for you to try to come up with a formulation that helps also to measure at the same time the inter-class diversity so let's get to coding.

I will use the same coding exercise we have used in Section 2 and then I will add the Inception score function at the end of the file you will find a file with this Inception score implementation with the name of the same lecture in the course materials. So what we do now is to scroll down at the end of the notebook. So you'll find a section with the name evaluation metrics Inception score and here you find actually the source where I have got this PyTorch implementation so we started by using a new object of the class Inception v3 which is a pre trained model for the Inception network and we call it a variable net.

And then we define the inception score function or subroutine that will calculate the Inception score given some images. And please note here that the images according to the paper you pass all the images in the dataset or you pass groups of images each one has 50000 images. So the Inception score doesn't work by applying it on only a few number of images but applied on a large number of set of images or groups of images. Each one has also a large number of images and then you average the score over these groups so we initialize an array scores that will hold the values of the inception score calculated on every batch of the mini-batches available in the data set of images we can create the number of steps how many times we have to iterate over the dataset giving the batch size and then we loop over the data set and then we divide the images into batches each one will have a start index and end index so that between the start and the end index we have a batch size let's say sixteen for example and then we pass the mini batch of images to the Variable method of the PyTorch autograd so that it can be a variable with gradients we pass it to the network and then Inception network returns to values the actual output from a linear layer and the output from the auxiliary layers in the intermediate layers of the network we append that to the scores and we convert the scores to a PyTorch tensor using a concatenation by taking the list of scores together as tensors remember at this point that each image will have a tensor or a vector of thousand values because this pretrained model is trained on ImageNet thousand classes thousand labels so for each label we have one output that gives us thousand neurons, thousand outputs and over these thousand neurons we have a probability distribution just like the one we have seen in the image on the slides and then we started the actual calculation of the conditional probability and the marginal probability and best them to the Kullback-Leibler divergence we need to best the output from the inception network to the softmax because it doesn't have a softmax layer at the output by default it has a linear output and then to calculate the conditional probability we just take the output along the first dimension that is dimension indexed by number 1 this is the second dimension this will give us the softmax probability distribution over all of the classes for all the images in the dataset and we'll call that probability of Y given X and then we do the marginal probability or the probability over the same class.

But for all the images and the data is. For example if the first class is the cat we want to sum and average all of the probability outputs for all the images in the same class. And we do the same for the second class and the third class. And then you can see for the conditional probability probability Y given X we use the softmax along the dimension one. But for the marginal probability probability y we use the same value but we do the averaging with the index zero for the dimension.

That means over all the images in the dataset not all the columns. And then we calculate the Kullback-Leibler divergence according to the formula conditional probability multiplied by the log of conditional probability minus the log of the marginal probability. And then we take the average Kullback-Leibler divergence distance and return it and the final score over here. I try to test the Inception score on only one batch generated by the generator trained in the previous lectures and it needs some preprocessing because first of all it has only one grayscale channel.

We need to convert that to RGB and it also size of twenty eight by 28. We need to resample this one to the size of 299 by two hundred ninety nine so that it is compatible with the design of the inception network and then we pass the resized images and the batch size to the inception score. We return a tensor variable we use score.item to get the actual value inside the tensor. You can see here we are getting a very low value for Inception score point 0 0 0 3.

This is very low value. First of all because we are training on this data set which is a data set that the Inception network is not trained on. That means it has different labels different categories from the ones available in the ImageNet dataset. That's one of the caveats. The other thing that we are calculating in the Inception score over only one mini batch you can see how computationally demanding it get. So when we tried to calculate Inception score over all the mini-batches in the data set and we do that every epoch.

So it is a very demanding computationally expensive process but it is a must know a tool in your toolbox as a machine learning and deep learning engineer especially when working with GANs because you cannot always use the subjective way to evaluate the performance of your networks. You need to have an objective metric and then if you scroll down you find that I try the same experiment over a batch of real images of the original dataset. Again a mini batch of 16 images and we do the same and we get a little bit higher.

Inception score point 0 0 0 7 which is even though it is higher than the ones we got for the generated images but it's still very low for the same reasons we have just explained. I hope that give your idea about the complexity of evaluating GAN models and about the novelty and creativity in this area. Well you have to find new metrics to evaluate against models. It's an open area of research. I encourage you to read more about this one and here is a reference to the original paper where the Inception score was first introduced.

Thank you and see you in the next lecture.

10.    

[Coding Exercise] GAN Evaluation Metrics FID Score
======================================================

Hello and welcome. And this coding exercise. We continue our discussion of generative adversarial networks and the evaluation techniques and metrics and this exercise to use the Fréchet Inception Distance or FID score. Let's get started Fréchet distance is a distance metric that is best fit for curves and polygons and it is perfect for pattern matching and shape matching it doesn't require the two curves to be identical it allows them to be within a given approximation range from each other.

As an example of this if you are going to walk your dog outside you have a leash and the dog is on the leash. It is allowed to be on the same path with you but it can also go on its own path as long as the leash allows for it the shortest leash that is going to allow you and your dog to walk on the same path on the same curve without diverging from each other is the minimum Fréchet distance. This is the intuition behind the Fréchet distance the real intuition behind using it for generative adversarial networks and their evaluation is that even though they we have studied the Inception score in the previous lecture it is found to be inferior to the Fréchet Inception Distance because it doesn't take into account the statistics of both the real image and the generated image.

When I talk about the statistics I mean the average the mean and the variance which define almost any statistical Gaussian distribution Fréchet distance or the Fréchet Inception Distance is designed to allow to study the mean and the variance of both generated images and the real images using the Inception network unlike the Inception score which uses the final output of the inception network in the Fréchet Inception Distance. We are going to use the last layer or the second to last layer before the last output in the Inception network.

That means we are using the inception network as a feature extractor. A very common concept in deep learning is called representation learning. It was found that in deep architectures such as AlexNet, VGG and ResNet and Inception network some particular layers are valuable as feature extractors because all of the convolutional layers in the network they learn the different features such as curves corners edges and so on of the input image and then they keep on accumulating this knowledge in a hierarchical topology from the lower layers up to the higher layers in the network till we arrive at a flattened vector which is a valid feature representation that maximizes the inter-class difference and minimizes the intra-class difference meaning that images for the same object will have similar features and images from different objects will be very far from each other according to this feature representation.

You can think of it as similar to PCA or principal component analysis for feature reduction. where you try to project the input features to different coordinates that try to maximize the difference between the features and allow us to have linear decision boundaries between the classes. So the idea of Fréchet Inception Distance that we are going to use the statistics of the mean and the variance of the features learned by an inception network on both the real images and generated images and then we calculate the Fréchet distance between this is that using these statistics let's put it into a mathematical formula.

Here we have the mean mu R which is the mean of the real images and mu of G, the mean of the generated images. Of course here I mean the mean of the feature representation of these images. We subtract them and then we have the square of the mu R and mu G difference and then we have a new operator TR which is the trace operator. It allows us to take the sum along the diagonal of a matrix and since this operator is applied to the variance the variance here is taken for the representation of the real images and the generated images.

So because we know that the variance matrix is the aggregate we take the sum along the diagonal. Then we take the product. Sigma are multiplied by Sigma G and then we take the square root multiply by 2 and subtract from Sigma R plus Sigma G A very simple equation. It gives us a scalar value at the end which is a distance the Fréchet distance between the generated images and the representation and the real images and I mean the representations using the mean and the variance of the feature representation learned by the inception network.

Let's see how this can be applied using Python. You can find the notebook associated with the section materials and it has the title evaluation metrics. FID score and of course is the source where the source code is being benchmarked on so we need a mechanism by which we take the output of the second to last layer in the inception network the code over here defines a new partial Inception network that inherits from torch.nn.Module and then it instantiates the pretrained Inception v3 network.

And it defines the layer with the title Mixed_7c which is actually the output from the last block of the inception network. Just before the final layer or the output layer and then it defines a hook because by default and I went into this error a loop that allows PyTorch internals to give you the output of this internal block directly when you pass the inputs through the forward method it is a syntactic sugar so that you can apply the output on an intermediate layer of the network then inside the forward path we pass the inputs through the inception network and then we get all the outputs we extract the output we are interested in which itself Mixed_7c output and put it in a variable activations and this output will have the shape of the number of batches multiplied by 2048 channels by eight by eight.

And then we need to reduce that to one by one so that we have only 2048 which is actually an ideal representation vector for any image that passes through the network. You find similar concept in AlexNet and VGG-Net and ResNet and so on. So in order to reduce the dimension we use average pooling 2D we pass the activations through the average pooling of kernel size one by one and then we view the output vector the output matrix or tensor using activations.view with the first parameter saved for the batch size and then 2048 which is the number of features in each sample or each image then we return the outputs and start by creating a new object of this partial Inception network and define a method.

FID score which would perform the actual calculations of the Fréchet Inception Distance so unlike the inception score we did in the previous lecture in this one we have both the real images and the generated images passed to the FID score method we accumulate the results or the representations of both the real and rated images in two lists, real_activations and generated_activations we loop over the number of steps in that dataset given for simplicity the number of steps here will be 1 because I'm passing at the later step only 1 mini batch but I encourage you to try it with the larger samples and see if it is computationally feasible because technically what I found through these two exercises is that the evaluation metrics whether it is Inception score FID score they are both computationally demanding and require a lot of computing resources and taking into consideration that you have to do these evaluations after every epoch or every 10 epochs of training the network which is itself is two networks to the generator and the discriminator.

So it is a bit of a headache to deal with every exercise you are going to do it again but it is a necessary evil that you must work with because it gives you an objective evaluation metric for GANs unlike the subjective manual inspection of every image that is generated by the network we loop over the number of steps we define the boundaries for the mini batch at the start and the end index. We slice the arrays the real images and the generated images by these indices we pass them to Variable to make them into PyTorch tensors with gradients then we pass them through the network and will have the feature vectors we are interested in.

We append them to the real activations and the generated activations list. Now we are due to calculate the statistics to the mean and the variance over these features vectors. What we do is to stack the list into a PyTorch tensor and then detach it from the gradient because it has gradients as a variable or a PyTorch variable. So we need to detach it using the method detach and then convert it to CPU if we have been using GPU and convert it to NumPy so that we can perform the calculations we are using the NumPy API for mean and variance and trace.

Operator it becomes a straightforward from here applying the formula in the previous slides directly in terms of NumPy using numpy.mean for the average, numpy.cov for the Sigma or the variance. Note here that rowvar equals False means that the rows will be on the horizontal side and the observations will be on the vertical side. Again you wrap your head around it because if you do rowvar equals True that will have the opposite the transpose of The Matrix.

Just give it a couple of tries to make sure that you are comfortable with this kind of visualization of the matrices in your head and then we subtract mu two from mu one which is the mean from each other and we do the squaring by doing the dot product of the diff twice because it is a matrix and squaring a matrix is not simply as just the squaring every element in the matrix but you apply that product to the Matrix so we can get a squared matrix then you apply the dot product to the variance 1 and 2 and then you apply the square root using the scipy linalg sqrtm.

This way you will have the square root applied correctly to the matrix and disp equals False will allow you to return the errors if any and I have ignored it using underscore and sometimes a square root. In this case will be a complex number with imaginary part we can get rid of it if it exists and we do that using sqrt_product.real which returns the real part of the imaginary object and then we apply the trace operator on the square root and score product.

And finally we calculate that FID score using the terms we have just calculated in the previous steps so using the term as we have calculated in the previous steps we apply the numpy trace operator on the Sigma 1, the variance of the real images. Now the trace on Sigma 2, the variance of the generated images minus two multiplied by the product trace which we have just calculated and we sum the difference and return the overall FID score. Next I need to test this method on some real and it generated images from our model so we get a batch from the train loader convert it to the device, CPU or GPU and then reshape it to twenty eight by twenty eight.

Repeat the RGB channels and upsample the image 299 by 299 and do the same for the generated images by applying the generator on the fixed noise vector. Do the same steps and finally pass the real images and the generated images to. FID score method and get the score printed here we've got 140. Please note here unlike the Inception score in the previous lecture where a small value meant a bad generator model and the higher value meant a better model.

In this case though FID score a small value near zero is the best larger value is bad. So they are opposite to each other. Don't be confused by that. Also one more thing the inception for a shared Inception distance. If I did is more sensitive to variance it means I would encourage you to have a better evaluation whether the images have high variety of classes and objects in your dataset. So it is actually even as recommended on Twitter by the inventor of the generative adversarial networks.

Ian Goodfellow. He also thinks and believes that FID is superior to the Inception score which he has contributed to the authoring of the paper of it himself. Again if you are interested in reading more about this topic you will find the original paper as referenced on the screen appendix A1 is the one concerned with the Fréchet Inception Distance. FID because the rest of the paper discusses a new technique for training generative adversarial networks and FID score is only a secondary contribution of the paper so cut it short and get to appendix 1.

If you are specifically looking for the FID you can also read the rest of the paper to help you understand better the other ideas and methods existing in the area of GANs. I would like to share with you for inspiration. A new paper published using deep reinforcement learning and generative adversarial networks together to teach an unsupervised way a the computers to draw and paint from real images or portrait images. And it is done completely unsupervised.

And if you scroll down here you can choose any of the images in this dataset. I would show you how it arrives at the painting and learning how to do doodles in a nice interesting way. So this is an inspiring idea inspiring topic where it merges both reinforcement learning and generative adversarial networks to promote the unsupervised learning of such skills and it can be applied to robotics that can be applied to game rendering it can be applied to many potential ideas and the paper I believe is done by DeepMind which is part of Google Alphabet.

Thought that was inspiring thank you and see you in the next lecture.

11.   



