# Copy this file to `.env` (same folder) if you want to enable optional OpenAI API steps.
#
# IMPORTANT:
# - There are no legitimate "free OpenAI API keys". Do not use or trust any site claiming to provide them.
# - Fine-tuning and API calls require a funded OpenAI account.

# Optional: enable OpenAI sections in the notebook (0/1)
RUN_OPENAI=0

# Optional: OpenAI API key (required only if RUN_OPENAI=1)
OPENAI_API_KEY=

