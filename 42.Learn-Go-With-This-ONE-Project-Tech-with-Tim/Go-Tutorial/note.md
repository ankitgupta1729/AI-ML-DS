1. start with `basic.go`. 

2. Starting the Project:

we will build a betting game while spinning the wheel with certain amount.

If a row has the same symbol then you win.

start with `main.go` file.